# v2.5.x_kw_001_knowevo_core.sql —— 迁移文件说明（T-03 落地）
**事实源**: [备忘录 10-A2 §2](../../../../../02-技术方案.md) 的字段级 DDL（12 表全文），本文件不复制 DDL——**实现时以备忘录 10 为唯一源拷贝**，避免双源漂移。

## 落地规则
1. 文件名按上游惯例补全版本号（`v2.5.x_kw_001_knowevo_core.sql`，x 以上游当前 tag 为准）；
2. 只增不改铁律：`deploy/sql/migrations/` 既有文件禁碰；
3. 索引与 GIN 一并写入；`kg_entity_t.embedding` 列类型由 pgvector 探测结果二选一（备忘录 10 §2 注）；
4. 幂等性：CREATE TABLE IF NOT EXISTS / 显式 DROP IF EXISTS 策略对齐上游迁移文件风格（T-03 读 2 个上游样例后定）；
5. 验收：本地 migrate 两次无 diff + `test_models.py` 全绿。
