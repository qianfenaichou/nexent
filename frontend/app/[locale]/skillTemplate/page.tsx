"use client";

// Direct-URL entry for the skill-template library (T-20). Navigation item
// is seeded for SU/ADMIN via v2.5.5_kw_007_skill_template_rbac.sql.
import SkillTemplatePanel from "@/features/skillTemplate/SkillTemplatePanel";

export default function Page() {
  return <SkillTemplatePanel />;
}
