/**
 * Controls the "click a citation marker -> locate the source card in the
 * right panel and highlight the cited chunk" behaviour used by both the
 * legacy /chat page and /newchat.  The feature implementation is kept in
 * place; flip this flag to true to re-enable it.
 */
export const ENABLE_CITATION_CLICK_HIGHLIGHT = false;
