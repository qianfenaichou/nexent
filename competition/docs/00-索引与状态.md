# 00 · 索引与状态（competition/ 工作区）

> 建立：2026-09-22（文档整理轮）。本文件是 `nexent/competition/` 内部的**唯一状态索引**：哪些任务是活的、哪个文档是哪一代、完成度多少、初赛前必须补什么。
> 上级索引：[`../../../AGENTS.md`](../../../AGENTS.md)（AI 入口）· [`../../../README.md`](../../../README.md)（人工总索引）· [`../../../00-总纲与决策依据.md`](../../../00-总纲与决策依据.md)（决策依据）。
> **2026-09-24 晨间更新**：夜间永续任务池已收口（8 done + T5 partial 45/50），未完成任务总账已归档至 `archive/交接记录-2026-09/`；**当前开放项唯一入口 = 工作区根 `README.md` §五 + `NEXT-SESSION-PROMPTS-2026-09-24.md`（四份任务：Q1 严格版升级 / Q2A 交付文档 / Q2B 技术实验 / Q3 key 规划）**；夜间证据归档于 `archive/overnight-2026-09-24/`。
> 任务清单的**权威来源永远是 `git log` + 产物文件存在性**，不是任何文档的状态列。

---

## 一、任务全景状态（T-00 ~ T-29）

判定依据 = commit 存在性 + 产物文件 + brief 的 Evidence 节。**「Evidence」列给出可自行验证的锚点。**

| 任务 | 标题 | 状态 | 依据 |
|---|---|---|---|
| T-00 | 仓库引导（拷入 knowevo 框架包） | ✅ | `8e82e45f0` |
| T-01 | 部署基线 + E0 微基准 | ✅ | `8e82e45f0`；`deliverables/T-01-{agent-published,chat-conversation,models-registered}.png`（2026-09-14 真实平台界面） |
| T-02 | 医疗数据批次1（58 份语料 + 解析体检） | ✅ | `2ba53e566`；`corpus/registry.csv` 58 行；`corpus/parse_checkup.py`；三批核查报告 |
| T-03 | 知识模型 + 12 表 + 接线 | ✅ | `f0fedebc6`；`backend/database/knowevo_db.py`；`deploy/sql/migrations/v2.5.5_kw_001`（**恰好 12 张表**） |
| T-04 | 本体种子构建 v0 | ✅ | `3bafa4e0d`；`backend/services/knowevo/ontology_service.py`；本体实况 v1.1.0 = 10 类 / 10 关系 |
| T-05 | 本体工作台 | ✅（e2e 截图欠账） | `f0070be1e`/`f9dccc437`；`frontend/features/knowledgeGraph/`；`docs/tasks/T-05-receipt.md`（**唯一一份 receipt**，其中 e2e 截图未勾选） |
| T-06 | 抽取流水线 v0（表格确定性通道） | ✅ | `b1da4142a`；`backend/services/knowevo/kg_service.py` |
| T-07a | 图存储 GraphStore 抽象 + PoC | ✅ | `ea9a7bd17`；`graph_store.py`；`docs/poc-graphstore.md`（多跳 p95=12.5ms / supersede p95=22.7ms @2 万实体） |
| T-07b | knowevo_mcp（双注册 5 工具） | ✅ | `fefe8eb3c`；`mcp_servers/knowevo_mcp/`；`backend/tool_collection/mcp/kg_tools.py` |
| T-08 | 接线集中 | ✅ | `d47f02248` |
| T-09 | kg_multi_hop + 决策卡 + 跳数标定 | ✅ | `a5f7deb05`；`version_pin.py`/`decision_service.py`；`cost-ledger` t09-curve 行（depth=2 饱和） |
| T-10a | 评测基线（E1 + testset v1 seed 20 题） | ✅ | `b0839ca29`/`732914057`；`corpus/testset-v1-seed.json`；`deliverables/e1-baseline-report.json` |
| T-10a-2 | E1 基线提交 + 合回 develop | ✅ | `732914057` |
| T-10b | 评测完整版（pass^2/pass^3 原生 code 评测器 + 120 题） | ❌ 未做 | 无独立提交；**120 题未建**（仅 20 题 seed + 8 题判别集）；能力部分被 T-22 吸收 |
| T-11 | 标准对齐 + 增量进化 | ✅ 由 T-21 承接 | `601bb0e21` 起 6 个提交 |
| T-12 | 进化看板（timeline 动画） | ❌ 未做 | `frontend/features/` 无 evolution 面板 |
| T-13 | Skill 模板库 | ✅ 由 T-20 承接 | `3ca491f66`/`8d2d5ac8f`/`8c0f42eca` |
| T-14 | 资产看板 | ⛔ 已放弃 | `wave-plan.md` 明列「不做：冻结砍序第一」 |
| T-15 | 政务迁移实验（附加分①） | ❌ 未启动 | 无语料、无迁移报告 |
| T-16 | 参赛交付物装配（旧编号） | ✅ 由 T-23 承接 | 见 T-23 |
| T-17 | 经验记忆（可选） | ❌ 未做 | ADR-0004 已定不引入 mem0 |
| T-18a | D5 三件套（导航 / active 端点 / g6） | ✅ | `62537db9c` |
| T-18b | D1 事实业务时间（valid_at 显式写入） | ✅ | `21f9e8ad6` |
| T-18c | D2 诚实分母 + D3 答案级溯源 | ✅ | `f3819e4a7` |
| T-18d | D4 权威度感知检索 + 每文档配额 | ✅ | `f1ae13b31` |
| T-19 | 决策卡生产入口 | ✅ | `db249a585` |
| T-20 | Skill 分层 + 模板库（D6） | ✅ | `3ca491f66`/`8d2d5ac8f`/`8c0f42eca` |
| T-21 | 标准对齐器（创新主轴） | ✅（数字口径待定） | `601bb0e21`→`0c6e4bc77`；P/R 首测 precision=0.00145/recall=0.111 → 改 topic 级 recall 9/9。**2026-09-24 金标扩容 batch2**：总话题 16→45（verified/unverified/UNC=37/8/12），recall 分母 9→37、阴性集 0→12；分母变更后的 recall 重跑待评测任务 |
| T-22 | 消融 A1-A4 × 题型 | ◐ runner 就绪待跑全量 | `e68fbf5d3`/`f8a8fb850`；`deliverables/e2-ablation-report.json`（`partial:true`，**A3 未跑**）。**2026-09-23 复核轮**：`ablation.py`（1287 行）B1-B6 全解除、`test_ablation.py` **34 passed**（全 mock）；D1 判别性真库实测 **t_v 2022=134 ≠ 2025=244 → 放行 E8**；图谱已填充（309 实体/243 关系）→ **E8 Δ 结构性≈0 风险解除**。resume 四段命令见 `deliverables/t22-resume.status.json`（真实 LLM 全量待摄取收口后执行） |
| T-23 | 交付物取证与装配 | ◐ 进行中 | `748299e6d`/`cb028b740`/`87d4b900a`/`4654bf810`；13 张截图 + 6 份装配文档；**缺「示例问答 ≥3」+ E2 数字回填** |
| T-24 | 抽取运行诊断落库 | ✅ | `ac233818e`；`v2.5.5_kw_009_extract_run_diagnostics.sql` |
| T-25 | 决策卡 as_of UI | ✅ | `9049cbaa0` |
| T-26 | 决策卡生产入口取证修复 | ✅ | `f0175fc5a` |
| T-27 | chat QA 链路解锁 | ✅ | 3 张 `deliverables/T-27-chat-qa-{1,2,3}.png`（2026-09-23 00:57–01:03）；`chat-qa-summary.json` `errors: []` + MCP 侧 **71×`CallToolRequest`**；真库 `decision_card_t` 三行逐题对应（`e2e0a66f`/`348c8a4d`/`962c9cf2`，第 2 行 UUID 后缀与截图互锁）。诊断报告 `docs/verification-reports/t27-chatqa-unlock.md`；坑 #64/#65 |
| T-28 | 判别题集 v1（8 题双金标） | ✅ | `10cecd27b`；`corpus/testset-discriminating-v1.json` |
| T-29 | 决策卡 used_tokens 观测 | ✅ | `887912443` |

> ⚠️ **证据留存的三个已知欠账**：
> 1. 除 `T-05-receipt.md` 外**没有任何任务留有独立 receipt**。完成证据只能依赖各 brief 的 Evidence 节 + git commit + 三份台账。**发新任务时请务必按模板补 receipt**，否则日后无法判断「做完没有」。
> 2. **T-03 与 T-11~T-17 没有独立简报文件**（`tasks/` 下无 `T-03-brief.md` 等），其内容散在 [`wave-plan.md`](wave-plan.md) 与 [`tasks/README.md`](../tasks/README.md) 的任务总表里。
> 3. `T-05` 的 e2e 截图（本体树 ≥30 节点 + diff 三色）在 receipt 中**未勾选**，属欠账。

---

## 二、本目录（competition/docs/）文档分类

| 文档 | 代次 | 状态 | 说明 |
|---|---|---|---|
| **`00-索引与状态.md`**（本文件） | 🆕 v4 | ✅ **活** | 唯一状态索引 |
| `dev-design-doc.md` | 交付 | ✅ 活 | 开发设计说明（186 行，官方六节骨架，**含括号占位注释待落实**）→ 官方 Word 的内容母本 |
| `agent-config.md` | 交付 | ✅ 活 | 智能体配置说明（模型/工具/知识库/Skill 四节） |
| `call-graph.md` | 交付 | ✅ 活 | 调用关系图（Mermaid） |
| `reproduce-README.md` | 交付 | ✅ 活 | 复现步骤（含部署方式） |
| `citation-map.md` | 交付 | ✅ 活 | 引用对照表（4 处引用错误已清零） |
| `skill-mechanism.md` | 说明 | ✅ 活 | Skill 机制说明 |
| **`kb-file-guide.md`** / **`mcp-file-guide.md`** | 交付 | ✅ 活（2026-09-23 新增） | **P0-3 交付物**：官方要求的「知识库文件说明」与「MCP 文件说明」两册独立成文、成组提交。均含「数字 → `file:line` 出处」对账表 + 已实跑的复现命令 + 如实列出的已知不一致清单 |
| `e0-baseline.md` / `e2-ablation.md` / `poc-graphstore.md` | 实验 | ✅ 活 | 三份实验说明 |
| `pitfalls.md` | 台账 | ✅ 活 | 踩坑台账，**已到 #74**（编号连续无跳号，已核） |
| `cost-ledger.md` | 台账 | ✅ 活 | **权威数字源**（T4/T5 已于 2026-09-24 晨间补登） |
| `evolution-log.md` | 台账 | ✅ 活 | 知识演进台账 |
| **`rubric-scorecard.md`** | 抢分 | ✅ **活（2026-09-24 新增）** | **初赛抢分矩阵**：官方四维各 25% × 现状资产 × 强度 × 缺口 × 补齐动作 × 归属任务；含附加分与「全未认领」项清单。**开工前必读**——本轮所有任务按它排力气 |
| `wave-plan.md` | 调度 | ⚠️ 部分过时 | 波次表状态列已加过时标注；**D1-D6 六个洞定义仍有效** |
| `plan/`（4 份） | Gen2 快照 | 📦 冻结 | 见 [`plan/README.md`](plan/README.md) |
| `verification-reports/`（8 份 + README） | 核查 | ✅ 活 | 语料三批核查（已合并为 `corpus-provenance.md`）+ **`platform-facts-redline.md`（平台事实红线，写对外材料前必读）** + `t22-readiness.md`（T-22 阻塞清单） + `t27-chatqa-unlock.md`（chat QA 解锁诊断，T-27） + **`t21-alignment-caliber.md`（T-21 对齐口径决定书）** + `official-site-2026-09-23.md`（官网核查）+ **`sessionB-closure-pg-bench-2026-09-24.md`（写路径 54.15× / 等价性对拍 6/6 实测包）** + `citation-verification-2026-09-23.md`（文献四要素核验） |

---

## 三、赛题对标矩阵（官方 5 条任务要求）

证据强度：**强** = 代码 + 实验数据 + 真实截图；**中** = 仅代码或仅数据；**弱** = 仅文档声称；**缺** = 无。

| # | 官方要求 | 对应实现（真实路径） | 强度 | 缺口 |
|---|---|---|---|---|
| 1 | 多模态异构数据**资产认知**（统一识别/理解/资产标识） | `ingest_service.py`、`pipeline/ingest_assets.py`、`doc_asset_t`（asset_no/authority_level/published_at/血缘）、`corpus/registry.csv` 58 份 | 中 | 前端**资产看板已砍**（T-14）；「多模态」以文本/表格/图文首版为主，缺量化实验 |
| 2 | 低资源本体**半自动构建** + 持续动态更新 + 与**行业规范对齐** | `ontology_service.py`（种子+两级提案+auto_accept 0.85）、`pipeline/build_ontology.py`、`alignment_service.py`、`pipeline/diff_guidelines.py`、9 对双语 prompt | 中偏弱 | **「与规范对齐」的 P/R 数字弱**（precision=0.00145 旧口径 → topic 级 recall 9/9 新口径，口径未定死）；缺「半自动 vs 人工」效率对照实验 |
| 3 | 深度集成 **MCP + Skills**，检索-推理**双驱动** | Local MCP：`tool_collection/mcp/kg_tools.py`（**5 工具**双注册，单 schema 源）；独立 FastMCP：`mcp_servers/knowevo_mcp/server.py`；双驱动：`decision_service.py`；Skill：`competition/skills/*` 4 组 + 平台 `ag_skill_info_t` 4 条 | 中偏强 | ~~平台内「智能体真实调用 KG 工具」无成功证据~~ → ✅ **已补证（T-27，2026-09-23）**：MCP 服务端 **71×`CallToolRequest`**，推理轨迹含 `kg_search`/`kg_multi_hop`/`decision_card_render`（截图 `T-27-chat-qa-*.png`）。余下缺口：仅 5 自研工具，规划的 `ontology_diff`/`alignment_*` 等未实现 |
| 4 | 跨文档**多跳推理** + 决策链路**可溯源** + 沉淀可复用 Skill 模板 | `kg_multi_hop` + `version_pin.py`；`decision_card_t` + `kg_evidence_t`；`skill_template_service.py` + `pipeline/mine_skill_templates.py`；4 张版本对比截图 + `test_decision_service.py::TestVersionPinnedWalk` | 中偏强 | 图谱摄取 **35/120 段**（09-23 13:05 psql 直查 entities=298 / relations=207；batch-A/B 后台续跑 09-24 05:30 实测**已停**，需起服务后续跑）；跨文档规模证据不足；多跳 depth=2 饱和 |
| 5 | 智能体基于 **Nexent 平台可运行** | `deliverables/T-01-*.png`（发布/对话/模型注册，2026-09-14）；`v2.5.5_kw_010` 给 `knowevo_assistant` 绑 5 个 KG 工具；**`deliverables/T-27-chat-qa-{1,2,3}.png`（2026-09-23：完整 `/api/agent/run` 3 轮全绿 + 真库决策卡可回溯）** | 中偏强 | ~~带 KG 工具的问答链路当前失败~~ → ✅ **已跑通（T-27）**：3 轮问答 `errors: []`、智能体真实调用 5 个自研 KG 工具（MCP 侧 71×`CallToolRequest`）。余下弱点：截图页脚为 fork 部署版本 v2.5.1，与对外叙述的 v2.6.0 需加 fork 时点限定一句 |

---

## 四、模块完成度估算

| 模块 | 完成度 | 依据 |
|---|---|---|
| 后端服务 | **~90%** | `backend/services/knowevo/` 24 模块 + 9 pipeline；10 个迁移（`kw_001`~`kw_010`）；路由双挂。缺 T-10b 完整评测器、资产激活生产服务 |
| 前端页面 | **~65%** | 实有 4 feature（`knowledgeGraph` 4 文件 / `decisionCard` 3 / `skillTemplate` 1 / `agentAutomation`）；**assetDashboard 与进化看板不存在**；T-05 e2e 截图欠账 |
| MCP 工具 | **~85%** | 5 工具双注册、单 schema 源；规划的 `ontology_diff`/`alignment_*`/`kg_evolution_trace`/`asset_search`/`evidence_verify` 未实现 |
| Skill | **~80%** | 4 个领域 Skill 真实落库 + `competition/skills/` 4 组（SKILL.md + config + schema）+ 模板挖掘；NL2Skill 演示未做 |
| 评测与实验 | **~55%** | E0/E1/PoC/四探针/E8配对 ✅；E2 partial（A3 缺）；120 题未建；T-21 P/R 弱；政务迁移 0 |
| 交付文档 | **~75%** | 5 份 Markdown 齐（dev-design-doc / agent-config / call-graph / reproduce / evidence-index）；**Word 未产出**；截图缺 1 类 |
| 平台配置 | **~95%** | `deliverables/agent-config.json`（模型 9 档 / 工具 5 / 知识库 58 / skills 4 / duty_prompt / 租户 id），来自真库导出 |
| **初赛可提交度** | **≈ 70%** | 初赛为纯材料评审：技术底盘 ~85-90% × 交付层 ~55-60%（Word 套版稿 29 页已出 / 示例问答 3 张闭环 / KB·MCP 两册成文 / 金标 45 话题；缺口 = 封面个人信息 + 单一《智能体设计说明》装配稿 + 代码附件未打包 + 4 张截图待重拍）加权 |

---

## 五、初赛前必补清单（按阻断度）

时间基线：2026-09-22 → 2026-11-30，**剩约 69 天**。判断：**没有「来不及」的项**，卡点主要是外部依赖。

**P0（硬阻断）**

1. **产出官方 Word 开发设计文档**：现有 `docs/dev-design-doc.md` 是 Markdown 母本，需按官方模板（`【赛题3-模板1】创新赛-开发设计文档-学校-队名-队长姓名-手机号`）转 Word。**模板下载链接已锁定（2026-09-23 官网核查，见 [`verification-reports/official-site-2026-09-23.md`](verification-reports/official-site-2026-09-23.md) §五）**：`backplatform/#/download?fileId=16462&edocId=M3T1A82N1301249787291177249`，**需登录态下载（实测重定向 UniPortal 登录）→ 待人工从已登录的 Edge 下载入库**。母本六节与官方要求六节已逐节对应；所有「（同 05 §x 注入）」类占位已清除。⚠️ 官网明文赛题3 赛程**只到中国总决赛**，且「材料无法复现 = 取消晋级/获奖资格**。**进展（2026-09-23）**：套版稿已出 [`deliverables/【赛题3-模板1】开发设计文档-套版稿-2026-09-23.docx`]（29 页，postcheck 六节齐/无 None/占位；生成器仓外 `~/kw-docx/generate-taoban.py`）；**余**：①封面四项个人信息【学校/队名/队长/手机号】待填 ②视觉验收待 subagent provider 恢复。
2. ~~**补齐「示例问答截图 ≥3」**~~ → ✅ **已闭环（2026-09-23，T-27）**：后端以 `NEXENT_SANDBOX_DEFAULT_LEVEL=local` + `MINIO_ENDPOINT=http://127.0.0.1:9010` 重启后，chat QA 3 轮全部完成（`errors: []`），产出 `deliverables/T-27-chat-qa-{1,2,3}.png`。**每个答案已可回溯**：真库 `decision_card_t` 三行逐题对应，其中截图 2 显示的 UUID 后缀 `…43ac-a205-d837546b9ec0` 与库行 `348c8a4d-9d50-43ac-a205-d837546b9ec0` 逐字符吻合。详见 `verification-reports/t27-chatqa-unlock.md`。
3. ~~**知识库文件说明 + MCP 文件说明独立成文**~~ → ✅ **已闭环（2026-09-23）**：产出 [`docs/kb-file-guide.md`](kb-file-guide.md) 与 [`docs/mcp-file-guide.md`](mcp-file-guide.md) 两册（配套成组）。两册均含「数字 → `file:line` 出处」对账表、已实跑的复现命令、以及**如实列出的已知不一致**（kb 册 6 条 / mcp 册 5 条，含「8 词表 vs 5 注册」的口径划清与 SPEC「不直连 DB」声明与实现的偏差登记）。

**P1（直接影响 25% 档得分）**

4. **维度③算法创新**：跑完 E2 的 A3（multihop）、扩 A1/A2 样本，回填 `e2-ablation-report.json` 并重绘 `t23-eval-report.png`；补「半自动化本体构建效率」实验（τ_a 扫描）；诚实处理 E8 Δ=0。**进展（2026-09-23/24）**：τ_a 已闭环（`probe_p6`）；效率口径已重定义为人工分钟数（`probe_p7`：门因子主效应 **2.31×**、排序因子结构零、质量轴 `insufficient_data`，预登记 sha256 冻结、复跑 IDENTICAL；创造性贡献③的 05 §3.1 替换文本 r1/r2 待用户拍板）；消融 runner 复核就绪（T-22 行），D1 判别性放行 + 图谱填充后 **E8 Δ≈0 的结构性原因已解除**，按 `t22-resume.status.json` 四段命令跑真实全量（等摄取收口避开 429）即可回填矩阵并重绘图。
5. **维度②资产激活**：图谱摄取从 35/120 段（09-23 13:05 psql 直查）补到 120 段（解锁 E8/A3 真数字）；补 N→M→K 漏斗量化。**进展（2026-09-23）**：阻塞已解——此前探针失败实为**本地代理**（`127.0.0.1:7897` 挂起 TLS）而非网关宕机（坑 #67）；绕开代理后网关健康，`batch-A-2024.json`（12 段）+ `batch-B-2020.json`（12 段）后台续跑（驱动 + 进度证据：`deliverables/p15-ingest-progress.json`）。真库快照（13:05 psql 直查）：**段 35/120、实体 298、关系 207、证据 59**。⚠️ 天花板：按预登记门禁 G1/G2/G3 上限 **88/120**（32 段被 G2 英文尾块/G3 长段排除）；已登记新决策（`p15 JSON decisions` 节）：原门禁 eligible 跑完后**仅放宽 G3→8000** 再逼近至 ~114/120，G2 不放宽；**评测分母一律用实际落库段数**。
6. ~~**T-21 对齐口径**~~ → ✅ **已定死并写进文档（2026-09-23）**：选定 **话题级（topic 级）为对外唯一口径**——`recall 9/9`、`precision 下界 64/517≈0.124`（**下界非 precision**）、`n=9` 限制与阈值敏感性（共享 token 门槛 2→3 时 recall 9/9→3/9）**已一并披露**；item 级 `0.00145/0.111` 作为口径演进基线并置，并标注**结构性天花板 `9/691≈1.3%`（单位错配：段落项 ≠ 话题）→ 不可作质量度量**。重算探针 `experiments/probe_p5_alignment_caliber.py`（零 LLM，与产物逐位吻合），决定书 `verification-reports/t21-alignment-caliber.md`；已落地进 `05` §4.1（原「只写目标 P≥0.90/R≥0.85」改为实测+限制+目标声明未达成）、`00` K5 行、`03` T-21 行。
7. **统一 MCP 工具口径**：对外只说 **5 个**（`05` §2.4 已修）。

**P2（体验与沉淀，维度④）**

8. T-12 进化看板动画（可选）、T-05 e2e 截图重拍（**2026-09-23 前置已备**：G6 树渲染 bug 已修〔`treeToGraphData`，坑 #70〕；数据前置=对演示租户真跑一轮 `build_ontology_round` 出 ≥30 待审提案再提交版本，**不许造数**）、截图重拍/重绘 5 张（清单见 `screenshot-review.md` 汇总：T-01-agent-published / T23-home-loggedin / 两张本体工作台 / t23-eval-report）。~~vision agent 逐张审图~~ → ✅ 已完成（16/16，2026-09-23）。

**可放弃/降级**（砍序已冻结，勿重新捡起）：T-10b、T-12、T-14、T-15、T-17。

---

## 六、附加分现状（共 10 分）

| 附加分项 | 现状 | 依据 |
|---|---|---|
| 多行业模板轻量化**迁移适配验证** | ❌ **未启动** | `corpus/registry.csv` 58 份全为糖尿病域（9 条 `doc_type=policy` 实为临床路径，仍属医疗域）；无迁移度量报告；`wave-plan.md` 明列 T-15「除非 1-4 全完」不做 |
| **真实脱敏行业数据集**效果评测 | ❌ **未启动** | 语料为公开渠道指南/说明书（非脱敏行业数据）；评测集为自建 20+8 题。「脱敏」一词仅出现在 API key 脱敏语境 |

---

## 七、交付物清单（`deliverables/`，26 项）

**截图 16 张**（全部唯一 md5，无同图改名）：`T-01-{agent-published,chat-conversation,models-registered}.png`（平台三证）、`T23-home-loggedin.png`、`t23-shot-{ontology-workbench,ontology-queue,skill-template}.png`、`T-23-decision-card.png`、`T-23-version-compare-{2021clock,2024clock-refusal,2025clock,2026}.png`、`t23-eval-report.png`、`T-27-chat-qa-{1,2,3}.png`（2026-09-23 新增，示例问答 ≥3）。

**实验产物**：`algorithm-probes/probe_p{1,2,3,4}_*.json`、`e1-baseline-report.json`、`e2-ablation-report.json`（partial）、`e2-ablation-paired{,-2}.json`（paired2 = 终版，`same_invocation=true`）、`alignment-diff.json`、`alignment-impact.json`、`e1-baseline-run.log`。

**配置与索引**：`agent-config.json`、`evidence-index.md`（素材→评分维度映射，**状态列按需更新**）、`.gitkeep`。

> 更新纪律（继承 `evidence-index.md`）：每新增/重拍一张截图、每落定一个评测数字，同步更新 `evidence-index.md` 状态列与 `cost-ledger.md` 对应行。

---

## 八、会话进度区（并行会话专用）

> **建立 2026-09-24**。本区是并行会话的**唯一进度写入点**：每个任务只写自己那一节，**只追加，不重排、不改他人小节**。
> 背景：本文件此前没有进度区，而四份任务提示词曾指向一个不存在的「晨间更新区」（已于 2026-09-24 修正为本节）。多会话同写同一段必然互相覆盖，故按任务分节。
>
> **并行纪律（三层冲突的对策）**
> 1. **文件层**：本区按任务分节；`cost-ledger.md` / `evidence-index.md` **只追加自己的块**，不重排；`pitfalls.md` 按头部**号段预分配**（Q1 `#75-79` / Q2A `#80-89` / Q2B `#90-104` / Q3 `#105-109` / Q4 `#110-119`）。
> 2. **git 层**：`competition/` 只属**代码仓**（根仓 `.gitignore` 排除 `/nexent/`）。所有会话**不自行 commit**，结束时报「改了哪些文件」，由**单点统一收口提交**。
> 3. **配额层**：LLM/sensenova2 配额是共享资源。**任务Q2A 的 A4 视觉验收在第一波禁用**（不产出交付物，却与 Q2B 摄取抢 429），推到服务栈收口后再做。
>
> **服务栈**：PG(`/home/qianqian/pg-env.sh` → `pg_start`，127.0.0.1:5434/库 nexent) + ES(项目自带 compose) + minio(`127.0.0.1:9010`) 由**任务Q2B 独家持有**；Q1 Phase 1+ 复用其栈，**禁止第二个会话再起一套**。

### 任务Q1 · 严格版升级

- **Phase 0 完成（2026-09-24，q1-phase0 会话）**：仅做无服务依赖部分；未起 PG/ES/minio、未跑真实库、未冻 sha256。
  - **P0-0 终点校准**：用 `probe_p7_efficiency_minutes.json` 原始数字独立复算审计三质疑——审计算术全对，但①为范畴错误、③为稻草人、②只对 v0.1 成立；主终点定为「门因子人审工作量削减（项数 21→10 / 分钟 14.55→6.30=2.31×）」，明确排除「单条分钟 0.693 vs 0.630」组合伪影终点。见 `deliverables/algorithm-probes/pre-registration-v0.3.md` §1。
  - **P0-1**：定位 `experiments/probe_p6_autonomy_tau.py:95-105` 冻结导出 SQL（只取 4 键：op/target/confidence/evidence_spans）；读 `s1-baseline-readout.md` §4 的 P-1（起库）/P-2（补导出键）/P-3（seed_terms）解锁前置。
  - **P0-2**：新建 `experiments/export_evidence_pool_v2.py`（纳入 parent/also_parent/anchor/seed_terms，不改 probe_p6）；明确 `seed_terms` 来源结论= `Quality().cov` 的调用方注入参数（非 DB 列、非 `seed_terms.py` 检索词），持久化未接线（C-3）。
  - **P0-3**：写 `deliverables/algorithm-probes/pre-registration-v0.3.md`（草案，**未冻结** sha256）。
  - **P0-4 可行性闸门**：当前 n=21（ev_rich 过门率 0/21）远不够排序臂 power 目标 98–392；但 58 份语料 + 摄取补到 88/114 段 + `build_ontology_round` 候选池即达 ≥300，可达 98/档；绑定约束是 C-4/C-5（分数连续化、ev_rich 过门率>0）而非语料规模；按"不止损"口径，门因子 2.31× 结构确定、n=21 已可呈现，直接命中官方「效率优势明显」。
- **待 Phase 1+（等 Q2B 服务栈就绪）**：跑 P0-2 真实导出重建 Quality 轴、估 ρ（C-7）、真人计时（≥2 标注者双盲随机序）、冻结 v0.3 sha256。
- **本轮改动文件（交单点提交，不自行 commit）**：`nexent/competition/experiments/export_evidence_pool_v2.py`（新）、`nexent/competition/deliverables/algorithm-probes/pre-registration-v0.3.md`（新）、`nexent/competition/docs/pitfalls.md`（追加 #75–79）、`nexent/competition/docs/00-索引与状态.md`（本小节）。

### 任务Q2A · 交付文档线

> 状态：✅ A1/A2/A3/A5/A6/A7 已完成；A4 本轮禁用（如实标注）。不自行 commit，由单点统一收口。

- **A1（最高优先）**：✅ 新建 `docs/智能体设计说明-初赛提交版.md`（官方第 2 项提交物唯一缺口的装配稿）。六节齐 + §⑦跨域可迁移性；口径红线全程遵守（自研 MCP 只说 5 个、平台 v2.6.0、无 pgvector、「可进化」标 L4 实现、3 张示例问答截图 ↔ `decision_card_t` UUID 互锁已写入）。无括号占位、无 None、每数字可指素材行号。
- **A2**：✅ `docs/reproduce-README.md` pytest 数字 **551 → 721 passed / 30 skipped**（权威源 `archive/overnight-2026-09-24/receipts/T1-pytest-raw.txt`）；§8 验收清单同步。并修正 `docs/dev-design-doc.md` §5.5 的 711→721/30 消歧（避免与 raw 721 冲突）。复现步骤与产物逐字一致，未改动接线文件。
- **A3**：✅ ①`deliverables/参考资料-参考文献表.md`（规范参考文献表，源自 `06-论文与开源武器库-v2.md` §3，含 Nemhauser-Wolsey-Fisher 1978 / Sviridenko 2004 / Gao 1907.09657 / uComp SW-150181 / Snodgrass & Ahn 1986 / Allen 1983 / BH-FDR 2010 / CELF 2007 / SAGE 2609.17775 / GUIDE 2608.12133 等）；②`deliverables/待用户填写清单.md`（封面 8 项 + 团队合照 ~2MB 规格）；③套版稿占位复核：unzip 检查 `【赛题3-模板1】开发设计文档-套版稿-2026-09-23.docx` 仅余 8 处封面【】占位（封面 8 项，预期待用户填），无残留内容占位（"注入"/"占位"均为合法行文）。
- **A4**：⏸ **本轮禁用**（与任务Q2B 摄取抢 sensenova2/subagent provider 配额 429）。如实标注：视觉验收待服务栈收口、provider 恢复后再由后续会话执行。未跳过不记。
- **A5**：✅ `deliverables/screenshot-reshoot-plan.md`（5 张必重拍图 T-01-agent-published / T23-home-loggedin / t23-shot-ontology-workbench / t23-shot-ontology-queue / t23-eval-report 的前置数据/页面路径/预期元素清单）；**不起服务**，交任务Q2B 起栈后衔接。
- **A6**：✅ `deliverables/submission-package-manifest.md`（官方 §三 L44 实体打包清单：5 项 → 官方原文 → 对应文件 → 打包路径 → 提交方式；含官方模板命名规则与"必须由用户执行的动作"置顶单列；原始 PDF 不提交仅元数据）。
- **A7**：✅ 已在 A1 装配稿单列 §⑦「跨域可迁移性」：用代码结构证据（registry.csv 唯一入口、无硬编码域名词、doc_asset_t 域无关字段、迁移脚本化）说明换域机制；**明确标注"多行业实测由任务Q4 补，本稿只作结构性论述"，未把没跑的迁移写成已验证**。
- **台账**：追加 `pitfalls.md` #80（对外数字跨文档互斥 551/711/718/721，以 721 为权威回填）；`deliverables/evidence-index.md` 末尾追加 Q2A 装配交付物块。
- **改了哪些文件（交单点收口，未 commit）**：`docs/智能体设计说明-初赛提交版.md`（新）、`docs/reproduce-README.md`、`docs/dev-design-doc.md`、`deliverables/参考资料-参考文献表.md`（新）、`deliverables/待用户填写清单.md`（新）、`deliverables/submission-package-manifest.md`（新）、`deliverables/screenshot-reshoot-plan.md`（新）、`docs/pitfalls.md`、`deliverables/evidence-index.md`、`docs/00-索引与状态.md`（本节）。

### 任务Q2B · 技术实验线

> 会话 q2b-stack-retry（2026-09-24 重启，前次 78min 中断零产物）。纪律：服务栈唯一入口；不碰接线文件 / knowevo 契约（B1/B5/B6 已拆给 Q5a）；不碰前端与 Q5b 新建文档；只写本小节 + 追加台账。

**第 0 步 · 机器探针（2026-09-24 实测，只读）**
- 内存：`free -g` → total 15G / used 6 / free 1 / **available 9G**（够起一个重服务）。
- dockerd：`docker info` OK；`/var/run/docker.sock` 存在（root:docker）。→ ES 可走项目自带 compose。
- **PG 已在跑**：`ss -ltnp` 显示 `127.0.0.1:5434` LISTEN，pid 42257（`postgres`）。`pg-env.sh` 存在且内容完整（`pg_start`/`pg()` 函数齐备）。→ 无需 `pg_start`，直接 `source /home/qianqian/pg-env.sh && pg ...` 可用。
- 实测连通：PostgreSQL 16.15（conda-forge）；`pg -c "select 1"` OK。
- **当前库内数据量（与文档旧值 35/120、298/207 严重不符，待 B2 复核）**：`kg_entity_t=64608`、`kg_relation_t=100400`、`kg_evidence_t=0`、`kg_pending_entity_t=0`、`doc_asset_t=0`。真实表名是 `kg_entity_t/kg_relation_t`（非文档里写的 `entity_t/relation_t`）；`knowledge_segment_t` 不存在。GiST 区间索引 `ix_kr_valid_range/ix_ke_valid_range` **尚未创建**（现仅有 `ix_kr_valid`）；脚本 `explain_valid_range.py` 自带 `ensure_indexes()` 会建。

**第 1 步 · B4① GiST 索引运行时等价 ✅ 完成**（会话被 429 中断前已落盘）。产物：`deliverables/t4-explain-valid-range-2026-09-24.txt` + `deliverables/algorithm-probes/explain_valid_range{,_forced}.json`。实测要点：
- 索引落地确认（`pg_indexes`）：`ix_kr_valid_range`（`kg_relation_t`）/ `ix_ke_valid_range`（`kg_entity_t`）均已存在（由脚本 `ensure_indexes()` 的 `CREATE INDEX IF NOT EXISTS` 建立）。
- **规划器选中**：`Bitmap Index Scan on ix_kr_valid_range` / `on ix_ke_valid_range`，且 **`Index Cond` 含 `tstzrange(valid_at, COALESCE(invalid_at,'infinity'),'[)') @> now()`**（seqscan=off，区间谓词作为访问路径）。
- 退化行预检查 = **0** → `safe_to_enable_range_predicate = True`。
- **运行时语义等价**：区间谓词与布尔谓词在真库上返回**完全一致的行集**（`kg_relation_t` 118400 = 118400）。
- **如实补充（重要，不加修饰）**：当查询同时带 `tenant_id` 等值（**应用真实用法**）时，组合索引 `ix_kr_hop_rev_cover` 被优先选中，区间谓词**退化为 `Filter` 而非 `Index Cond`**——该路径同样正确且更优，两种语义仍返回相同行集。**结论：GiST 区间索引在"区间谓词独立作为访问路径"时被规划器选用，与组合索引不冲突。** 此证据补齐 T4 receipt 中标注 `insufficient_data` 的那一项。

---

> ### 🔴 单点收口时发现的**阻断级问题**：B2 摄取会写错库（下个会话必须先解决这一条）
>
> 本节"第 0 步探针"记录的库内数据量与文档旧值不符，单点收口时已**直查复核**，结论如下（均为实测，非推测）：
>
> | 检查 | 实测结果 |
> |---|---|
> | `docker ps -a` | **`nexent-postgresql` 容器已停止**（`Exited (0)`，5 小时前）；`nexent-elasticsearch` 亦停止（`Exited (143)`） |
> | 跑着的 PG（`127.0.0.1:5434`，conda-forge 16.15） | `kg_entity_t=70608` / `kg_relation_t=118400`，但 **`doc_asset_t=0` / `kg_evidence_t=0` / `kg_pending_entity_t=0`** |
> | 租户分布（`kg_entity_t`） | 多个租户各**恰好** `20000` / `3000` 条 → **典型合成基准数据特征**（非真实语料抽取产物） |
> | 真实表名 | 实为 `kg_entity_t` / `kg_relation_t`；文档里写的 `entity_t` / `relation_t` **不存在**；`knowledge_segment_t` 不存在 |
> | 索引现状 | GiST `ix_kr_valid_range` / `ix_ke_valid_range` 原**未创建**（原仅有 `ix_kr_valid`），由本步脚本的 `ensure_indexes()` 补建 |
> | 脚本是否会造数据 | `experiments/explain_valid_range.py` **只有** `CREATE INDEX IF NOT EXISTS`（一次 `commit()`），**不插入/不删除**任何行 → 上述数据是**库里本来就有** |
>
> **⇒ 判断**：本机存在**两套 PG**——① 跑着的 `127.0.0.1:5434`（conda-forge，装的是**合成基准数据**）；② 已停止的 docker 容器 `nexent-postgresql`（历史核查一直走它，见坑 #69 / #13，**真实图数据应在其中**，但**未启动、未验证**）。文档记载的「真库 35/120 段、实体 298、关系 207、证据 59」**与 5434 库对不上**，与"两套库"假设吻合。
>
> **⚠️ 因此：B2 摄取与 B3 E2 在解决本条之前一律不得开跑**——否则会把真实摄取结果写进一个装基准数据的库里，并污染后续所有"真数字"。
>
> **下个会话（任务Q2B 续跑）的第一步**：① 启动 `nexent-postgresql` 容器并直查其 `doc_asset_t` / `kg_entity_t` / `kg_evidence_t` 计数，确认真实数据是否完好；② 查清本项目**到底该用哪套 PG**（文档口径是 5434，坑 #69 口径是容器，两者矛盾必须由用户或维护者裁定）；③ 把结论补进 `AGENTS.md` 与 `pitfalls`，避免后续会话再踩。**在裁定之前不要起栈、不要摄取。**
>
> **B2 起栈与摄取 / B3 E2 全量 A3 / B7 截图重拍 / B8 收口交接：均 ❌ 未做**（会话被 429 限流中断于 16:16 前）。服务栈状态**无交接记录**。

### 任务Q3 · key 与环境规划

- **产出 1（Q3-5 官方核验）**：`docs/verification-reports/official-site-2026-09-24-external-crosscheck.md` —— 外部多源（华为微博 `compId=85132021` + `enrollmentDeadline=2026-11-30 23:59:59`、4 所高校通知）互证 **2026-11-30 = 报名截止（非材料提交截止）**；初赛在其后（2026-12，待用户在报名系统确认具体日）。三项仅内部口径（2 份 Word / 四维各 25% / 模板文件名）对外一律加"以官网原文为准"。
- **产出 2（Q3-1/2/3 配置计划）**：`docs/verification-reports/provider-key-plan-2026-09-24.md` —— 摸清两条轨：轨A 平台侧 `model_record_t`（key 存 DB，env 仅选 model_id，`const.py:807-809`）+ 轨B 子智能体 provider（ZCode 设置，09-23 真卡点 `Invalid API Key`）；含每步动作+验证命令+429 行为+环境就绪总清单（provider key/PG/ES/minio/subagent 各写"谁做/卡谁/验证命令"）。
- **踩坑台账**：`pitfalls.md` 号段 `#105–109` 追加 #105（报名截止误读）、#106（全球总决赛口径冲突）。
- **纪律**：本会话不起任何服务、不碰真实 key、不自行 commit；服务栈由 Q2B 独家持有。

### 任务Q4 · 加分与体验补强线

（待填）

> **2026-09-24 拆分说明**：C1（多行业迁移实测）需服务栈，排到任务Q2B 收口之后执行；**C2（交互体验）/ C3（Skill 沉淀方案）/ C4（脱敏数据集方案）零服务依赖**，已并入任务Q5b 并行执行。

### 任务Q5a · 无服务收尾线 · 契约与金标（从任务Q2B 拆出）

> **2026-09-24 落盘状态（由单点收口时按实测登记；本会话两轮均因外部故障中断——先网络 `EAI_AGAIN`、后模型 429 限流至 16:16）**

- **B1 ✅ 完成**：双份契约已立且质量合格——工作区根 `knowevo/backend/services/knowevo/alignment_semantic.py.md`（122 行 / 9445 B）与仓内 `nexent/knowevo/backend/services/knowevo/alignment_semantic.py.md`（122 行 / 9451 B）。含职责、五步法、接口冻结（零新增依赖：stdlib only）、`probe_p8` 实测口径（recall 9/9、`precision_identifiable=false`）。
  - 两份唯一差异为**相对链接层级**（根副本 `../../../../` vs 仓内副本 `../../../../../`）——已对照先例 `graph_store.py.md` 确认**先例也只差这一行**，故为正确做法。
  - **待办（措辞）**：契约内"内容逐字一致"应改为"内容一致；相对链接按层级调整（根 4 级 / 仓内 5 级），与 `graph_store.py.md` 先例一致"。
- **B5 ◐ 部分完成（已如实标注）**：`corpus/guideline_diff_seed_batch2.md` 更新——新增 **B-18 阿司匹林一级预防年龄分层**（verified，含 2024 L2650/L2680-2694/L2652 与 2020 L2703/L2708 行号级对照）；**B-17 糖尿病视网膜病变分级由 `unverified` 升 `verified`**（对两版分级表做了逐句 diff，发现 2024 新增「美国标准」亚变体）。话题总数 **45→46**（verified 37→**39**）。**距 ≥50 目标仍缺 4 个话题，未填满**（原缺口类别：低血糖药物 / 心衰话题 `insufficient_data` 不硬凑 / 妊娠老年细分需改种子行 / 等）。
- **B6 ❌ 未做**：`competition/docs/` 过时工单盘点本轮无产出。

> **2026-09-24 新增**（用户要求"多个独立的任务并行"）。把原任务Q2B 中**不需要服务栈**的三项拆出来独立成线：**B1**（补 `alignment_semantic.py` 双份契约 `.py.md`）、**B5**（T5 金标补 5 条 45→50）、**B6**（`competition/docs/` 过时工单盘点，只盘点不动手归档）。这三项只碰契约树 / `corpus/` / `docs/`，与任务Q2B（服务栈）、任务Q5b（前端与打分）在文件上不相交。

### 任务Q5b · 无服务收尾线 · 打分补强（原任务Q4 的无服务三项）

> **2026-09-24 落盘状态（由单点收口时按实测登记；本会话因模型 429 限流中断于 16:16 前）**

- **C2 ◐ 基本完成**：
  - 前端两处已改，且**单点补跑了类型检查：`npm run type-check`（= `tsc --noEmit`）通过，零输出**：
    - `nexent/frontend/features/decisionCard/DecisionCardPanel.tsx`（+30）：新增 `renderError` **持久**生成失败态，与空态、与 `INSUFFICIENT_EVIDENCE` **业务态**三者分开，带重试入口；注释引坑 #71 判据。
    - `nexent/frontend/features/skillTemplate/SkillTemplatePanel.tsx`（+50/−8）：把 **error / empty / pending-wiring 三态拆开**，`load` 抽为 `useCallback` 以便重试；注释引坑 #71。
  - `docs/interaction-flow.md`（103 行）✅ 已产出（交互流程图：提问 → 决策卡 → 证据溯源 → 技能模板沉淀）。
- **C3 ✅ 完成**：`docs/skill-template-playbook.md`（89 行）—— Skill 工作流模板沉淀方案（方法论 + `competition/skills/` 4 组模板的复用边界），对应官方维度④原句。
- **C4 ❌ 未产出**：`docs/desensitized-dataset-eval-plan.md` 未建（附加分② 前期可行性）。**留待下一轮。**

> **2026-09-24 新增**。任务Q4 的 **C2**（前端 4 个 feature 的空/错/载态走查 + `docs/interaction-flow.md` 交互流程图）、**C3**（`docs/skill-template-playbook.md` Skill 工作流模板沉淀方案）、**C4**（`docs/desensitized-dataset-eval-plan.md` 脱敏数据集评测方案，只做可行性）。三项零服务依赖，直接服务官方维度④与附加分②。**红线**：只动 `frontend/features/` 下自有文件，不碰路由/菜单注册（接线文件）。
