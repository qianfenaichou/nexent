# 踩坑台账（pitfalls）
> 纪律（01 总计划 §5）：从第一个坑就开始记。格式：现象→定位→根因→修复→沉淀机制。每个坑是 PPT 开场的伏笔，机制是回收。
>
> ⚠️ **号段预分配（2026-09-24 起，并行会话专用）**——本文件是**多会话共写**台账，并发追加会重号/覆盖（现象见坑 #74 同族的"默认环境与直觉不一致"家族）。规则：
> - **任务Q1** = `#75–79` ｜ **任务Q2A** = `#80–89` ｜ **任务Q2B** = `#90–104` ｜ **任务Q3** = `#105–109` ｜ **任务Q4** = `#110–119`
> - 每个会话**只在自己号段内新起编号**；只追加行，不重排、不改他人行。
> - 号段用尽时，先在本节登记新号段再使用；**禁止**占用他人号段或号段外编号。
> - 收口纪律：会话**不自行 commit**，由单点统一提交（见 `00-索引与状态.md` §八）。

| # | 日期 | 类别(部署/解析/抽取/图谱/评测) | 现象 | 根因 | 修复 | 沉淀机制 |
|---|------|------|------|------|------|------|
| 1 | 2026-09-13 | 部署 | ES 启动失败（本机未复现，开机值已满足） | vm.max_map_count 需 ≥262144 | sysctl -w vm.max_map_count=262144（本机实测 1048576，免调） | 部署 README 检查清单 |
| 2 | 2026-09-13 | 部署 | 现象：自动化环境跑 deploy.sh 拉镜像走 docker.io 源，国内网络拉取缓慢/超时 | 定位：读 deploy/common/common.sh L8 发现 `DEPLOYMENT_IMAGE_SOURCE_DEFAULT="general"`；非 TTY 环境 TUI 步骤（common.sh L1184 multiselect 等）静默跳过全走默认 | 根因：镜像源默认值为 general，交互式选单在管道/后台环境不可达 | 修复：显式传 `--image-source mainland`（deploy.sh 参数入口 docker/deploy.sh L69）；本次实测 mainland 源（elastic.m.daocloud.io / quay.m.daocloud.io / ccr.ccs.tencentyun.com）全部拉取成功 | 沉淀机制：自动化部署一律显式传 `--image-source`；CI/脚本封装时禁止依赖交互默认值 |
| 3 | 2026-09-13 | 部署 | 现象：`psql -d nexent -c "\dt"` 显示 0 张表，误判迁移失败/suadmin 未建 | 定位：`select schemaname from pg_tables` 发现全部表在 `nexent` schema 下（76 张） | 根因：Nexent 迁移 SQL 显式建 `nexent` schema，psql 默认 search_path 不含它；另 MCP 日志中 `pg_type_typname_nsp_index duplicate key` 为迁移幂等检查的良性输出 | 修复：验证一律加 schema 前缀（`nexent.user_tenant_t`）；判定失败前先看 pg_tables | 沉淀机制：DB 验证命令模板写入任务简报验收命令节 |
| 4 | 2026-09-13 | 部署 | 现象：登录接口试 `/api/v1/auth/login` 404，找不到正确路径 | 定位：grep backend/apps/user_management_app.py L106 `@router.post("/signin")`，前缀 `/user` + app_factory root_path `/api` | 根因：Nexent 登录路由是 `/api/user/signin`，非 Supabase 原生 `/auth/v1/token` | 修复：`POST http://localhost:3000/api/user/signin` 返回 200 + SU 角色 | 沉淀机制：API 探活前先 grep 源码定位路由，不猜 RESTful 惯例 |
| 5 | 2026-09-13 | 部署 | 现象：suadmin 登录后访问 /models 被弹"无权限访问"并重定向回首页 | 定位：查 nexent.role_permission_t，SU 的 LEFT_NAV_MENU 权限只有 /、/owner-manage、/resource-manage、/space 四条，无 /models | 根因：Nexent 是多租户架构，SU 是平台超管（管租户/配额），模型配置是租户级页面（ADMIN/SPEED/DEV/USER 才有 /models）；模型注册前必须先建租户+租户管理员 | 修复：suadmin 建租户 knowevo → 生成 ADMIN_INVITE 邀请码 → 注册 admin@knowevo.com（ADMIN）→ 用它登录配模型 | 沉淀机制：多租户平台的"超管≠业务管理员"，初始化流程=建租户→建管理员→再配业务资源；写进部署 README |
| 6 | 2026-09-13 | 部署 | 现象：创建租户时勾选"立即生成租户管理员账户"填 admin@knowevo.local，提示"租户已创建"但"创建租户管理员账户失败" | 定位：config 日志 `POST /api/user/signup 422`；容器内 email_validator 验证 `admin@knowevo.local` → "special-use or reserved name"（.local 是 RFC6761 保留域） | 根因：pydantic EmailStr 内嵌 email_validator，保留域名（.local/.test 等）在请求体校验层直接 422，但前端把 422 显示为通用失败，租户却已创建，状态半成品 | 修复：邀请码 HCIAQP 仍有效，改用 admin@knowevo.com 调 signup API 完成 ADMIN 注册（200）| 沉淀机制：测试环境邮箱一律用 .com/.net 等真实 TLD，不用 .local/.test；前端 422 错误提示不具体时直接看后端日志 |
| 7 | 2026-09-14 | 图谱(建表) | knowevo_db.py import 即抛 `InvalidRequestError: Attribute name 'metadata' is reserved` | doc_asset_t 的 metadata 列照抄 DDL 作 ORM 属性名，撞 SQLAlchemy DeclarativeBase 保留属性 | ORM 属性改名 `meta_data`，用 `Column("metadata", JSONB)` 保住 DDL 列名（上游 db_models 全库同样用 meta_data 规避） | 新 ORM 建模规则写入 knowevo_db.py 模块注释；后续 T-06/T-09 建 Pydantic 视图时同样先查保留名 |
| 8 | 2026-09-14 | 图谱(建表) | 会话层一提交就 AttributeError('commit') | 上游 `get_db_session` 是 @contextmanager 返回 ctx 对象，误当 session 直用；照 a2a 模式 `with _get_db_session() as session:` 才对 | 删自造 knowevo_session()，三个 CRUD helper 全改 `with _get_db_session() as session:` | 接上游任何基础设施前先读其函数装饰器/签名（备忘录 11 的核验方法在写码时同样适用） |
| 9 | 2026-09-14 | 图谱(建表) | bi-temporal 现行视图查出"未来行"（valid_at > now） | valid_now() 无参分支只查 invalid_at IS NULL，漏了 valid_at <= now 上界；单测靠 mock 编译产物自证通过，真实 PG 一跑就露馅 | 改为统一 `valid_at <= as_of AND (invalid_at IS NULL OR invalid_at > as_of)`，无参时 as_of=func.now()（DB 侧时钟） | 时间谓词必须上真实库集成验证（本任务 RUN_POSTGRES_INTEGRATION 门控模式固化为上游对齐做法）；mock 只验形状不验语义 |
| 10 | 2026-09-14 | 图谱(建表) | psycopg2 连不上：`could not translate host name "4321@localhost"` | 密码 `nexent@4321` 含 `@`，手拼 DSN URL 被解析断 | 用 `sqlalchemy.engine.URL.create` 构造连接串 | 凭据类测试一律 URL.create；密码带保留字符是部署常见雷 |
| 11 | 2026-09-14 | 测试 | 上游 database 测试 15 文件 error（ModuleNotFoundError: smolagents）+ 2 文件 failed（FileNotFoundError: deploy/sql/init.sql） | ① smolagents 在 sdk/pyproject.toml，backend venv 不含 SDK 依赖，且 `cd sdk && uv sync` 会踩 [tool.uv] override-dependencies 的 `psycopg2-binary==999.0.0` 哨兵陷阱解析失败；② 两个 failed 文件用相对路径 `Path("deploy/sql/init.sql")`，必须从仓库根跑 | ① 按上游 CI（.github/workflows/auto-unit-test.yml L76-77）：backend 下 `uv sync --extra data-process --extra test` + `uv pip install -e "../sdk[dev]"`；② 测试从仓库根目录起 | 上游测试本地复现 SOP = 照抄 CI 命令，不自造 uv sync 组合；逐文件跑时先定 cwd 再跑（run_all_test.py 控制工作目录正是为此） |
| 12 | 2026-09-14 | 测试 | test/backend/services/ 下新建 knowevo 测试目录后 import 自己的服务模块报 ModuleNotFoundError: services.knowevo.ontology_service | test 树 backend/services/ 有 __init__.py，是真实包；新加 test/backend/services/knowevo/__init__.py 后包名 `services.knowevo` 先在 test 树命中，遮蔽 backend/services/knowevo 实现包 | 测试目录不加 __init__.py（pytest rootdir 导入模式），sys.path 插 backend 绝对路径（上游 test_agent_repository_service.py 同款约定 parents[3]） | test 树下新增子目录测试时禁加 __init__.py；import 实现一律 sys.path 显式指向 backend |
| 13 | 2026-09-14 | 测试 | PG 集成测试首跑 17 errors（FATAL: database "knowevo_test" does not exist） | T-03 集成测试设计连独立 knowevo_test 库（隔离建删表循环），环境里该库不存在（上个会话建的库未持久或换栈） | `docker exec nexent-postgresql psql -U root -d nexent -c "create database knowevo_test;"` 后全绿 | 集成测试 README 化前置条件：先建 knowevo_test 库（写进 T-04 简报验收命令） |
| 14 | 2026-09-14 | 测试 | 新会话复跑 T-04 PG 集成测试：`RUN_POSTGRES_INTEGRATION=1` 直接跑 19 条全过但 test_full_round_lands_in_pg 报 psycopg2 FATAL 密码错误（连的 localhost:5432 dev 库） | knowevo_db._get_db_session 走 database.client → 读环境变量 POSTGRES_HOST/PORT/USER/NEXENT_POSTGRES_PASSWORD；部署栈 PG 发布在宿主 5434，凭据在 deploy/env/.env（容器内 host=nexent-postgresql:5432） | 复跑命令：从 deploy/env/.env export NEXENT_POSTGRES_PASSWORD，再 POSTGRES_HOST=localhost POSTGRES_PORT=5434 POSTGRES_USER=root POSTGRES_DB=nexent RUN_POSTGRES_INTEGRATION=1 → 19 passed | 本机复跑集成测试的环境变量模板写进简报 Evidence 节；宿主端口映射 5434≠容器 5432 勿混淆 |
| 15 | 2026-09-14 | 解析(摄取) | 现象：POST /api/file/upload 成功（destination=local）但 process 报 FileNotFoundError /opt/uploads/xxx | 定位：local 目标把文件写进 config 容器 /opt/uploads，data-process 是独立容器无共享卷，文件不可见 | 根因：local 模式只服务单容器部署；跨容器摄取必须走 minio（对象存储中转） | 修复：destination=minio 全链通过（upload→process→ES 1497 chunks） | 沉淀机制：多容器部署下文件摄取一律 minio；API 探活后仍要看 data-process 日志确认文件真正可达 |
| 16 | 2026-09-14 | 解析(摄取) | 现象：hybrid 检索 500 "404 Client Error for url: https://api.siliconflow.cn/v1"——URL 是根路径而非 /v1/embeddings | 定位：读 SDK embedding_adapter.py L120 `session.post(self._base_url)`——SDK 直接 POST base_url，不拼 /embeddings 后缀；默认值（L188 jina）就是完整端点 | 根因：OpenAI 兼容平台注册时 base_url 需填完整 embeddings 端点，与 chat 模型的 /v1 前缀习惯相反 | 修复：模型配置更新 base_url=https://api.siliconflow.cn/v1/embeddings 后索引即通 | 沉淀机制：注册 embedding 模型先读 SDK 源码确认 URL 拼接约定（Nexent 是"完整端点直连"派）；配完跑 healthcheck + 单文件全链验证再批量 |
| 17 | 2026-09-14 | 部署 | 现象：摄取大 PDF（95/124页）时 Ray 报 OutOfMemoryError "Memory on the node 14.59GB/15.35GB (95%)"，worker 被 kill，任务 FAILURE 63 个 | 定位：宿主 15GB 内存跑 17 容器（data-process Ray 池常驻 3.5GB+ES 1.5GB+dev 系列 1.2GB），大 PDF 解析峰值击穿 95% 阈值 | 根因：RAY memory_usage_threshold 是节点级（含宿主其他进程），容器又无 memory cap，Ray 的 kill 策略实际失效于宿主粒度 | 修复：重建 data-process 容器加 --memory=6g --shm-size=512m，DP_PART_PROCESSOR_COUNT=2→1 降并行，大 PDF 串行摄取（PACE=240s）后零 OOM | 沉淀机制：15GB 级宿主跑 Nexent full 栈，摄取吞吐上限≈单文件串行；资源不足时先限容器内存再降 Ray 并行度，别让 Ray 看到整节点内存 |
| 18 | 2026-09-14 | 部署 | 现象：docker compose up -d 重建单服务失败 "network nexent_network has incorrect label (expected nexent)" | 定位：deploy.sh 建栈时网络 label 与 compose project 名不一致，compose 后续管理操作全被网络校验挡住 | 根因：deploy.sh 的网络创建路径与 docker/compose/docker-compose.yml 的 project 命名规则不同源 | 修复：容器级变更走 docker inspect 导出配置→docker run 重建（保留 binds/env/entrypoint），绕开 compose 网络校验 | 沉淀机制：deploy.sh 栈做单容器热改用 inspect+run 通道；端口映射 PortBindings 记得显式写宿主端口（空 HostPort 会随机分配，32864 断了 web 网关 3000→5010 转发） |
| 19 | 2026-09-14 | 部署 | 现象：容器内突发 NameResolutionError "Failed to resolve api.siliconflow.cn"，全栈 embedding/检索 500 | 定位：宿主 systemd-resolved 运行但上游 DNS SERVFAIL；Docker 内嵌 DNS（127.0.0.11）透传宿主故障；8.8.8.8 直查正常 | 根因：本机 DNS 链路抖动（systemd-resolved 上游无响应），非容器问题 | 修复：sudo systemctl restart systemd-resolved 恢复；期间重建的容器注入 --dns 223.5.5.5 --dns 8.8.8.8 兜底 | 沉淀机制：外网依赖型故障先分层排查（容器内 resolv.conf → 宿主 resolv.conf → 公网 DNS 直查）；长跑容器建议显式 --dns 公网地址免受宿主 DNS 抖动牵连 |
| 20 | 2026-09-14 | 评测(E0) | 现象：E0 小档首轮 17/20 题 429 Too Many Requests；部分题 0.5-1.8s 极速返回但答案为空（tokens≈问题本身 44-68） | 定位：tokenrouter 免费档并发限流严格（无退避时连续调用全 429）；空响应是 glm-5.3-free 免费档特性——返回 200 但 content 空 | 根因：免费档 QPS/并发配额 + 平台侧空响应率 ~40% | 修复：runner 加指数退避（15s×attempt，上限 90s）+ 题间 sleep 3s + 空响应标记 EMPTY（判 MISS 不判 ERROR）；三轮数据诚实记录在 e0-baseline.md | 沉淀机制：免费 API 做基准测试必须带 429 退避与空响应统计，否则数据全是噪声；E0 结论"分档差异是噪声"正源于此 |
| 21 | 2026-09-14 | 语料(合规) | 现象：任务线索中"WS/T 786 血糖仪标准"查无此文；2024 版指南标题与预期不符 | 定位：核查代理全量 URL 直连验证——WS/T 781-2021 才是《便携式血糖仪临床操作和质量管理指南》；2024 版官方标题为《中国糖尿病防治指南（2024版）》不含"2型"二字 | 根因：二手资料（任务书/备忘录）转引的标准号与标题有误 | 修复：registry 按正确标准号/标题登记；血缘配对规则去掉"2型"字样做家族匹配（guide-2020→guide-2024 才配上） | 沉淀机制：合规登记前每个标准号/标题都以一手源（标准原文/期刊页眉）复核，二手线索只当检索起点；标题差异导致血缘漏配的教训写进 build_supersede_link 的家族归一化 |
| 22 | 2026-09-16 | 图谱(抽取) | 现象：T-06 验收测试 12 failed / 48 passed；覆盖对齐/标定/合并/抽取四块核心 | 定位：逐个读失败断言，5 处独立实现 bug：①`extract()` 的 `save_evidence` 漏 `await`（证据从未落库）；②`_merge_edge` 权威比较方向反（authority 数值小=权威高，代码用 `>`）；③`merge_delta` 未处理 `ext.pending`（待审实体不计数不进池）；④`_insert_new_entity` 丢 ext_id/ext_scheme 且漏 `status="active"`（二次合并 L0 找不到锚点）；⑤`align()` L0 别名阻断过强（有 embedding 的剂型变体被归一化匹配截胡，绕过 L1/L2 向量通道） | 修复：①补 `await`；②比较改 `<`（lower number = higher authority）；③merge 开头统一处理 pending；④persist ext_ids + status=active；⑤L0 别名/剂型归一化仅在实体无 embedding 时执行；另 τ1 语义修正为"fpr≤2% 安全区内 recall 最大"、AUC 换 Mann-Whitney U（梯形积分在离散点只得 0.51） | 沉淀机制：async 存储方法一律补 await（无 lint 告警，靠测试兜）；权威等级语义（1=最高）写进 docstring；有 embedding 的实体应走相似度通道而非词面归一化——词面匹配只服务无 embedding 兜底 |
| 23 | 2026-09-16 | 图谱(抽取) | 现象：contended 测试第二次 merge 走 supersede 分支（期望 contested），旧边 authority 查不到 | 定位：打印 store 调用——第二次 merge `get_evidence` 只命中新边；旧边 evidence_id 藏在 `props` 里，`_merge_edge` 却从 `existing["evidence_id"]` 顶层读（None → old_authority 落默认 3 → 2<3 误判"新权威更高"） | 根因：PgStore 的 `_relation_row_to_dict` 把 evidence_id 从 props 提出来放顶层，FakeStore 却存嵌套在 props；两个 store 契约不一致，`_merge_edge` 只适配了 PgStore | 修复：`_merge_edge` 兼容读 `(existing.get("props") or {}).get("evidence_id") or existing.get("evidence_id")` | 沉淀机制：多 store（Pg/Fake）并存时，跨 store 的字段读取必须做双路兼容；FakeStore 与 PgStore 的行为差异是测试漏网的温床，在 store docstring 标注契约 |
| 24 | 2026-09-16 | 图谱(合并) | 现象：code-review 双代理审查 T-06 时发现两个"测试全绿但实现不完整"的缺陷：①ALIAS 合并只挂别名不合并补充 props（`_merge_entity` docstring 声称合并但代码没做）；②CONTRA 冲突只比权威不比时效（简报要求"按时效+权威度 supersede"，两本同年份同权威/低权威新文档会误判） | 定位：Spec 轴逐条对账简报验收标准——"新增/属性追加"条（ALIAS）与"时效晚者现行"条（CONTRA）；Standards 轴核对 §4.2 出处规范发现 graphiti bi-temporal 未注明 | 根因：测试只断言了"合并发生"，没覆盖合并的具体数据语义；SUT 完整但测试采样稀疏 | 修复：`_merge_entity` 调 `merge_entity_props`（新键并入、幸存者值不覆盖）；`_merge_edge` 改用 `(published_at, -authority)` 二元组比较（时效优先、同位比权威），新增 `doc_published_at` 取数；补 graphiti 出处注释；新增 2 条测试锁定新语义（62 passed） | 沉淀机制：审查抓"测试未见的行为"比抓"测试失败"更有价值；关键合并/冲突规则必须有一测锁定具体数据语义（幸存者值 vs 新值、时效 vs 权威优先级），不能只断言计数 |
| 25 | 2026-09-17 | 图谱(对齐) | 现象：T-06 PG 集成测试 test_extract_merge_round_trip 首次在真实 PG 跑（docker 起 supabase-db-mini 5436 + knowevo_test 库）即失败：expect added=1 实得 pending=1 | 定位：debug 脚本打印——extract 正常出实体("二甲双胍", Drug)，但 align 返回 action=pending_review（L3）而非 new | 根因：FakeLLM 默认 adjudication={action:new, confidence:0.0}；`align()` 的 L2 分支用 `confidence>=0.9` 作唯一执行判据，把 LLM 明确说 "new"（两实体不同）也当成低置信怀疑落 pending_review——语义 bug，Layer 1 单测（无 LLM/无 embedding→new 路径）恰好绕过，真实链路才暴露 | 修复：`align()` L2 分支先判 `decision.action=="new"` 直接返回（new 是明确裁决不是怀疑），只有 merge 才看置信度门槛 | 沉淀机制：PG 集成测试不能只当"门控跳过"的装饰——必须真实跑；T-06 三个集成测试从编写起从未在真库执行，首次执行即抓到 Layer 1 测不到的真实 bug。每次 T 合并前至少跑一次 RUN_POSTGRES_INTEGRATION=1 |
| 26 | 2026-09-17 | 图谱(存储) | 现象：T-07a graph_store PG 集成测试 5 failed：sqlalchemy.orm.exc.DetachedInstanceError "Instance <KgEntity> is not bound to a Session" | 定位：`neighbors`/`entity_lookup` 把 ORM row 对象带出 `with _get_db_session()` 块外，再访问 row.props/row.name 触发惰性 refresh | 根因：`_get_db_session` 是 commit-on-exit 的 contextmanager，退出时 session 关闭并 expire 所有 ORM 对象；块外属性访问必然 DetachedInstanceError（T-03 建的 helper 是自包含 CRUD 没暴露此问题，GraphStore 是第一个把查询结果带出块外的调用方） | 修复：所有查询在 with 块内完成 ORM→dataclass card 转换，块外只传纯值对象 | 沉淀机制：`_get_db_session` 的语义是"块内用、块内消费"——查询结果要带出去必须先转成非 ORM 值对象（dataclass/dict）；这条写进 graph_store.py 模块 docstring 供后续任务（T-07b MCP、T-09 决策链）遵守 |
| 27 | 2026-09-17 | MCP(工具) | 现象：T-07b 简报 "结构化错误 {error_code,hint}" 与 "双注册无漂移" 两项已勾 [x]，但 code-review 双轴一致指出代码里根本没有 error_code/hint（SPEC discipline 4 未实现）；另 FastMCP 工具签名与 Pydantic 模型双份字段定义（漂移风险） | 定位：Spec 轴 grep error_code/hint 零命中；Standards 轴指出 server.py 装饰器签名重复了 schemas 字段 | 根因：勾验收项时按"设计已写进 docstring/SPEC"自信判定，未做"代码里能否 grep 到行为"的核对；FastMCP 装饰器习惯写裸参数签名，与 pydantic 模型形成两个字段源 | 修复：补 ToolError 模型 + handler try/except 边界返回结构化错误；FastMCP 工具签名改为直接吃 pydantic 模型（单一字段源，bound 随之进入 advertised schema）；新增 2 覆盖测试（13 passed） | 沉淀机制：勾验收标准前必须能在代码里指到实现该行为的行（docstring 不算）；FastMCP 工具一律用 Pydantic 模型做参数类型（裸签名会与 schema 漂移）；引入 code-review 作为合并前硬门禁的价值再验证——第二次抓出"假完成" |
| 28 | 2026-09-17 | 测试 | 现象：T-10a 测试文件里 `testset_hash` 函数（eval_v1.py 的内部工具函数，非测试）被 pytest 收集成测试项，且因参数名 `data` 撞 fixture 注入报 "fixture 'data' not found" | 定位：pytest 默认 `python_functions = test*`，模块里任何以 test 开头的函数都会被收集；`testset_hash` 恰好满足 | 根因：给 CLI/工具模块起的公共函数名以 test 开头，无意中落入 pytest 收集规则 | 修复：改名 `_testset_hash`（下划线前缀规避收集），CLI 与测试同步改引用 | 沉淀机制：非测试模块（pipeline/services）里以 test 开头的函数一律加下划线前缀；写工具函数前先想"它会被 pytest 收集吗" |
| 29 | 2026-09-17 | 测试(接线) | 现象：T-08 新增 llm_client 测试 5 个全挂（AttributeError: module has no attribute 'KW_LLM_SMALL_MODEL_ID'），而 llm_client.py 无任何 git diff——代码没变但测试从 5 passed 变 5 failed | 定位：中间跑过一次 `ruff check --fix`，它把 `from consts.const import KW_LLM_SMALL/MID/LARGE_MODEL_ID` 三个"未直接引用"的 import 删掉了；而代码用 `_tier_model_id()` 经 `globals()["KW_LLM_SMALL_MODEL_ID"]` 活读取，import 一删 globals() 就取不到，monkeypatch 也无处可 patch | 根因：ruff 的 F401（unused import）只识别直接引用，不识别 `globals()["名字"]` 这种字符串键引用；`--fix` 自动"清理"了语义上必需的 import | 修复：恢复三个 import 并显式 `# noqa: F401 - read live via _tier_model_id()`；模块内活读取 env 驱动的常量时，import 必须保留 + noqa 注释说明为什么"看似未用" | 沉淀机制：①跑 ruff --fix 前先看它会改哪些文件（git diff 预览），自动修复可能删掉语义必要的"看似未用"代码；②用 globals()/getattr 按名字读取模块常量时，import 要加 noqa + 说明；③"测试挂了但代码没 diff"→ 先怀疑工具链副作用（--fix/格式化），再怀疑测试本身 |
| 30 | 2026-09-17 | 测试(接线) | 现象：T-08 router 挂载冒烟脚本 `'knowevo' in r.path` 输出 `[]`，误判 include_router 没生效；Local MCP 冒烟 `_tool_manager._tools` 只有 test_tool_name，误判 kg 工具没挂上 | 定位：FastAPI 新版把 include_router 的路由包成 `_IncludedRouter` 惰性包装（真正路由在 `.original_router.routes`），`app.routes` 里不再是平铺的 APIRoute，`r.path` 对包装对象返回空；FastMCP 的 mount 是命名空间挂载，子 server 工具不进顶层 `_tool_manager._tools`，而是 `_mounted_servers[].server._tool_manager._tools` | 根因：用"平铺路由表/顶层工具表"的旧假设验证新框架的挂载语义；冒烟脚本的断言方式与框架实际结构不符，产生假阴性 | 修复：config_app/runtime_app 各自遍历 `_IncludedRouter.original_router.routes` 找到 5 个 `/api/knowevo/ontology/*`；Local MCP 检查 `MountedServer(prefix='knowevo').server._tool_manager._tools` 得 kg_search/kg_stats | 沉淀机制：写接线验证脚本前先查框架的挂载数据结构（FastAPI _IncludedRouter / FastMCP MountedServer），"grep 不到就断言没挂"在包装型框架里是假阴性温床；验收冒烟必须用框架实际暴露的结构断言 |
| 31 | 2026-09-17 | 接线 | 现象：T-05 router 挂载后（T-08 接线），`GET /api/knowevo/ontology/proposals` 404——但 TestClient 冒烟证明 `_IncludedRouter.original_router.routes` 里 5 个路径齐全、`include_router` 也确实执行（instrument 可见） | 定位：对比既有 router 后发现关键差异——上游 `APIRouter(prefix="/memory")` 用裸前缀（`create_app(root_path="/api")` 全局补 `/api`），而 knowledge_graph_app.py 写死 `prefix="/api/knowevo"`；实测 `/api/api/knowevo/ontology/proposals` 返回 403（路由命中）证明路径被双前缀 | 根因：T-05 建 router 时照抄了"最终 URL 长这样"的习惯（把 root_path 的 /api 也写进 prefix），没有核对 `create_app(root_path="/api")` 的全局前缀机制；`/api/api/...` 荒谬路径下真实请求 404，单测直接调 app 函数（不走路由）所以全绿 | 修复：`prefix="/api/knowevo"` → `prefix="/knowevo"`；新增 test_t08_wiring.py HTTP 护栏（断言 5 端点非 404）锁定，防回归 | 沉淀机制：①新建 APIRouter 前先看同库其他 router 的 prefix 惯例（上游全是裸前缀 + root_path 全局补 /api，只有 /api 入口）；②路由挂载类接线必须有 HTTP 层护栏测试（非 404 断言）——直接调 app 函数的单测永远测不到 URL 层双前缀；③bug 修复若直接在 develop 上提交（未走分支），要在简报备注流程偏差 |
| 32 | 2026-09-17 | 图谱(存储) | 现象：T-09 版本钉住集成测试 4 连挂，报 `KeyError: 'Drug:c'`；手工复现发现 `neighbors(tenant, ['a','b'], hop=1)` 对一条两端都在种子集里的边 `a->b` 返回空集（只给一个种子 `a` 时却正常返回） | 定位：二分——单种子正常、双种子空，说明与数据无关而与 frontier 语义有关；读 `PgJsonbGraphStore.neighbors` L295 判据是 `if card.src not in seen or card.dst not in seen`（"任一端点未见过"才算新边），而 `seen` 初始化即包含全部种子，于是两端都是种子的边永远不满足该条件 | 根因：把"边的遍历新颖性"错判成"端点的遍历新颖性"。两者在图遍历里是不同集合：一个种子集 `{a,b}` 本来就要取 a-b 这条边（它incident to frontier），但它的两个端点都在 seen 里 | 修复：改为按边去重（新增 `seen_edges: set[id]`，`if card.id not in seen_edges`），端点集合只用于决定下一跳 frontier；坑 #31 的"真实 PG 才暴露"规律再现 | 沉淀机制：①图遍历的"新边"判据必须按边 id 去重，不能用端点是否见过（多重边/双种子是反例）；②一个只在集成测试里失败的 bug，先做"单参 vs 多参"最小二分再读码——本次二分 3 分钟定位，读 200 行存储代码反而绕远；③T-07a 的 6 个非门控测试全绿却漏掉此 bug，说明"多条边 + 双种子"必须进 Layer 1 用例（已补） |
| 33 | 2026-09-17 | 图谱(存储) | 现象：修完 #32 后 `multi_hop(seeds=['Drug:a'], depth=2)` 在"a->b 有效、b->d 有效"的链上仍返回 `[]` | 定位：读 `multi_hop` 的早停判据 `if not any(len(p.entities) > 1 for p in paths): break`——它问的是"beam 里有没有长路径"，而 beam 里还留着停在原地的那条**短**路径时，整个光束会因为"有路径没展开"而被判成"没有任何展开" | 根因：把"某条路径走不动"（dead end，正常）与"所有路径都走不动"（该停）混在一个判据里；`paths` 里同时存在长度 1 和长度 ≥2 的路径时判据失真 | 修复：引入局部 `expanded` 标志，只由"本次循环是否真的产生了子路径"决定是否 continue/break；无子路径的路径原样保留（便于调用方区分"走到头"与"没开始"） | 沉淀机制：光束搜索的早停条件必须由"本轮是否发生扩展"这类**迭代级**信号决定，不能从"当前解集形态"（长度、分数）反推——解集里混着停滞解时，任何基于形态的判据都会误判 |
| 34 | 2026-09-17 | 决策(融合) | 现象：T-09 复核时给"跨通道矛盾→contested"补正例测试，用 `"b 与 c 有禁忌"` vs `"b 与 c 不有禁忌"`，`_contradicts` 返回 False 导致新测试直接挂；而另一组 `"eGFR 45 时 SGLT2i 仍可起始"` vs `"…不可起始"` 返回 True | 定位：打印 `NEGATION_MARKERS` 命中结果——`"禁"` 按子串匹配，两条声明都含**"禁忌"**（contraindication），双方都被判"含否定"，`!=` 两侧同真 → False | 根因：中文否定词表用子串匹配时，把域名词内部的否定字当成了极性翻转。"禁忌/禁用/禁食"都含"禁"，把"禁"放进否定词表等于让所有药物安全性声明都变成"已否定"，恰好命中本检查最该覆盖的那类文本 | 修复：`"禁"` → `"禁用"`（并保留 `停用/避免/不可/不再` 等完整词），注释写明"子串匹配要求每一项都是完整极性词，不得是域名词的组成部分"；新增跨通道矛盾正例测试（kg 断言 vs doc 否认，断言两侧都留、都置 contested、且不升级为 kg+doc） | 沉淀机制：①中文子串匹配的关键词表必须逐项自问"这个词会不会出现在正常文本里"——"禁"在医疗语料里几乎只以"禁忌/禁用"出现，是最坏的一类词条；②**写新测试时故意让它先跑一次**：本次正是"新测试挂"才暴露启发式在真实语料上失效，靠读代码不会发现；③关键启发式失效通常表现为**功能静默失灵**（矛盾永远不告警）而非报错，必须有正例测试把它钉住 |
| 35 | 2026-09-17 | 决策(校准) | 现象：T-09 复核揪出三处"卡上写假话"的路径：①置信度未落进任何桶时原值透传，而 `calibration_applied` 仍为 True；②桶结构不合法被 `continue` 静默跳过，卡照写"已校准"；③`test_card_records_that_calibration_ran` 的十桶表只在 `i==8` 特判 empirical，候选自报 0.9 却落进桶 9（`[0.9,1.0)`）拿到 0.95，断言 0.88 失败——**测试自己也没算清桶区间是半开的 `[lo,hi)`** | 定位：逐行读 `calibrate` 的返回路径与 `render_card` 里 `card.calibration_applied = table is not None` 的赋值，发现"表存在"被当成"校准已执行"；再手算十桶边界：0.9 属桶 9 而非桶 8 | 根因：把"有校准表"与"本次确实查到桶并应用了经验值"混成一个布尔；十桶曲线的区间语义（半开、末桶闭合）没有任何可执行契约表达，实现与测试各猜一套 | 修复：拆出 `_calibrate_in_table(conf, table, note)`，未命中桶/桶损坏时由调用方记 note 并置 `calibration_applied=False`；新增 `CalibrationTableModel`（十桶契约、bounds 越界即报错不 clamp）与 `DecisionCardContract`（拒答必空候选、推荐必有候选）把不变量写成 Pydantic 校验；`render_card` 出口自校验（失败记 warning，不把生产者 bug 留给下游读者）；新增 6 条测试覆盖未命中桶、坏桶、契约拒绝、payload 回读 | 沉淀机制：①"资源存在"≠"本次生效"——任何"已应用/已校准/已启用"标志必须由**真正把值写进去的那条代码路径**设置，不能从资源存在性推导；②不变量要写成可执行契约（Pydantic/断言）而非注释，并在生产者出口自校验，让 bug 在自己的栈里暴露；③分桶/区间这类边界敏感逻辑，先写一条"边界值归属"的独立断言把区间语义钉死，再写业务断言——本次是测试先错、才顺带发现实现里同样含混 |

| 36 | 2026-09-17 | 评测(平台) | 现象：E1 冒烟时 judge 调用间歇 500，`{'error': {'message': 'no active accounts available: total=36 active=0 cooldown=36 expired=0, earliest recover at ...'}}`；生成（deepseek）也偶发 500，重试 1-2 次后恢复 | 定位：freeshare 是共享免费池（36 个账户轮转），限流是平台侧按账户 cooldown 调度，返回体里带恢复时间戳；E0 记录过同类 429/500/空响应（E0 局限#3） | 根因：免费池账户并发上限 + 冷却窗口；不是代码问题，但**若把平台故障记成"答错"，会系统性压低 acc**——E0 当时只能"取可用应答子集"是妥协 | 修复：`eval_e1.py` 加 `_is_platform_fault`（识别 500/429/配额文案）+ 指数退避重试（judge 6 次，间隔 5s×(n+1)）+ 最终失败标 `platform_fault` 并从 acc/pass^k 分母隔离、单列报告 | 沉淀机制：①外部 LLM 网关故障必须与"模型答错"分离——评测指标的分母只能含"真实判定过的 run"，平台故障单列（`platform_faults` 计数）；②免费档跑分要预判限流，把重试预算写进 runner 而不是靠运气；③记录恢复时间戳便于调度（本次 earliest recover 09:18/09:31 UTC，与本地 17:18/17:31 对应，本地时段每 ~30s 一个 cooldown 窗口） |
| 37 | 2026-09-17 | 部署(环境) | 现象：PG 集成全量测试 8 连挂，`PermissionError: [Errno 13] Permission denied: '/mnt/nexent-data'`，位置在 T-08 接线测试的文件系统 mock | 定位：对比 T-09 验收命令（只显式传 PG 变量）与本机命令（`set -a; . ../deploy/env/.env` 全量 source），发现 `.env` 里 `ROOT_DIR="/home/qianqian/nexent-data"` 与 `LOG_DIR=/mnt/nexent-data` 是 **docker 容器内路径**，本机进程 source 后把 `LOG_DIR=/mnt/nexent-data` 注入 → 上游文件服务建目录时写 /mnt 无权限 | 根因：`.env` 是部署配置文件（docker 内部视角），本机跑测试全量 source 会注入容器路径；T-09 简报的验收命令已规避（只传 5 个 PG 变量） | 修复：本机跑测试只显式传 `POSTGRES_HOST/PORT/USER/DB/NEXENT_POSTGRES_PASSWORD`，不 source 全 `.env`；E1 跑分同样只带 PG 变量（模型配置从 PG model_record_t 读，不经 .env 的 KW_LLM_*——见 T-10a-2 简报的 LLM 配置说明） | 沉淀机制：①Nexent 本机测试环境变量纪律：**只显式传 PG 五件套**，绝不 source deploy/env/.env（那是容器视角）；②`ROOT_DIR/LOG_DIR` 这类路径变量在 docker 与宿主机语义不同，混淆会表现为难以归因的 PermissionError；③验收命令模板（03 §5）应把这条写进"运行环境"节 |
| 38 | 2026-09-17 | 评测(模型) | 现象：E1 冒烟跑分日志显示 HTTP 200 全绿、答案正常，但 model_id 显示 `z-ai/glm-5.3-free`（tokenrouter）；核对后才发现全程用的是**已作废的旧模型**，而非新注册的 deepseek | 定位：`const.py` L10 `load_dotenv(override=False)` 只从**当前目录**（backend/）找 `.env`——本机 backend/ 下没有 `.env`，所以 `KW_LLM_SMALL/MID/LARGE_MODEL_ID` 全空；`LlmRouter._resolve_model_config` 在 tier id 为空时 fallback 到 `tenant_config_manager.get_model_config("LLM_ID")`——租户配置表里的 LLM_ID=2 指向 tokenrouter glm-5.3-free | 根因：三层静默——①配置不在 load_dotenv 能看到的路径（deploy/env/.env 是 docker 视角，本机进程不读）；②tier id 空时不是报错而是 fallback（设计上合理，但 fallback 目标没更新）；③旧模型（tokenrouter）与新模型（freeshare）都叫"可用"，不报错不告警，看起来一切正常 | 修复：跑 E1 时显式 `export KW_LLM_SMALL/MID_MODEL_ID=4 KW_LLM_LARGE_MODEL_ID=6`（已写进简报验收命令）；冒烟命令先打印 `model.model_id` 自检再全量跑（本次靠这步抓到） | 沉淀机制：①**"不报错的错误"是最贵的错误**——LLM 配置想 fallback 时必须打 WARNING（llm_client 已有该日志，但被 INFO 淹没）；②换模型/换 key 后第一件事是打印 `model.model_id + api_base` 自检，而不是直接跑业务；③租户 LLM_ID 默认值要随模型注册一起审查（本次 tokenrouter 作废但租户 LLM_ID 没跟着换，是 E0→E1 迁移的漏网之鱼） |
| 39 | 2026-09-17 | 评测(端点) | 现象：换稳定商用端点 token.sensenova.cn 后，E1 全量跑分 53 分钟卡死——`/proc/<pid>/io` 显示 25 秒内零读写（进程完全静默），两条到本地代理的 TCP 连接收发队列为 0（请求已发、响应不来） | 定位：① 查 /proc/<pid>/task/*/wchan 见一线程在 `hrtimer_nanosleep`（退避睡眠）、一线程 `poll_schedule_timeout`（网络等待）；② 抓日志见 `429 Too Many Requests {'message': 'inference exceeds tpm/rpm limit', 'code': '429003'}`，且 SDK 内部重试日志写 `Retrying create in 147.9 seconds`；③ 查 model_record_t 发现 `timeout_seconds` 为 NULL → SDK 用默认客户端超时（无请求级上限） | 根因：两层叠加——**商业端点是 TPM/RPM 配额限流**（E1 的 judge 单次输入 800+、生成 2000+ tokens，串行密集调用很快打满），触发 429 后 SDK 内部按服务端 Retry-After 等 147 秒；而**模型记录没设超时**，一旦某次上游连接挂起，整个串行批处理无限阻塞（表现为"零 IO 静默"而非报错） | 修复：① 给 model_record_t 全部 LLM 记录设 `timeout_seconds=120`（SDK 会把它设成 HTTP client timeout，请求级失败而非永久挂起）；② runner 加 `_pace()` 调用节流（`MIN_CALL_INTERVAL_SECONDS`，CLI `--pace`，默认 8s）把请求速率压到 TPM 配额以下；③ `_retry_after_seconds()` 解析服务端建议等待并**封顶 300s**（防止被一个畸形值把批处理停几小时）；④ runner 层 `asyncio.wait_for(CALL_HARD_TIMEOUT_SECONDS=180)` 硬兜底，不依赖 SDK 行为 | 沉淀机制：①**外部 LLM 调用必须有三层时间防护**——HTTP client timeout（配置）+ 调用级硬超时（代码）+ 退避上限，缺一层就可能"静默挂死"（最难诊断的故障形态：进程活着、CPU 0%、无日志）；② 商业端点与免费池的失败模式不同：免费池是 500/账户冷却，商业端点是 TPM/RPM 429 + 长 Retry-After，节流（pacing）是商业端点的正确解法；③ 诊断"进程假活"先看 `/proc/<pid>/io` 增量再猜代码——25 秒零 IO 比任何日志都直接 |
| 40 | 2026-09-18 | 接线(前端) | 现象：本体工作台（T-05 已交付，`frontend/app/[locale]/knowledgeGraph/page.tsx` 存在）在生产里**根本点不到**——左侧导航无该项；同时前端 `knowledgeGraphService.activeVersion()` 一直在调 `GET /api/knowevo/ontology/versions/active`，后端却无此路由（404），本体树面板拿不到当前版本号 | 定位：① `SideNavigation.tsx` 的 `ROUTE_CONFIG` 无 `/knowledgeGraph` 项；② 更隐蔽的是**导航按 `accessibleRoutes` 过滤**（来自 RBAC `VISIBILITY.LEFT_NAV_MENU`），而 `v2.5.5_kw_003` 只种了 `RESOURCE.KNOWLEDGE_GRAPH.MANAGE`，**没种导航可见性** → 光加菜单项也不显示；③ `grep -rn 'versions/active' backend/` 零命中，但前端客户端接缝早已写好 | 根因：**页面/服务/客户端三层各交付了一部分，但没有一层负责"把它接进用户的视野"**——T-05 简报把导航注册登记给 T-12 后一直未做，形成"代码存在但不可达"的隐形洞（与坑#31 双 `/api` 前缀同类：接线漏项不报错、只让功能静默消失） | 修复：T-18a 三件套——① `ROUTE_CONFIG` 加 `/knowledgeGraph`（parentKey `/resource-space`）+ zh/en locale 键；② 新建 `v2.5.5_kw_004_workbench_nav.sql` 种 SU/ADMIN 的 `VISIBILITY.LEFT_NAV_MENU /knowledgeGraph`（幂等）；③ 后端加 `GET /ontology/versions/active`（无 published 版本 404，服务层新增 `load_active_version_row`/`get_active_row`，不动 T-04 的 snapshot-only 契约）；④ `package.json` 补声明 `@antv/g6`（此前只在 lockfile+node_modules，靠隐式存在） | 沉淀机制：①**功能任务的"待接线项"必须有唯一归属与到期波次**，否则就是永久待办；② 前端菜单=ROUTE_CONFIG×RBAC 双闸门，验收必须查真库 RBAC 行（`select ... where permission_subtype='/knowledgeGraph'` 得 2 行）而不只看代码；③ 新前端依赖必须同时进 package.json（lockfile 有 ≠ 声明有）；④ 验收清单加一条"该功能在导航里可达" |
| 41 | 2026-09-18 | 图谱(检索) | 现象：V 题（版本敏感）实测 BM25 把《2020 版指南》排 #1、《2024 版》排 #3-4；多份说明书可占满 top-5，权威指南被挤出视野；retrieve_context 的证据带 `authority_level` 但排序阶段完全没用它 | 定位：`e1_retrieval.Retriever.search`（backend/services/knowevo/e1_retrieval.py）只按纯 BM25 排序（`scored.sort(key=lambda h: h.score, reverse=True)`），`authority_level` 仅在 `Chunk` 上携带、从未进打分；也无任何按 `doc_id` 去重/配额——低权威说明书靠更高的词频天然压过高权威指南 | 根因：**检索排序没有"权威先验"概念**；同时单文档可占满 top-k，缺少多样性约束（MMR 的简化需求） | 修复：T-18d——① 乘性权威先验 `ranked_score = bm25 * w(authority)`，`w(1)=1.0, w(2)=0.95, w(3)=0.85, w(4)=0.75`（**降权低权威**而非提权高权威，`Hit.score` 保留纯 BM25 可追溯，新增 `Hit.ranked_score` 用于排序）；权重表可配置、默认值单测锁定；② 每文档配额 `per_doc_quota=2` 贪心（跳过超配额文档，跳过不占名额）——确定性 MMR-lite，零依赖；③ `authority_weights={}` 可关闭先验（T-22 消融的无权威臂）。真跑验证："胰岛素起始治疗" query 中 auth=1 指南以 bm25=22.979 胜过 bm25=24.034 的 auth=2 指南，且说明书不再刷屏 | 沉淀机制：① 检索排序必须显式消费权威/质量信号，不能只携带不消费；② 排序变更必须保留原始分（`Hit.score`）以便报告可追溯、改动可审计；③ 多样化（配额/MMR）与排序是两个正交维度，配额单独可配置；④ **消融实验必须有一个"关掉新机制"的开关**（`{}` 直接对应 no-authority 臂），否则无法证明是机制带来的提升 |
| 42 | 2026-09-18 | 评测 | 现象：E1 基线 acc=0.7692 的分母被污染——`eval_e1.py` 的 `_call_with_retry` 对非平台异常 `return None,None` 吞掉,上层无条件标 `platform_fault:True` 移出分母;实测 60 次运行排除 8 次,M 型 15 run 只判 8 次(M-003 整题消失),`by_type.M.acc=1.0` 是零判定静默消失制造的假象;且 `trace_completeness` 只测 BM25 字段填全率(构造上恒 1.0),不是答案级溯源 | 定位：非平台异常(代码 bug/schema 错/KeyError/Pydantic 校验失败)与平台故障(500/429/超时/配额)混为一谈;`_is_platform_fault` 用裸子串匹配(消息里偶然含 "502"/"429" 的代码异常会被误判);`summarize.by_type` 对零判定题型直接不进 dict | 根因：**"分母洗白"——用排除运行抬高准确率**,违反铁律 6;溯源指标测错了对象(测检索器没测答案) | 修复：T-18c——① 非平台异常默认 re-raise;`on_unexpected="count_fail"` 显式选择计 `pass=0` 进分母(batch 默认,避免长跑死于单题 bug 且不丢分母);② `_is_platform_fault` 类型优先(TimeoutError/ConnectionError 恒平台)+ 传输型 marker 加帧约束("error code: 502"才算,裸 502 不算);③ `summarize` 恒报 `n_judged/n_expected`;零判定题型输出 `{acc:null, insufficient_data:true}` 而非消失;分母无法被平台故障解释的缺口 → `integrity_warning:true` | 沉淀机制：① acc 永远与 n_judged/n_expected 并列报;② 排除运行必须有白名单(平台故障),其余异常要么炸要么进分母;③ by_type 不允许静默缺席;④ 溯源指标分两层:trace_machine(检索定位符完整率)+ trace_answer_cite(答案引用编号∈检索集,确定性零成本) |
| 43 | 2026-09-19 | 图谱(双时轴) | 现象：T-18b 修复前，真库 `kg_relation_t.valid_at` 全部是入库墙钟（生产代码无一处显式写入，`server_default=now()`），与本体版本 `created_at` 同轴 → `valid_at <= t_v` 恒真，版本钉住谓词退化为永远放行，V 题 pinned/unpinned 无差别、A4 消融 Δ 不可测；判别性 SQL 基线 t_v=2022 纳入 0 / t_v=2025 纳入 1 / 全库 369 | 定位：三个"时间"全是墙钟——`kg_entity_t/kg_relation_t.valid_at` 列默认（knowevo_db.py）、`resolve_version_clock` 取 `versions[i].created_at` 当 t_v（version_pin.py）、`doc_published_at` 读 `doc_asset_t.created_at`（kg_service.py）；且真库勘察发现 369 行关系 props 全无 evidence_id、46 条证据行 edge_ids 全空（eval 夹具/合成 PoC 数据，非证据链摄取产物）——简报"294 条关系可回填"前提失效 | 根因：**D1 事实业务时间缺位**——事实的"业务生效日"（文档出版日）从未被建模，valid_at 被墙钟顶替；比对基准 t_v 也取墙钟，两钟同源必然恒真。这是一类系统性坑：**比对谓词两侧的时间字段如果同源，谓词就退化为常量** | 修复：T-18b——① registry 加 `published_at`（derive_published_at.py R1-R5 从 license_note 期刊年卷期/文号/资产号年可溯推导，R5=不发明）；② 摄取落 `doc_asset_t.metadata` + `_merge_edge` 显式 `valid_at`=来源文档业务日（无据可查按 undated 计数不伪装）；③ `commit_version` 写 `fact_cutoff`，`resolve_version_clock` 优先级 as_of→fact_cutoff→created_at→now；④ 三阶段幂等回填 `repair_fact_time.py`（文档←registry / 版本←租户最大出版日 / 关系←证据链，全链诚实计数，本次实测回填 30 份文档日期、369 行关系无证据链如实保留）；⑤ 判别性护栏=单测 TestDiscriminativeVersionPin + PG 集成 test_discriminative_version_pin_on_real_db（真库播种：同组边 t_v=2022 纳入 1 / t_v=2025 纳入 2） | 沉淀机制：① 任何"时间语义"字段入库前先答三问——这是业务时间还是系统时间？比对对象和它同轴吗？无值时回退谁且如实计数了吗？② 回填脚本的产出物是**分类计数报告**而不是 UPDATE 行数——无证据链的 369 行全部落 no_evidence_link 桶并保留原值，"回填了 0 行"与"没跑"在报告里必须可区分；③ 简报里基于历史快照的数字（294 行）在动手前要重新勘察真库，数据前提会过期 |
| 44 | 2026-09-19 | 模板(Skill) | 现象：T-20 spike 发现三处**静默过滤**陷阱——① SKILL.md frontmatter 白名单外字段（如 `version`）被静默丢弃不报错（skill_loader.py:64-65）；② `allowed-tools` 里未注册的工具名被静默过滤（skill_db.get_tool_ids_by_names 只查已注册名，上传成功但工具关联为空）；③ frontmatter 必须成对 `---`，否则上传报 `SKILL.md must have YAML frontmatter`。另：挖掘真跑首轮 LLM 全部失败——管线默认租户 00000000-…-001 下 model_record_t 配置解析不到（"no LLM configured for tenant"），模型 7/8 实际属于构建租户 6756b0ab | 定位：skill_loader.py `_ALLOWED_SKILL_META_KEYS` 白名单 + 静默 continue；skill_db.py:674-692 `ToolInfo.name IN (...)` 查不到即空；llm_client 按传入 tenant_id 解析模型配置，而 model_record_t.tenant_id 是作用域键 | 根因：**配置/校验失败普遍走"静默降级"而非报错**（与坑 #31/#40/#38 同类）；LLM 模型配置是租户作用域资源，跨租户跑管线必炸但报错信息不指向租户错配 | 修复：①② 写进 skill-mechanism.md spike 结论与 SKILL.md 模板编写约束（只写白名单五键；allowed-tools 允许预写未注册工具名——T-20 模板预写 decision_card_render 安全）；③ 挖掘脚本加 `_self_check`（打印 tier→model→api_base 实际解析结果，坑 #38 纪律）+ 显式 `--tenant` 传构建租户 + `--cross-tenant` 汇聚（100 卡/75 租户单租户 support≤1 的现实），降级时 `induced_by=deterministic` 标注、0 次调用计入 cost-ledger | 沉淀机制：① 读上游机制先找"静默过滤点"（白名单/IN 查询/正则 fallback），把"会被吞的字段"写进对接文档而不是靠报错发现；② LLM 配置自检必须打印 tenant→model→api_base 三元组，"model_id 存在"≠"该租户可用"；③ 小样本分散（2 卡/租户）是评测夹具库的常态，跨租户汇聚必须显式开关并在 source 里留 provenance，不得静默混数据 |
| 45 | 2026-09-19 | 决策卡(暴露层) | 现象：T-19 给 `decision_card_render` 做"进程内真跑"时，按简报式命令 `from server import decision_card_render; await decision_card_render(...)` 调用报 `'FunctionTool' object is not callable`——fastmcp v4 的 `@mcp.tool` 装饰器把模块级函数名替换成了 FunctionTool 对象；另，MCP 冒烟发现租户 A 有图谱证据、模型配置在租户 B（模型 7 属构建租户 6756b0ab），证据齐但 render 阶段抛 LLMConfigurationError | 定位：fastmcp 装饰器包装后原函数只剩 `.fn` 属性，server.py 里 `*_handler` 纯函数才是进程内/测试可调的缝（kg_tools.py 双注册走的就是它）；LlmRouter 按 tenant_id 解析 model_record_t，跨租户必炸但裸异常类型不指向根因 | 根因：①验收命令把"注册名"当"可调用对象"——装饰器形态与纯函数缝没在文档里区分；②更危险的是暴露层（HTTP/MCP）若照搬"任何异常都降级"的思路，会把**基础设施故障伪装成诚实拒绝**——图库挂了还渲染 INSUFFICIENT_EVIDENCE 卡等于谎报"知识不存在"，恰好摧毁 T-09 拒绝纪律的证据价值（INSUFFICIENT 是"检索过但没有"，不是"没查成"） | 修复：①handler/装饰器双层保持——`decision_card_render_handler` 纯函数供进程内冒烟与测试调用，装饰器只做注册；②暴露层错误三分级——证据收集失败→502/结构化 error（**不出卡**）、有证据无 LLM→503/`error_code=llm_unavailable`（**不出卡**）、无证据→INSUFFICIENT_EVIDENCE 卡（零 LLM 调用、照常落库进 needs_rerun 台账）；③MCP 冒烟带真实租户+自播种边+finally 清理，三分支各验一遍：INSUFFICIENT 真实落库（decision_card_t 新增行 card_id=32c98cff-96d9-4120-a7a9-3ddc6eff61bd，needs_rerun=t）、llm_unavailable、真实 LLM 403→`decision_card_render_failed: PermissionDeniedError`（不泄内部细节） | 沉淀机制：①**"诚实拒绝"是数据结论、"故障"是运维事件，暴露层必须映射成不同的响应**，否则降级纪律在边界上被静默摧毁；②fastmcp 工具的验收/冒烟命令写 handler 名而不是注册名（FunctionTool 不可调用是 v4 行为，`.fn` 才是原函数）；③跨租户组合（数据在 A、模型在 B）是租户作用域资源的常态故障场景，工具的 error_code 必须能区分"没配置"（llm_unavailable）与"坏了"（*_failed），hint 不泄内部信息 |
| 46 | 2026-09-19 | 模板(Skill)/暴露层 | 现象：T-20 集成轮给 /skillTemplate 前端页找数据源时发现简报只授权了 MCP 工具（skill_template_apply）作为后端暴露，没有任何 HTTP 列表路由，现成的列表能力只在服务层（list_templates）与 T-19 领地的 knowledge_graph_app.py 里；另：①进程内真跑 handler 首次未带 DB 配方 env，psycopg2 报错指向本地 socket `/var/run/postgresql/.s.PGSQL.5432`（与实际 localhost:5434 配置无关）造成一次误判；②apply 真跑发现渲染后的"实例"文本仍保留 `{relation_template}`/`{domain_rules}` 占位符 | 定位：T-20 简报独占文件清单不含任何 HTTP app 文件；DB 连接串由 env 决定但报错文本只显示"失败发生地"；skill_template_service.render_template 对空串/None 变量的契约是保留占位符不注空值 | 根因：①任务简报未授权的暴露面若顺手补上，等于越权改他人领地并发明未验收的后端面；②基础设施故障的错误文本天然指向失败现场而非配置错因；③apply 时刻成功与否未知、空变量注入会让模板失去"待填"语义——宁保留占位符不编造 | 修复：①前端 /skillTemplate 按"页面+类型+服务层就绪、数据源待接线"交付——skillTemplateService.ts 指向约定的 GET /api/knowevo/skill-template/list 并注释未接线原因，页面 catch 后渲染"数据源待接线"Alert（写明当前唯一后端暴露是 skill_template_apply MCP 工具、模板清单可 psql 查 skill_template_t 核实），不编造行数据、不动 knowledge_graph_app.py；②进程内真跑一律先对齐 DB 配方 env，结构化错误边界（skill_template_apply_failed）保证故障不伪装成业务结果；③apply 语义如实记录：占位符保留是"待填"而非缺陷，reuse_success 只由 record_reuse_outcome 回写，MCP apply 不返回成功率、不虚构复用效果 | 沉淀机制：①跨任务集成时"暴露面授权清单"以简报独占文件为准，缺的路由记"待接线"并让 UI 如实展示缺失状态——编造数据比缺功能更毁信任；②进程内验证带 DB 的代码先对齐 env 配方，连接类报错先查连接串再怀疑代码；③渲染器的空值语义（保留占位符）必须与消费方（Agent/人）对齐，"模板未填"要可见，不能静默变空串 |
| 47 | 2026-09-21 | 校准(度量) | 现象：T-21 P/R 首测 precision=0.00145 / recall=0.111，无法反映检测质量；未提交的 tier-3 "token 重叠"修复也救不回来（仍 4/9） | 定位：①机器按**段落**发 691 项变更、金标是 9 条**话题级**行——条目级分母错配，precision 上界≈1.3% 由粒度决定而非质量；②未提交 tier-3 用 `re.findall(r"[\u4e00-\u9fff]{2,}")` 把整串中文当一个 token——"血糖控制目标应遵循个体化原则"是一整块，任何两段文本都共享不了 token，重叠匹配结构性失效 | 根因：**度量口径与标注粒度错配 + CJK 无分词时把"整句"当"词"**。金标非穷尽样本（16 条 curated 话题 vs 691 项真实变更）意味着按条目算的 precision 是伪指标；而中文 token 化不做 n-gram 切分则任何重叠匹配都退化为恒假 | 修复：①新增 `calibrate_topic`（纯函数）：段落级变更 → (change_type, 归一化章节) 聚合 → 519 组；金标话题 ↔ 组用**判别性 token 重叠**（CJK 三元组 + ASCII 词，文档频率 ≤5% 过滤"糖尿病/治疗"类通用词，min_shared≥2）；②官方口径=recall_topic（9/9）+ precision_lower_bound（64/519，明确标注下界）；strict 条目级数字保留在报告与 ops_summary 里不隐藏；③参数随结果落盘可复现；④负对照验证（航天/量子/区块链等 5+ 不相关话题全 0 命中）+ df 参数扫描平台期确认非凑数 | 沉淀机制：①**度量粒度必须与标注粒度对齐**：金标是话题级就按话题级聚合再算，条目级分母错配的 precision 不要报成指标；②**中文 token 化必须用 n-gram**（CJK 无分隔符），整串正则切分在重叠匹配里等于恒假；③"下界/基线"类数字必须带标签落库（ops_summary.calibration.mode / precision_is_lower_bound），防止下游把下界当精确率；④任何匹配规则的默认参数必须过**负对照 + 参数扫描**两道防线才能算稳健，否则就是调参凑数 |
| 48 | 2026-09-21 | 接线(前端) | 现象：T-23 截图轮实测 `/knowledgeGraph`、`/skillTemplate` **直接 URL 导航被重定向回首页**（T-05 e2e 未跑过的直接后果）；截图轮用 playwright `addInitScript` 大小写不敏感补丁才绕开 | 定位：路由守卫把 `accessibleRoutes`（RBAC 可见性表）`toLowerCase` 后比对，而 URL 路径段含大写 → 守卫比较大写路径恒不命中 → 兜底重定向首页 | 根因：守卫两侧大小写不对称（一侧归一化一侧没归一化），"接线漏项不报错、功能静默消失"（坑 #31/#40 同类） | 修复：✅ **已修复（r18，提交 0c4f613e3）**：三处比较统一 lowercase 归一化——①`canAccessRoute` 入参 `route.toLowerCase()`；②守卫 `normalizedCleanPath = cleanPath.toLowerCase()`（`includes` 精确匹配与 `startsWith(route + "/")` 前缀匹配共用）；③侧边栏菜单过滤 `route.path.toLowerCase()`；证据=type-check exit 0 + eslint 两文件 0 新增违规 + prettier（SideNavigation 已过；useAuthorization 遗留 diff 为改动前既有）+ 本分支新增 node:test 回归测试 10 passed 锁定语义；**e2e/浏览器验证未跑**（栈不可用，诚实记录） | 沉淀机制：①前端验收清单必须含"直接 URL 访问该路由"（区别于点击导航进入）；②守卫类比较逻辑两侧都要归一化，半侧归一化等于没归一化 |
| 49 | 2026-09-21 | 图谱(HTTP边界) | 现象：T-23 截图轮本体工作台数据为空——本体 HTTP 边界 `OntologyService(store=None)`（T-05"合并前还原"遗留），实例化时未注入 PgStore | 定位：ontology 路由层服务构造处 store 参数默认 None、未接图存储 → 查询返回空 | 根因：T-05 合并时把 store 注入还原掉，路由层与服务层脱节（坑 #31 同类：接线漏项不报错） | 修复：✅ **已修复（r18，提交 1c3467dae）**：`_ontology_service()` 函数体内惰性导入 `ontology_service.PgStore` 并注入（与 `_decision_service` 同纪律，模块无 DB 仍可导入）；证据=单测 21 passed/3 skipped + PG 集成 24 passed（含构建租户 v1.1.0/10 类/10 关系实证）+ ruff clean + 真库实证 active_version=v1.1.0 | 沉淀机制：①HTTP 边界验收必须断言"返回非空数据"而不只是 200；②服务实例化的依赖注入在合并还原后要重跑冒烟 |
| 50 | 2026-09-21 | 评测(节流) | 现象：T-22 续跑准备核对发现 `--pace` 参数**不生效**——ablation.py 的 `global MIN_CALL_INTERVAL_SECONDS` 改的是自身模块副本，`eval_e1._pace()` 读的是 eval_e1 模块的全局，节流恒为 8s（配置里已如实记录） | 定位：跨模块全局变量语义——改 ablation 模块的全局不改变 eval_e1 模块同名全局；CLI 传 --pace 但 eval_e1 仍用默认 8s | 根因：全局常量跨模块共享必须从定义方导入，ablation 的赋值只改了自己的名字空间 | 修复：**仅登记不修**（默认 8s 可接受、未阻塞续跑）——建议后续把 MIN_CALL_INTERVAL_SECONDS 收敛到单一定义处（如 const.py）或改为显式参数传入 eval_e1 | 沉淀机制：①"改名字空间≠改全局"是跨模块全局的隐蔽失效点，参数应显式传递；②CLI 参数验收加一条"实际节流间隔可观测"断言，别只测解析 |
| 51 | 2026-09-21 | 网关(配额诊断) | 现象：构建租户图谱摄取自 2026-09-19 17:50 起 **1.5 天零进度**（r12-r18 七轮、多次突发窗全部 0 span）；sensenova 端点（模型 7/8/9）抽取调用反复 120s 超时或 429，A3 消融诊断拿到确切报错 `429 {'error': {'code': 'insufficient_quota'}}`（SDK 退避 148-236s）；轻量调用（探针）间歇 200 OK | 定位：①模型→端点/上限映射实证（model_record_t）：sensenova（7/8/9，max_tokens 8192，商用配额）、freeshare.cc.cd（4/5/6，max_tokens **4096**，免费池独立配额）、tokenrouter（1/2，已弃用）；②sensenova 硬配额耗尽（insufficient_quota），非滚动窗（1.5 天未回血）；③freeshare 桶实测：响应快（~90s）但 max_tokens=4096 截断抽取 JSON → 解析失败 → 熔断；加紧凑指令（r18 实验版 run_real_ingest_compact.py）仍超时；④A3 消融：首题 ~5 分钟能过（轻量调用可通过），随后撞墙 | 根因：**sensenova 账户 token 配额耗尽（insufficient_quota）**，密集大调用（抽取 5k in/8k out）必撞；freeshare 免费池模型输出上限 4096 且对长 few-shot 抽取 prompt 易跑偏/超时，**两个可用桶目前都不支持剩余 ~111 span 的可靠摄取** | 修复：**外部卡点，需用户决策**——①给 sensenova 配额充值/升级（r8 预判"若用户升级配额可一箭到底"）；或②提供另一可用的 API key/端点（写入 model_record_t 即可）；或③接受 freeshare 路径并改造抽取（span 拆分 + 紧凑 prompt + 重试调优，属工作区驱动改造、需如实标注抽取模型混合）；熔断器全程保护数据（无空段污染，span 保持可重试） | 沉淀机制：①**探针健康 ≠ 持续调用可行**（小调用通过、大调用撞墙），诊断必测真实负载；②429 报文要读 `code` 字段——`insufficient_quota`（硬配额）≠ 瞬时限流（rolling tpm），应对策略完全不同；③模型配额按**端点分桶**（model_record_t.base_url 是唯一可靠分桶键），换模型前先查 base_url；④freeshare 模型 max_tokens=4096 是抽取类任务的硬约束（结构化 JSON 输出常超），选型先看上限 |
| 52 | 2026-09-21 | 抽取(模型行为) | 现象：摄取抽取调用 **HTTP 200、completion_tokens>0，但正文为空**（r19 三窗全 0 span 的直接原因）。原始非流式响应实测：`content_len=0`、`reasoning_len=30795` 字符、`finish_reason=length`、`completion_tokens=8192` 且 `completion_tokens_details.reasoning_tokens=8192` | 根因：deepseek-v4-flash（模型 7）对本抽取 prompt 产生超长推理链，**输出预算（DB `max_output_tokens=8192`）被推理 token 全额占用**，模型在推理中途被截断，正文（JSON）从未开始生成。适配器侧 `OpenAIModel` 流式路径把 reasoning 只送 observer（`sdk/nexent/core/models/openai_llm.py:417-426`），无可见正文时抛 `EmptyModelResponseError`（同文件 :565），驱动把它计为硬失败——所以表现是「空」而非「报错」 | 修复：✅ **已修复（r19 提交 7443a57f1；r20 独立评审 + 修复项闭环 f3d154f53，评审报告 .task_b_status/r20-review-r19.status.json）**——**唯一因果杠杆**=按调用传 `extra_body={"thinking":{"type":"disabled"}}`（smolagents `generate()` 从 `self.kwargs` 组请求体，构造层命名参数进不了 wire——r19 探针实测 `completion_kwargs == ['messages','model']`）；client 级 `max_output_tokens` 置空属清理项（wire 上本就是 null，截断来自 provider 默认 8192——r20 评审 P2-4 归因修正）。证据链：`test_llm_client.py` **9 passed**（r20 加判别性 cap 断言 + ruff PLR0402 清零）+ `probe-wire-extra-body.json`（wire 证据以它为准；`wire-captured.jsonl` 是 709 字符截断的入站抓包，**不能**作 wire 证据——评审 P1-2）+ `model-compare-r19c.json`（评审 P1-1：**内存态** 4 实体/2 边/11.7s/out=394，脚本按设计不落库，非编造）+ `usage-350026.json` + **r20 真链路（seg-r20/seg-r20b 窗）：extract 6-18s/out 590-3518 全部成功、质量门禁全绿（83/83 实体带 evidence_ids、0 缺 valid_at、pending_pool=0）**。诚实缺口：①`finish_reason`/`reasoning_tokens` 观测尚未接进产品抽取路径（评审 P1-4，登记为后续任务）；②r20 首窗 usage 未测量（kill-after 先于 finally，已知取舍） | 沉淀机制：抽取链路必须记录 `finish_reason` 与 `completion_tokens_details.reasoning_tokens`；「无正文」与「无实体」分开统计；模型对比/实验脚本必须记录解析到的 model_name+base_url（见 #53） |
| 53 | 2026-09-21 | 接线(实验) | 现象：r19 模型对比首跑三个「不同模型」全部走到同一端点 `api.tokenrouter.com` 并 403 `This token has no access to model z-ai/glm-5.3-free`，0 token 消耗白跑一轮 | 根因：`llm_client._tier_model_id()` 读的是**模块 globals**（`KW_LLM_*_MODEL_ID` 在 `consts/const.py` import 时从环境变量绑定一次）；实验脚本在 import 之后改 `os.environ` 完全无效 → 三档 model_id 全空 → 回退租户默认 LLM（tokenrouter 的 glm-5.3-free）→ 令牌无该模型权限 | 修复：按 `llm_client.py:47-50` 的设计直接 patch 模块属性（`setattr(llm_client, 'KW_LLM_MID_MODEL_ID', '9')`）；已在 `compare_extract_models.py` 落地并加 `resolved{model_name,base_url}` 证据字段 | 沉淀机制：任何绕过 CLI/shell 的 LLM 实验必须在结果 JSON 里回显 resolved 模型名与端点；「403 无权限」先查是否静默回退到了租户默认模型（与 r14 的 STEP3 403 同源） |
| 54 | 2026-09-21 | 图谱(驱动保护) | 现象：paced 驱动文档声称「彻底失败的 span 保持可重试、绝不记为空抽取」，但代码路径未兑现：`ingest_graph._run` 对**每个处理过的 span 无条件** `record_extract_run(status=done)`，而 `PacedJsonLLM.__call__` 在三次尝试全空后 **return {}**（不是抛错），熔断阈值默认 `KW_MAX_SPAN_FAILS=3` 使**首个**失败 span 不触发熔断 | 根因：空结果与失败结果在契约上不可区分（都返回 dict），驱动用「返回 {}」表达失败，上层把它当成功处理并落 done → `span_hash` 幂等会让该段被永久跳过（r8 曾实际发生并需手工 DELETE 恢复） | 本窗实证：r19 10:02 窗日志 `progress: 10 spans, 1 extracted, 9 skipped`，但事后核库 `kg_extract_run_t` 仍**恰 10 行**、最新 02:13——该记录未提交（进程被 timeout 杀在提交前），故**本窗未实际污染**；但「是否污染取决于时序」不可接受 | 修复（待执行）：突发窗起跑前设 `KW_MAX_SPAN_FAILS=1`（首个彻底失败即 raise 退出，span 不落 done），或改驱动在彻底失败时抛错而非返回 {} | 沉淀机制：每个突发窗收尾必须做三方对账——`extract_run` 行数增量 = 实体/关系/证据增量 是否自洽；发现 `kg_extract_run_t` 有可疑空段即 `DELETE ... WHERE run_id=<可疑>` 恢复可重试（实体不删，重抽 merge 去重） |
| 55 | 2026-09-21 | 评测(LLM 行为) | 现象：E8 真跑（r20-e8）中**非 extract 种类**也出现空正文——`empty llm content for kind=hop_plan (attempt 1/4)`（20:54）与 `kind=decision_card (attempt 1/4)`（20:57）；**本次均在下一次尝试恢复**（后续 200 OK，无 attempt 2/4 记录），但单题耗时被拉到 ~9min | 根因（待定论）：与 #52 同族——hop_plan/decision_card 是生成型 prompt，deepseek-v4-flash 可能把输出预算花在推理链上（`thinking:disabled` 仅对 extract 生效，其他 kind 保持 provider 默认）；亦可能是网关瞬时抖动 | 现状：**观测级不修**——重试即愈、未影响落库正确性；若复现率上升，候选=①把 `thinking:disabled` 扩到 hop_plan/decision_card（需评估推理质量影响）②为这两个 kind 显式设 max_tokens | 沉淀机制：LLM 调用层记录「kind × 空正文次数」计数器（与 r20 评审 P1-4 的 finish_reason/reasoning_tokens 观测项一并纳入后续任务） |
| 56 | 2026-09-21 | 交付物(评测报告诚实性) | 现象：r21 真跑 E8 分片后核对交付物 `e2-ablation-report.json`，**同一份 JSON 自相矛盾**——`e8.caveat` 声称"构建租户图谱为空（0 实体 / 0 关系）…判别性计数 all:474 / t_v=2025-06:1"，而同文件 `data_reality`（本次实测）为 `entities:136 / relations:65 / t_v_2024_06_01:190 / discriminative:true`；再核对发现 e8 块的 `pin_on_run`/`pin_off_run` **恒为 null**，且 `per_type` 的 Δ 是把 **2026-09-19 的 pin=on 臂**（eval_run 17ce73d5…，nq=2）与 **2026-09-21 的 pin=off 臂**（eval_run 628d7fce…，nq=1，含配额风暴）拼成一个"配对"结果 | 根因：① `E8_CAVEAT` 是模块级**硬编码字符串**（写于 2026-09-19 空图期），不随实测更新，而顶层 `generated_at` 每次刷新 → 造成"内容新鲜"的假象；同源文案还留在模块 docstring（"the current build-tenant graph is empty"）；② `_e8_section` 读 `row.get("run_id")`，生产写入的键却是 `eval_run_id` → 真实报告臂溯源恒空；**单测 fixture 恰好用 `run_id`**，把键名错配掩盖了整整两轮；③ `results` 按设计跨 invocation 累积（配额窗口断续续跑），但 e8 块**没有任何 vintage 披露**，读者会把"两个日期、可能两个图谱状态"误读为一次受控配对实验 | 修复：✅ 已修（r21 提交 47bed07e0 之后同一波）——`E8_CAVEAT` 重写为**带日期标注的历史观察** + 指向实测 `data_reality`，删除所有会过期的绝对数字；模块 docstring 同步纠正；`_e8_section` 改用 `eval_run_id`（保留 `run_id` 回退以兼容旧报告/fixture），新增 `arm_vintage`（每臂 eval_run_id / invocation_id / recorded_at + `same_invocation` **三态**：True 配对 / False 并置 / None 无戳）；结果行新增 `invocation_id`+`recorded_at`。证据：修后重算同一 `results`（`per_type` 数字逐位不变）→ `pin_on_run=17ce73d5…(2026-09-19)` / `pin_off_run=628d7fce…(2026-09-21)`，`same_invocation=null`（诚实标"无法判定"）；测试 +4，`test_ablation.py` 34 passed、knowevo Layer-1 **675 passed** 零回归、ruff 无新增问题 | 沉淀机制：交付物的"数据现实"必须**每次实测**（`data_reality`），叙述性文本一律**禁止复述会过期的快照数字**；跨 invocation 聚合的指标必须带 vintage 戳与 `same_invocation` 语义，Δ 不得在未披露来源时呈现为配对对比；单测 fixture 必须使用生产真实键名，否则键名错配会被 fixture 静默掩盖 |
| 57 | 2026-09-21 | 图谱(产品诊断落库) | 现象：#52/#55 的观测在 r21 只做到「调用方可拿到」——产品台账 `kg_extract_run_t` 仍只有 `tokens_spent`，无法回答"这次 span 为什么没抽出东西"；「无正文」（空 body，如推理链吃掉输出预算 `finish_reason=length`）与「无实体」（解析成功但 entities 为空）在库上不可区分 | 根因：① `LlmRouter.__call__`（llm_client.py）把 `call_with_usage` 的 usage 直接丢弃（`content, _usage = ...`），走 `__call__` 的抽取路径看不到 `finish_reason`/`reasoning_tokens`；② `KgExtractRun` 无对应列，`record_extract_run` 签名只有 `tokens_spent` | 修复：✅ 已落地（r22）——①基线 `v2.5.5_kw_009_extract_run_diagnostics.sql`（**只加列、幂等**，`ADD COLUMN IF NOT EXISTS`，真库两遍 0 error）给 `kg_extract_run_t` 加 `llm_calls`/`empty_content_calls`/`reasoning_tokens`（INT NOT NULL DEFAULT 0）+ `finish_reasons`（JSONB）；②`LlmRouter.last_usage()` 新增只读诊断面（`__call__ -> str` 契约不变，既有 14 条测试零改动）；③`record_extract_run` 新增可选诊断参数（老 5 参调用行为不变）；④`pipeline/ingest_graph.py` 用 `_SpanDiagnostics` 包装注入的 LLM，把 span 内跨调用聚合成 `llm_calls=N / empty_content_calls=M / reasoning_tokens=Σ / finish_reasons={fr:count}` 随 span 台账落库（`_is_blank_body` 同时处理 str 与驱动返回的 dict：空 dict=无正文，`{"entities":[]}`=无实体） | 证据：单测 105 passed（新增聚合/兼容/列/`last_usage` 用例）+ PG 集成 713 passed 零回归 + ruff 零新增（改动 7 文件 HEAD==NOW：10/0/0/0/0/0/25）+ 真库 `\d nexent.kg_extract_run_t` 四列可见 + kw_009 两遍 0 error | 沉淀机制：「无正文 vs 无实体」不能只停在进程级 usage JSON（驱动层），必须落**产品台账**每行；调用层诊断面一律用**加性只读方法**（`last_usage()`）而非改既有返回值；新增诊断列一律 `NOT NULL DEFAULT` 保证老行可读 |
| 58 | 2026-09-21 | 评测(实验方法学) | 现象：E8 连续两轮窗口都在"缺 on 臂"上翻车——`--pin off,on` 时 **off 臂先跑并吃光 12 分钟预算**（r22 log：`resuming A4_full\|off\|1` → `[2/3]`→`[3/3] V-003` → `budget exhausted before V-001 [1/3]`（on 臂））；中途想杀进程改序，**kill 类命令在本机无人应答时被安全审批门挡住（超时=未批准）**，只能看着窗口跑完 | 定位：多臂 runner 的 plan 顺序 = `--pin` 列表顺序（`build_plan(levels, pins)`），预算检查在**题与题之间**执行；r22b 改成 `--pin on,off --limit 3` 后 on 臂立刻先跑（`[A4_full/pin=on] [1/3] V-001`），23:34 拿到真实行 `9beec27a` | 根因：**"谁先跑"完全由参数顺序决定，而缺臂恰好总在列表末尾**；且无审批环境＝无法运行期干预，参数必须一次定对 | 修复：r22b on-first 成功产出 pin=on 行；规则固化为**缺臂/关键臂放 `--pin` 首位**；窗口启动前把臂序/题数/预算一次定对（dry-run 或核对 plan 顺序） | 沉淀机制：①多臂实验的臂序=优先级序（缺臂必须第一）；②无审批环境禁止"先起后调"；③**并置 Δ 解读纪律**：`same_invocation=false`、CI 重叠、或存在空正文退化（如本次 on 臂 2×hop_plan 空正文+2×160s 退避）时，只能报"不可判定/需配对复跑"，**不得宣称机制有效或无效** |
| 59 | 2026-09-22 | 评测/产品(LLM 调用契约) | 现象：E8 配对复跑（e8-paired，pid 565799，01:00-01:42）形式上跑完（on 3/3+off 3/3、`same_invocation=true`），但 **24× `event=llm_empty_content kind=ablation_decision_card/ablation_hop_plan`**（finish_reason=length rt=8192，以及 stop rt=3921 但正文仍空）→ on 臂 2×runner_error、off 臂 3×runner_error（"card render runner error: llm returned no content after 4 attempts"）按 count_fail **计入分母** → `delta_acc_on_minus_off=-0.1667` 不可读 | 定位：r19 修复（7443a57f1）把 `thinking:{type:disabled}` 只挂在 `kind=='extract'`（llm_client.py `_get_model` 的 `disable_thinking = kind == "extract"` + `call_with_usage` 的 per-call `call_kwargs`）；而 ablation 包装器给 kind 加 `ablation_` 前缀（ablation.py:235 `kind=f"ablation_{kind}"`），decision_service 实际 kind 是 `route_llm`/`hop_plan`/`decision_card` → 卡链两个 JSON-object kind 的输出预算被推理链吃光 | 根因：r19 的修复范围按"当轮观测到的症状"（仅 extract）收窄，**没有按调用契约的 kind 清单系统性排查同族 JSON-object kind**；judge/route/answer 有观测未空 → 未列入；hop_plan/decision_card 当时无 per-kind 空正文计数器（r21 P1-4 才补），盲区未显形 | 修复：✅ 已落地（r24 主线程）——`llm_client.py` 新增 `_THINKING_DISABLED_KINDS = {extract, decision_card, hop_plan}` + `_thinking_disabled()`（剥离 `ablation_` 前缀后判定；judge/align/route/route_llm/answer 保持 provider default 不动），`_get_model` 与 `call_with_usage` 统一走该判定（`_EXTRACT_EXTRA_BODY` 更名 `_NO_THINKING_EXTRA_BODY`）；测试新增 `test_card_chain_kinds_disable_thinking` + 真实 kind 清单化的 `test_other_kinds_send_no_provider_extras` | 证据：test_llm_client.py **16 passed**、ruff All checks passed、T-26 工作树测试（test_kg_service+test_decision_card_app）114 passed 5 skipped 零回归；e8-paired.log 空正文事件为根因证据；**配对 Δ 判读纪律**：修复落地前产出的 e2-ablation-paired.json Δ=-0.1667 作废，待 e8-paired2 重跑 | 沉淀机制：①按 kind 契约做修复时必须**枚举全部 kind 并逐一核对**（结构化 JSON 输出 kind 同族），不能只修当轮观察到的那一个；②per-kind 空正文观测（r21 P1-4）是发现此类盲区的探针——新 kind 接入时先跑一次最小探针看 finish_reason/reasoning_tokens；③小样本中间态 Δ 会翻向（#58），配对窗跑完也必须先核对 runner_error/空正文占比再读 Δ |
| 60 | 2026-09-22 | 网关(LLM 调用契约·行为漂移) | 现象：kw-cardfix 首次真实 no-thinking 调用（hop_plan kind，5010 已载 r24 修复代码）**HTTP 400**：`invalid thinking type, only be disabled when reasoning effort is none and enabled when reasoning effort is not none`——r24 的 `_NO_THINKING_EXTRA_BODY={"thinking":{"type":"disabled"}}` 被网关整体拒绝（连响应都没有，不是空正文） | 定位：kw-cardfix 用 monkeypatch 一次性实测（零代码改动、1 次调用）：`extra_body={"thinking":{"type":"disabled"},"reasoning_effort":"none"}` → **200** / content OK / finish_reason=stop；主线程随后 mid 档（模型 7）三 kind（decision_card/hop_plan/extract）真网关探针全部 **200 / fr=stop / rt=0** | 根因：**网关行为漂移**——r19 的 wire 抓包（09-21）时 bare `thinking:disabled` 曾通过（extract 出实体/出边），09-22 同一 extra_body 已被 sensenova 新校验拒绝；`reasoning_effort` 缺省为非 none 时，`thinking:disabled` 单独出现即非法 | 修复：✅ 已落地（r24，提交 `35d62f910`）——`_NO_THINKING_EXTRA_BODY` 补 `"reasoning_effort":"none"` 成对发送；该常量喂**所有** disabled kind（r19 extract + r24 卡链）→ extract 摄取路径被同一修复救回（否则当前网关下摄取必 400 挂掉——重大潜在回归被提前拦截）；test_llm_client.py 断言改用共享 `_NO_THINK` 常量（16 passed） | 证据：kw-cardfix monkeypatch 实测（1 调用）+ 主线程 mid 三 kind 真网关探针（3 调用，fr=stop/rt=0）+ test_llm_client.py 16 passed + ruff 干净 | 沉淀机制：①**LLM 调用契约有半衰期**——provider 网关会加校验，任何 extra_body 修复在复用前必须先跑一次最小真网关探针（r19 的 extract 修法若直接沿用，摄取链路会在下一次突发窗全灭且报错形态是 400 而非空正文，更难诊断）；②extra_body 的**成对参数**（thinking 与 reasoning_effort）必须整体进出，拆开发送即非法；③子智能体在独占文件边界外发现缺陷时**停手报告 + monkeypatch 实测修法**是正确的协作形态（零越权 + 零猜测） |
| 61 | 2026-09-22 | 产品(决策卡语义·交付审计) | 现象：T-26 复验 card5（`53580256`，糖尿病前期/v1.1.0/as_of=2026-01-01）→ **INSUFFICIENT_EVIDENCE**，与 T-23 交付口径卡 `9189abf8` 同工作点的 RECOMMEND（1 候选/score=0.9）冲突——kw-cardfix 按"禁止回归"纪律停手报告；主线程 SQL 直读两卡 payload 定案：**`9189abf8` 的候选 option 是字面定义陈述"糖尿病前期包括IFG、IGT或二者兼有"**（score=0.9），其 3 条证据链全是 EXTRACTED 定义边（糖尿病前期包括IGT/IFG/二者兼有）→ 旧"RECOMMEND"是 **render LLM 把分类定义包装成治疗推荐**（语义缺陷），card5 的"定义性陈述不能生成候选推荐"拒绝才是语义正确 | 定位：`decision_card_t.payload->candidates[0].option` 与 `evidence_chain[].claim` 直读比对（证据面两卡一致、均 definition-only，`uncertainty_notes` 明写"仅命中定义性陈述…未提供干预或管理相关的证据路径"）；判定翻转机制 = render LLM 对同一证据面的采样不稳定（旧采样过度声称、新采样诚实拒绝） | 根因：①**交付锚点建立时只审了 decision 字段与计数**（RECOMMEND/1 候选/3 证据链"看起来合格"），未逐条审候选 option 语义——把"某病包括某亚型"当"推荐"展示给用户是临床误导；②render LLM 单次采样被当成可复现的"工作点"，而它本来就是 flaky 的 | 修复：✅ 裁定（r24 主线程）——**card5 拒绝为新基线**；`9189abf8` 口径作废（T-26 简报"2026-01-01→RECOMMEND 禁止回归"判词撤销）；版本对比载体换实体（"糖尿病前期"邻域两时钟均 definition-only，两卡必双拒绝=对比不成立；kw-qa 槽位先跑零 LLM walk 探针选真实分化实体，候选：糖化血红蛋白/2型糖尿病——图谱携带 valid_at=2021-04 与 2025-01 两批事实边） | 证据：两卡 payload SQL 直读（候选字符串+证据链 claim 逐条）+ kw-cardfix 零 LLM store walk（36 边三时点单调）| 沉淀机制：①**交付卡合格性审计必须逐条审候选 option 语义**（是否真是可执行推荐而非定义/分类陈述），decision 字段+计数不构成合格性；②"禁止回归"锚点在建立时就要审语义——否则锚住的是缺陷本身；③render LLM 判定 flaky 是常态，跨日比对"工作点行为"前先用结构化字段（claim 类型/证据通道/pinned）预筛，单次采样不得当契约 |
| 62 | 2026-09-22 | 评测(实验方法学·配对复跑结论) | 现象：E8 配对复跑（e8-paired2，pid 640634，02:53-03:05:30）**干净完工**（`same_invocation=true`、日志 `llm_empty_content=0`/`runner_error=0`/`429=0`/`200 OK=34`）→ **两臂 V 题均 acc=1.0（n=6/6）→ `delta_acc_on_minus_off=0.0`**；这与 r22 并置结论 `Δ_V=+0.3334`（on 0.6667 vs off 0.3333，方向支持"版本钉住有益"）**方向相悖** | 定位：r22 的 +0.3334 是**跨 invocation 并置**（`same_invocation=false`）+ **空正文污染**（#59：on 臂 2×runner_error + off 臂 3×runner_error 被 count_fail 计入分母）双重缺陷下的产物；修复 #59/#60 后配对重跑，两臂都满分 | 根因：**污染会伪造方向**——空正文 runner_error 按 count_fail 计入分母，会让"答得慢/空正文更多"的那一臂 acc 被系统性拉低；而小样本（n=6）+ 两臂满分意味着这 3 个 V 题对两臂都"证据充分且易答"，Δ=0 测量不到版本钉住差异 | 修复：判读结论（r25 主线程）——**e2-ablation-paired2.json 的 Δ=0.0 取代 r22 并置 +0.3334**；cost-ledger/evidence-index 口径更新为"V 题 3 题×2runs 配对复跑两臂满分、Δ=0、无法据此宣称版本钉住有益或有害"；要暴露钉住差异需用**判别题**（旧时钟 0 证据 vs 新时钟 N 证据），kw-qa 零 LLM walk 探针已定位（糖化血红蛋白 t2021 0 边 vs t2025 3 边） | 证据：e2-ablation-paired2.json e8 块（same_invocation=true / per_type.V 两臂 acc=1.0 / delta=0.0）+ e8-paired2.log 零污染计数 | 沉淀机制：①**并置（same_invocation=false）结论一律不得进正式口径**——即便方向"看起来合理"，也必须等配对复跑；②配对复跑读 Δ 前先核 runner_error/空正文计数≈0（污染会伪造方向）；③**Δ=0 + 两臂满分**要如实披露 n 与"两臂都易答"的限制，不得反推"机制无效"；④要验证机制效果，测试集必须有**判别题**（机制能改变答案的题），否则 Δ 恒 0 是测试集选择问题不是机制问题 |
| 63 | 2026-09-22 | 评测(判别题集·#62④闭环) | 现象：#62 判明"测试集无判别题 → E8 恒 Δ=0"，但判别题此前不存在，机制验证被卡 | 定位：判别实体不能人工点名——用零 LLM 双时钟 store walk 探针（t_old=2021-06-01 / t_new=2025-06-01，构建租户 6756b0ab）对 136 实体逐个直查 PG，客观筛选"旧时钟 0 有效证据边 且 新时钟 ≥1"→ **31 个判别实体**；糖化血红蛋白 old=0/new=3 复现 kw-qa r25 与决策卡双卡佐证（9ed40a72 INSUFFICIENT vs 109425c1 RECOMMEND） | 根因：testset-v1-seed 20 题对两臂都易答是测试集选择问题；判别题必须先建后跑消融 | 修复：T-28 落地（提交 10cecd27b）——`competition/corpus/testset-discriminating-v1.json` 8 题/7 实体，**同题双金标**（旧臂=诚实拒绝，新臂=guide-2024 证据支撑，valid_at=2025-01-01）+ `discriminating:true`/`probe_evidence`（两时钟边数）随题入库，hash=d59034f245061ffc；`eval_v1` 加性 `--testset` 参数（默认 seed 路径逐字符不变）+ `validate_testset_file`；11 新测试，pytest **711 passed/30 skipped** 零回归，ruff 零新增，全程 LLM=0 | 证据：probe-results.json（.task_b_status/t28-discrim，不进 git）+ CLI 双 validate 输出（seed VALID 20/hash fd56571a304fb09d、判别集 VALID 8/hash d59034f245061ffc）+ HEAD before/after 对照 | 沉淀机制：①机制类实验**先建判别题再跑消融**——无判别题的 Δ=0 不得解读为机制无效（#62④的落地闭环）；②判别实体由零 LLM 探针按"旧 0 边/新 ≥1 边"客观筛选，不人工点名；③探针证据（两时钟边数）随题入库可复核 |
| 64 | 2026-09-23 | 部署(sandbox/minio) | 现象：chat 页 3 轮提问全部只回「Agent execution failed」，**前端界面无任何可诊断信息**；后端 SSE 原始流里的真错误是 `Run Agent Error: Error in interaction: Failed to grant sandbox output access: chgrp: cannot access '/home/qianqian/nexent-data/workdir/<uuid>/<hash>': No such file or directory` | 根因：**后端跑在宿主机、中间件跑在 docker** 这一拓扑——`NEXENT_SANDBOX_DEFAULT_LEVEL=docker` 使沙箱产物回传链路去连 docker 网络内部 DNS 名 `nexent-minio:9000`，宿主机解析不了 → `EndpointConnectionError` → 沙箱产物授权失败 → 被逐层包装成 `MemoryPreparationException`，最终只剩一句笼统的「Agent execution failed」（**错误信息在传输链路上被抽象掉了**，这是本坑耗时的主因） | 修复：✅ 选最低侵入度方案（**零仓内文件改动**）——`NEXENT_SANDBOX_DEFAULT_LEVEL=local` + `MINIO_ENDPOINT=http://127.0.0.1:9010`（`nexent-minio` 容器映射 `0.0.0.0:9010->9000`）+ 后端重启；3 轮问答随即全绿（`errors: []`，MCP 侧 71×`CallToolRequest`） | 沉淀机制：①**宿主进程 + docker 中间件**拓扑下，任何指向容器名的 endpoint 都必须换成宿主可达的 `127.0.0.1:<映射端口>`；排查顺序固定为「先 `docker ps` 看端口映射，再动代码」；②「Agent execution failed」这类无信息错误**必须读 SSE 原始流**（curl 直打 `/api/agent/run`）才能拿到真错，在前端界面上反复重试是纯浪费；③沙箱级别是部署共享状态，改动需主会话放行并记录环境快照（`~/t27-chatqa/host.env`） |
| 65 | 2026-09-23 | 接线(MCP 租户·平台行为) | 现象：`knowevo_mcp` 服务端日志反复 `request-scoped tenant unavailable: no Authorization header (headers present: ['accept','accept-encoding','connection','content-length','content-type','host','user-agent'])`（61 行含 tenant）——新写的「按调用方 JWT 解析租户」逻辑**一次都没生效** | 定位：MCP 服务被平台**进程级挂载一次**、服务所有租户，设计上只能靠调用方 JWT 区分租户（`tool_collection/mcp/nl2agent_mcp_tools.py` 同款模式）；但实测本部署形态下 Nexent v2.5.1 内置的本地 MCP 服务**根本不转发 `Authorization` 头**（上列 7 个头里没有它；`create_agent_info.py` 注册时 `authorization_token: None`） | 根因：**「按请求解析租户」的前提（调用方带头）在本部署形态下不成立**——代码写的是前向兼容路径，实际生效的必须是**服务级锚定**；若只有前者，租户恒为空 → 图谱查询查不到数据且**不报错**（静默降级），表现成「答案没依据」而非失败，极难归因 | 修复：✅ 已落地——`mcp_servers/knowevo_mcp/server.py` 保留 `_request_tenant()`（独立 FastMCP 部署 / stdio 场景仍可用，异常降级 `''` 不抛错），优先级定为 显式参数 > 请求头 > 服务级默认；`backend/tool_collection/mcp/kg_tools.py` 的 `wire()` 增 `KW_MCP_TENANT_ID` 服务级锚定（本部署形态下**真正生效**的机制）。日志只记**头名**、永不记 token 值 | 沉淀机制：①**租户解析失败必须留日志**——否则查询静默查空，表现为「答案没依据」而非报错，归因成本极高；降级路径只记头名不记 token；②对接「共享服务 + per-call 身份」的设计，**上线前先抓一次真实请求头**确认身份载体确实存在，别把设计意图当既成事实；③对外表述严格区分「请求级多租户已工作」与「服务级锚定在工作」——本次是后者 |
| 66 | 2026-09-23 | 评测(实验方法学·**口径单位错配**) | 现象：T-21 标准对齐器的 P/R 长期挂着两套数字、都没定死——item（段落级）口径 `precision=0.00145 / recall=0.111`，话题级口径 `recall 9/9`。前者看起来像「算法几乎全错」，而对外母本 `05` §4.1 当时**只写目标 P≥0.90/R≥0.85 不写实测** → 评委一追问「实际做到多少」，唯一可查的实测数就是 `0.00145`，比自己的目标低三个数量级 | 定位：`probe_p5_alignment_caliber.py`（零 LLM）对既有产物回溯重算，**算出 item 级的结构性天花板 = `9/691 ≈ 1.3025%`**——分母是 691 条**段落级**机器变更项，金标是 9 条**话题级**人审条目，**两者不是同一个单位**。即：**检测器就算完美，precision 也只有 1.3%**；实测 loose `1/691` 甚至还没碰到天花板（`achieved/ceiling = 0`）。话题级口径同单位比较 → `recall 9/9`、precision **下界** `64/517≈0.124`（产物字段名即 `precision_lower_bound`），负向对照 14 个无关话题命中 0 | 根因：**评测口径的比较单位必须与被评对象/金标的单位一致**；用「细粒度机器产出」除以「粗粒度金标」会得到一个**没有语义的天文数字级低分**，而且它随粒度比例变化、与算法质量无关。该上界其实早在归档材料里被记录过（`mission.md:146`），但**只停留在内部笔记、没进对外文档，也没写进口径定义**，于是这个数字持续以「首测质量」的面目出现在索引与计划书里 | 修复：✅ 已定死（2026-09-23，P1-6）——**对外唯一口径 = 话题级**；`0.00145/0.111` 作为**口径演进基线并置**，且必须带「结构性天花板 9/691≈1.3%、不可作质量度量」的标注；三条限制一并披露（n=9 不得反推 precision 高 / 下界≠precision / 阈值敏感：min_shared 2→3 时 recall 9/9→3/9）；`05:155` 原文「只写目标」改为「实测 + 限制 + 目标明确标注未达成」；决定书 `verification-reports/t21-alignment-caliber.md`。证据：探针实跑与 `alignment-diff.json` 的 `calibration`/`topic_calibration` 两块**逐位吻合**；另与归档独立记录（上界≈1.3%、df≤0.02→7/9、min_shared=3→3/9）交叉一致 | 沉淀机制：①**报 P/R 前先问「分子分母同不同单位」**，不同就先算**结构性天花板**——天花板远低于人直觉时，该数字不是质量指标；②金标口径与被评产出粒度不一致时，**口径选择本身就是结论的一部分，必须写进对外文档**，不能只留在内部笔记；③「目标值」与「实测值」在对外材料里**必须同时出现或都不出现**——只写目标会被追问，且一旦被追问，任何历史低分数字都会以最不利的方式被引用 |
| 67 | 2026-09-23 | 部署(本地代理·**假故障**) | 现象：模型网关探针连续失败，两次都是 `APIConnectionError: Connection error.`，每次约 54s；随后手工诊断 `curl https://api.sensenova.cn` 得到 `connect=0.000118s tls=0.000000s total=17.85s`（**TCP「连上」耗时接近 0、TLS 从未完成**）→ 一度判定为「网关侧降级、外部阻塞」，据此**搁置了整条 P1-5 图谱摄取** | 定位：`env \| grep -i proxy` 发现本机导出了整套代理变量——`HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` / `ZCODE_HTTP_PROXY` **全部指向 `http://127.0.0.1:7897`**；`curl --noproxy '*'` 同一条请求立刻 `code=404 connect=0.162s tls=0.224s total=0.269s`（**0.27s 完成**，404 只是路径不存在，说明 TLS+HTTP 全通）。随即把探针的代理摘掉重跑：`healthy=True`，两次调用 1.3s / 2.0s、token 正常回传 | 根因：**本地代理进程对目标域名拦截并挂起 TLS 握手**（TCP 被本地代理瞬间 accept → 所以 `connect` 时间趋近 0，这个「好得反常」的 connect 时间本身就是代理在场的指纹）。而**真实调用端点其实是 `token.sensenova.cn`**（不是我当时拿模型配置猜的 `api.sensenova.cn`），所以「curl 某个域名超时」并不能代表网关状态 | 修复：✅ 已落地（2026-09-23）——①`~/kw-ingest/run_batch_A.sh`（启动器）在起进程前显式 `unset` 全部代理变量；②`~/kw-ingest/probe_gateway.py` 在构造任何 HTTP 客户端**之前**自行摘掉代理（就地自带，避免每个调用点重复）；**不改任何全局设置、不入仓**。修复后摄取真跑：首个 span `call ok kind=extract 3.9s in=5007 out=646 fr=stop`，**0 次 429**（远好于 r20-burst 的 429 风暴） | 沉淀机制：①**「外部服务宕机」是最后一种可能，不是第一种**——先查本机 `env \| grep -i proxy`，再查 DNS/TCP/TLS **分层**差异：本文的判据是「TCP 瞬连 + TLS 永挂」= **中间有人在代答 TCP**（代理/中间盒）；②`connect` 时间**异常地小**（μs 级）是本地代理在场的强指纹，别当成「网络很好」；③**探针与被测目标必须是同一个端点**——拿 `api.sensenova.cn` 的失败去推断 `token.sensenova.cn` 不可用是错的推断链，端点名要从运行时日志（`HTTP Request: POST https://token.sensenova.cn/...`）或配置真值取，不要猜；④「探针不健康」的处置应是**先修复诊断手段本身**（本项目：探针脚本自带摘代理），否则每一轮监督任务都会重复得出「网关不可达」并无限搁置摄取 |
| 68 | 2026-09-23 | AI协作(子智能体调度·限流风暴) | 现象：4 个并行子智能体开跑约 5 分钟起批量阵亡——P2 截图审查、P1-4b τ_a（**两次**）、visual-judge 视觉验收、P1-5 摄取共 **5 次失败**，报错全是 `tpm exhausted` / `rps exhausted`（API 限流），存活 283-927s；对失败任务原样重拉继续死（P1-4b 第二次 8 分钟即挂） | 定位：5 条失败通知时间线对齐——视觉大负载并发是主挤压源（visual-judge 连读 25 页渲染图 + P2 连读 16 张截图同时进行）；P1-5 智能体虽被杀但其 nohup 摄取进程**不受影响**照常跑完 batch 12/12 | 根因：多子智能体并发 + 视觉批量读图在分钟级窗口击穿 TPM/RPS 配额；对失败任务**同频重拉**只会再撞同一面墙 | 修复：①错峰重试（同一时刻至多重拉 1 个）；②简报内置「防限流节奏」条款（每读 2-4 页图先落盘判词、`sleep 540 && tail` 长轮询取代高频短查、输出精炼）；③同一任务两轮仍被限流则**收回主线程自做**（τ_a 即此例，收回后一次跑通）；④长跑进程 nohup 与智能体解耦 | 证据：5 次失败通知（tpm×3 / rps×2）+ 重试后 P1-5 摄取照常收口 + τ_a 收回后 probe_p6 一次通过 | 沉淀机制：①子智能体简报模板固定带「防限流节奏」条款；②重试策略=**错峰+降频+回收，不加并发**；③长跑活（摄取/评测）一律 nohup 与智能体解耦，用「进程存活」而非「智能体存活」判活；④限流失败与逻辑失败分开记账——前者不该消耗重试预算上限 |
| 69 | 2026-09-23 | 部署/核查(命令行环境坑三连) | 现象：本轮数据核查命令连续三次翻车：①`ls path/probe_p6*` 在 zsh 下整条复合命令中断（`no matches found`，连后面的 psql 都没执行）；②宿主机 `psql` 不存在（`command not found`）；③JSONB 导出 SQL 报 `operator does not exist: text ->> unknown` 与 `invalid input syntax for type json: Token "CLS" is invalid` | 定位：①zsh 默认 nomatch——空 glob 不是「无匹配」而是**抛错中断整条复合命令**（bash 只是 ls 报错继续）；②历史会话 DB 核查一直走 `docker exec nexent-postgresql psql`（坑 #13 早有记录），宿主机从未装 psql；③PG 的 `||` 与 `->>` 同为左结合：`a->>'x'||b->>'y'` 被解析成 `((a->>'x')||b)->>'y'`，拼出的文本 `CLS:...` 再被隐式转 json 即报 Token CLS | 根因：三个都是「默认环境与直觉不一致」：zsh 的 nomatch 语义、psql 只在容器里、JSONB 操作符优先级陷阱 | 修复：①复合命令里用 `find -name` 查存在性（不 glob）或 `|| true` 兜底；②DB 核查固定 `docker exec nexent-postgresql psql -U root -d nexent -Atc "..."`；③JSONB 拼接**逐项加括号**：`(op->>'op')||'|'||(op->>'target')` | 证据：三次翻车的报错原文 + 修正后同查询一次通过 | 沉淀机制：核查命令模板进任务简报（本行即模板出处）；JSONB 表达式一律全括号；zsh 下 glob 只用于确定存在的路径 |
| 70 | 2026-09-23 | 图谱/前端（G6 树静默空白） | 现象：本体树面板交付截图全空（零节点零边），无报错无 toast，tsc/next build 全绿 | 根因：`classesToTree` 产出嵌套树对象直接喂 G6 v5 `Graph`——`data` 只收 `{nodes,edges,combos}`、`NodeData.children` 是 ID 数组，嵌套树被当成空 GraphData，mindmap 无根可排→静默不画；`as never` 断言让类型检查闭眼通过，掩盖了传错形状（`OntologyTreePanel.tsx:23-44,79-111`） | 修复：经官方 `treeToGraphData()` 转 `{nodes,edges}` 再传，删 `as never`（tsc/prettier/next build 三关实测过） | 沉淀机制：G6 数据一律走 GraphData 契约/`treeToGraphData`；**禁止 `as never`/`as any` 绕过第三方库类型**——它把「传错形状」变成「静默空渲染」，连报错都没有 |
| 71 | 2026-09-23 | 评测/证据学（错误态吞成空态） | 现象：提案队列截图显示「队列为空，等待新提案」，审图时无法区分「真为空」还是「API 报错被吞」——截图作为证据不能自证数据状态 | 根因：`ProposalQueue.tsx:67-75` / `OntologyTreePanel.tsx:68-69` catch 只弹 toast 后落入 Empty 分支；`graph.render()` 异步失败是静默 unhandled rejection | 修复：错误态（Alert+重试）与真空态（Empty）二分渲染；`setData(null)` 顺带废掉过期 A/X/P 键盘流；`render().catch` 落 Alert | 沉淀机制：所有「空列表/空面板」UI 必须把**加载失败**与**为空**分开渲染，否则截图证据不可自证；`DiffAndQualityPanel.tsx:43,51` 同款问题已登记待修 |
| 72 | 2026-09-23 | 评测/取证（读图通道陈旧图粘滞） | 现象：批量审图时读合成控制图（900×300 红底 `OMEGA-42-CTRL`）返回的却是另一张 1440×980 聊天截图内容，2/2 控制实验失败（物理上不可能）；本地 OCR 降级通道（vision 技能/tesseract）均不可用 | 根因：多模态读图通道「陈旧图粘滞」缺陷——返回上一张图的观察而非当前图 | 修复：两段式取证：画面文字取自首轮视觉观察；**文件名↔画面绑定**用像素级程序化证据独立重建（标题条补丁 RMSE=0.00/44.37 判同异、问题气泡浅蓝底定位、侧栏补丁对比），并与 `decision_card_t` 库行时间戳、文件 mtime 时序互证 | 沉淀机制：读图先跑合成控制实验（纯色+随机字符串图）防幻觉；绑定存疑时用像素补丁 RMSE/哈希交叉验证，宁标「不确定」绝不编造图中文字 |
| 73 | 2026-09-23 | 抽取/前端（wire 渲染字面 `None · None`） | 现象：决策卡证据行来源列渲染字面 `None · None`（截图审查 §10/§12 实锤），对外提交易被评委追问 | 根因：三层——①后端 `str(prov.get("doc",""))`：LLM JSON `"doc": null` 时 `dict.get` 的 default 不生效，`str(None)`→字面 `"None"` 写进 wire；②卡片 prompt 输入从不给 doc/span 而模板要求 provenance{doc,span}，模型只能回 null（LLM 往返必丢）；③真实来源在 `kg_evidence_t`(doc_id,span_loc)→`doc_asset_t.title`，全仓无人解析 | 修复：后端 `_evidence_sources` 沿 `props.evidence_id` 正向解析真实来源 + `_backfill_provenance` 按 claim 回填 LLM 重建条目（模型没见过的字段回显不作数）+ `_clean_str`（None/null→空）；前端判别联合 `evidenceSourceDisplay`（fields/graph/missing）缺省渲染「图谱内证据」/「—」并兜底历史脏数据；实测 721 passed / 30 skipped | 沉淀机制：①JSON 往返可选字段**禁止 `str(x.get(k,""))` 直转**（null 不吃 default），显式判 None 或过清洗函数；②喂给 LLM 重建结构化输出的字段，装配期真实值必须留存回填；③展示层对历史脏数据留兜底 |
| 74 | 2026-09-24 | 核查/命令行环境（head 截断数字失真） | 现象：T5 金标扩容两次抓到「终端输出被 head 截断导致数字失真」——①章名跨行 grep 截断：pdftotext -layout 正文标题跨行致 grep 命中行不含完整章名，初判「2024 第六章章名扩展（新增『和高血糖的治疗路径』）」经 2020 目录区 L43-44 复核为误判，该条撤销不入册；②戒烟 2024 命中数初记 6（`head -6` 截断），复核 `grep -c` 实为 10，分册按 10 修正并补「2020 第九章=戒烟 → 2024 第九章=药物治疗」章号前移佐证 | 根因：`head` 只取前 N 行命中，其行数被直接当成全量计数引用；数据核查默认用 head 抽样却忘了它不提供计数语义（与 #69 zsh nomatch 同属「命令行环境默认值坑」家族） | 修复：两处均在「写进文件前复核终端输出」纪律下纠正（详见 `nexent/competition/corpus/guideline_diff_seed_batch2.md` 头部与 receipt T5 §2）；未造成入库数字失真 | 沉淀机制：①计数类断言一律 `grep -c` 先行，`head`/`grep -n` 输出仅用于抽样定位；②任何引用终端输出作数字的场合必须二次复核后才落盘；来源=overnight-T5（receipts/T5-gold-expansion.md §6），05:30 收尾运行裁决入账 |
| 80 | 2026-09-24 | 交付/文档口径（对外数字跨文档互斥） | 现象：同一「PG 集成测试通过数」在多处文档口径互斥——`reproduce-README.md` 写 **551 passed**、`dev-design-doc.md` 写 **711 passed**、`evidence-index.md` 写 **718**、而 2026-09-24 凌晨实测为 **721 passed / 30 skipped**（`archive/overnight-2026-09-24/receipts/T1-pytest-raw.txt` 原文：721 passed, 30 skipped, 1 warning in 50.88s） | 根因：同一指标由不同会话/时点在多份文档各自写入，缺单一权威源仲裁；数字随测试集增长（551→711→718→721）但未回写旧文档 | 修复：以 `T1-pytest-raw.txt` 为权威源，回填 `reproduce-README.md`（551→721 passed/30 skipped）与 `dev-design-doc.md`（711→721 passed/30 skipped）；`evidence-index.md` 的 718 待任务Q1/Q2B 复核后对齐 | 沉淀机制：任何对外数字在改一处后须 `grep -rn` 全仓核对一致，单一权威源（原始 receipt / cost-ledger 行）优先；跨文档口径冲突一律先定位权威源再回填，不得留多版本并存 |
| 105 | 2026-09-24 | 核查/赛事规则（报名截止误当材料提交截止） | 现象：团队多份文档（含内部 official-site-2026-09-23.md L92）把 2026-11-30 当作"初赛材料提交截止" | 定位：`official-site-2026-09-23.md` L92 已自标 ⚠️待确认；WebSearch 外部多源（华为官方微博帖 `compId=85132021` + `enrollmentDeadline=2026-11-30 23:59:59`、宁夏理工/西华大学/黑龙江工商学院/北京工业大学通知）一致写"报名截止 2026-11-30，初赛 2026年12月" | 根因：将 `enrollmentDeadline`（报名截止）与初赛材料提交节点混淆 | 修复：2026-09-24 外部核验确认 11-30 = 报名截止；初赛材料提交在其后（约 2026-12），官网未给具体日 | 沉淀机制：收口/倒计时基准**不得锚定 11-30**；材料提交截止日 `insufficient_data`，待用户在报名系统确认；对外引用赛事时间一律附 URL + 访问日期 |
| 106 | 2026-09-24 | 核查/赛事规则（赛程"全球总决赛"口径冲突） | 现象：多所高校通稿称"创新赛三级赛制含全球总决赛"，但本项目原内部口径曾提过全球总决赛叙事 | 定位：官网赛题3详情页（compId=85132021）明确"赛题3 的赛程为'初赛-中国总决赛'"（内部报告 §一 L18）；高校通稿为创新赛**通用**口径，未区分赛题3 特例 | 根因：引用赛道级通用通稿代替赛题级原文 | 修复：以官网赛题3详情页为准，对外叙事勿提"全球总决赛" | 沉淀机制：引用赛程/赛制前先核对**赛题级**官网原文而非赛道通用通稿；口径冲突时括号内标注"以官网赛题3详情页为准" |
| 75 | 2026-09-24 | 评测/终点选择（效率实验） | 现象：把"单条人工分钟 0.693 vs 0.630"当作门因子的效率增益终点 | 定位：`probe_p7_efficiency_minutes.json` cells：门开 14.55/21=0.693、门关 6.30/10=0.630；但 α=0.75/β=0.63 是固定常数，门不改变单条成本 | 根因：两臂审的**提案组合不同**（门开多审 11 条更贵的 CLS_ADD），单条均值之差是 composition 伪影，不是 treatment effect | 修复：主终点改取「人审项数 21→10 / 分钟 14.55→6.30」的门因子工作量削减（2.1×/2.31×）；单条均值排除出终点 | 沉淀机制：终点若用 per-item 均值，必须先验证差值来自**处理效应**而非**条目组合差异**，否则是伪影 |
| 76 | 2026-09-24 | 图谱/同名概念陷阱 | 现象：`Quality(·).cov` 的 `seed_terms` 在冻结池恒为 0.0，误以为缺字段 | 定位：`ontology_service.py:754,990-1035` 的 `cov` 需**调用方注入** `seed_terms`；生产代码从未传入（仅 `test_ontology_service.py:389` 单测传过）；与 `backend/services/knowevo/seed_terms.py` 的「问题驱动检索种子词」**是两回事** | 根因：跨模块同名概念（cov 的种子词汇 vs T-26 检索种子词）被混为一谈 | 修复：明确 `seed_terms` 来源=种子骨架 S2 输出（每章 top-K 术语），持久化未接线（C-3）；导出脚本改为外部 manifest 注入 | 沉淀机制：同名但语义不同的参数/模块必须显式区分来源链路，不得默认同源 |
| 77 | 2026-09-24 | 评测/审计攻击识别 | 现象：敌意审计用"98–392 接受事件/档 vs δ≈0.10 需 1568/臂"制造"差 4 倍"质疑 | 定位：预登记 98–392 是**排序臂 per-accept-minute 终点**的规划数（假设 δ=0.20–0.40），0.693/0.630 是**门因子两臂的提案组合均值**，二者不同设计不同终点 | 根因：把不同设计/终点的数字强行等比，立稻草人 | 修复：预登记须逐条声明每个 power 数字对应的**具体终点 + 假设 δ**；门因子是确定性结构效应，不进 power | 沉淀机制：任何 power 数字必须标注「对应哪个终点、δ 取值来源」，避免被等比误读 |
| 78 | 2026-09-24 | 评测/n 估算口径 | 现象：用「图谱实体 325」直接当作「本体候选提案数」推算可达 n | 定位：325 实体是 `kg_entity_t` 抽取实体；候选提案来自 `build_ontology_round` 在已摄取图谱上跑出的队列（量级 10²–10³/轮），冻结 21 提案则来自种子 schema（`applied_ops`）而非摄取段 | 根因：混淆「图谱实体数」与「本体候选提案数」两个口径 | 修复：n 估算显式区分；扩池靠摄取补全 + 跑 build_ontology_round，不是简单乘实体数 | 沉淀机制：样本量估算必须先定「分析单位对应的真实供给源」，再谈折算系数 |
| 79 | 2026-09-24 | 图谱/扩池前提 | 现象：以为"语料不够 58 份"是扩池到 98–392 的瓶颈 | 定位：`p15-ingest-progress.json` ceiling_analysis：可达段上限 88/120（G3 放宽 ~114）；ev_rich 过门率 0/21 是冻结池**单文档证据**特征 | 根因：真瓶颈是 C-4（分数连续化）与 C-5（ev_rich 过门率>0，需跨文档链接），不是文档数量 | 修复：Phase 1+ 先补摄取 + 两级提案回流（`mention_count≥3`）；仅 2 份种子指南补摄取到 88/114 段即可达 ≥300 候选 | 沉淀机制：扩池前先列「绑定约束清单」，区分语料规模约束与管线/门禁约束 |
| 107 | 2026-09-24 | 配置/命名错位（provider 名不存在 → 误报 Invalid API Key） | 现象：`Invalid API Key` 卡住 visual-judge 视觉验收与 E2 全量 LLM 侧**数轮**；团队在代码仓里 grep `api_key`、查 `model_record_t`、查 `consts/const.py` 都没定位到真因，最后结论写成"密钥本身无效 / 待用户在 ZCode 里配 key" | 定位：2026-09-24 用户提供编排工具配置截图，**实测配置的供应商名是 `sensenova1`**；而全仓 11+ 处（`NEXT-SESSION-PROMPTS` L20/L145/L150、`provider-key-plan` 5 处、`evidence-index`、`memory`）写的是 `sensenova2`，`grep -rn sensenova1` 全仓**命中 0 处** | 根因：**报错信息指向"密钥无效"，真因是"引用了不存在的 provider 名"**——同族于本仓已反复记录的"命名/接线错位不报真因"（坑 #31 双 `/api` 前缀、#40 导航接线漏项、#44 静默过滤、#48 大小写不对称、#52 `thinking` 只挂 extract 一个 kind）。此族共同特征：**错在名字/范围，报错却指向结果**，于是排查者一直在错误的对象上钻 | 修复：① `provider-key-plan-2026-09-24.md` 头部加更正块 + U4 行改为"sensenova1（不是 sensenova2）"；② 本文其余 `sensenova2` 保留为历史引用、不作现行依据；③ 会话提示词与任务简报统一以 `sensenova1` 为准 | 沉淀机制：①**报"认证/密钥无效"类错误时，第一步先核对"被引用的名字是否真实存在"**（provider 名 / 路由名 / 字段名 / kind），再去查凭证本身；②跨系统配置（平台 DB 配置 vs 编排工具配置 vs 环境变量）必须先**枚举实际存在的名字**（截图/列表/`SELECT DISTINCT`），不能用文档里的名字当事实；③凡文档里出现的外部系统标识符，标注"实测于何时、以何种方式确认" |
| 108 | 2026-09-24 | 环境/数据（两套 PG 并存 → 摄取会写错库） | 现象：任务Q2B 第 0 步探针发现库内数据量与文档严重不符——文档记「真库 35/120 段、实体 298、关系 207、证据 59」，而 `127.0.0.1:5434` 库里是 `kg_entity_t=70608` / `kg_relation_t=118400`，且 `doc_asset_t=0` / `kg_evidence_t=0` / `kg_pending_entity_t=0` | 定位：① `experiments/explain_valid_range.py` 全文只有 `CREATE INDEX IF NOT EXISTS`（一次 `commit()`）**不插不删任何行** → 数据是库里本来就有；② 租户分布显示多个租户各**恰好** `20000`/`3000` 条 = **合成基准数据特征**；③ `docker ps -a` 显示 **`nexent-postgresql` 容器已停止**（`Exited (0)`，5h 前），而坑 #69/#13 记载历史 DB 核查**一直走该容器**；④ 真实表名是 `kg_entity_t`/`kg_relation_t`，文档里的 `entity_t`/`relation_t` 与 `knowledge_segment_t` **均不存在** | 根因：**本机并存两套 PG**——跑着的 conda-forge `5434`（装合成基准数据，供写路径基准用）与已停止的 docker 容器 `nexent-postgresql`（真实图数据所在）。而 `NEXT-SESSION-PROMPTS` 的服务栈口径写"PG 用 `/home/qianqian/pg-env.sh` 的 `pg_start`（5434）"，与坑 #69 的"核查走 `docker exec nexent-postgresql`"**互相矛盾**——两条口径都没注明"这是哪套库、装什么数据" | 修复：**未修，已封堵**——单点收口时判定「B2 摄取 / B3 E2 在解决本条前一律不得开跑」（否则把真实摄取写进基准库，污染此后所有"真数字"），并把裁定要求与三步复核清单写入 `00-索引与状态.md` §八 任务Q2B 节 | 沉淀机制：①**多服务栈并存的环境，任何"连接数据库"的动作前必须先回答"连的是哪套、里面装什么数据"**（先查容器状态 + 抽样看行是否呈合成特征）；②文档里的连接方式（端口/容器名）必须标注**库身份**（用途 + 数据来源 + 是否可写），不能只写端口；③发现两处口径矛盾时**先停手报冲突**，不要挑一条就往下跑 |


