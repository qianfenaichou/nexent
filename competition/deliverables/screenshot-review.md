# 交付截图逐张审查（vision 审图）· Nexent-KnowEvo

> 执行者：P2 截图取证审查智能体（vision 逐张审图）。方法：Read 工具直接读原图（多模态看图），**只读不改原图**，不做任何 git 操作。
> 覆盖范围：`deliverables/` 下 16 张交付截图（evidence-index.md 缺口表第 6 行欠账）。
> 审查维度：①整体描述与图中文字数字 ②泄密排查（key/token/密码/内网 IP/主机名/姓名/手机/邮箱/租户 UUID）③与台账数字一致性 ④异常（模糊/截断/空态/报错/加载态）⑤页脚实见版本号。
> 诚实纪律：不确定即标「不确定」，不编造图中不存在的文字或数字。

---

## 1. T-01-agent-published.png（2026-09-14，1280×720，195 KB）

**整体**：Nexent 平台「智能体开发 → 智能体配置」页。左侧导航为完整业务菜单（首页/开始问答/自动任务/智能体开发〔模型配置、知识库配置、智能体配置、记忆配置、智能体评估〕/资源仓库/资源管理）。

**图中文字/数字逐条**：
- 页头：`选择智能体` / `请选择一个智能体进行编辑`；按钮 `新建` `导入` `版本管理`
- 中间弹层 `智能生成`：黄色提示条 `请先新建或选择可编辑的智能体。`；正文 `你好，我是智能体生成助手` / `请描述你希望如何配置当前智能体，我会搜索已安装资源并生成 Prompt 字段。`；输入框占位 `发送消息...`
- 右栏 `配置` + `智能生成` 按钮；空态 `请先选择一个智能体` / `请从上方选择一个智能体进行编辑，或创建新的智能体`
- 顶栏：`Github` `ModelEngine` `简体中文` 铃铛 `admin@knowevo.com` 头像
- 页脚：`Nexent © 2026 · v2.5.1` ｜ `使用条款` `联系我们` `关于我们`

**泄密排查**：邮箱 `admin@knowevo.com`（右上账号区，1 处）——演示租户邮箱，无真实姓名/手机号；**无** API key/token/密码串；**无**内网 IP（未见 127.0.0.1 / 192.168.*）；**无** `nexent-minio` 等主机名；**无**租户 UUID。

**一致性核对**：⚠️ **文件名与画面不符**。文件名为 `agent-published`（智能体已发布），但画面是「请先选择一个智能体进行编辑」的**空态**，**看不到任何已发布智能体、无智能体名称、无发布状态标识**。台账（evidence-index §一「平台跑通（智能体发布/对话/模型注册截图）」）把它当作「智能体发布」证据，**画面不能支撑该结论**。

**异常提示**：右栏为纯空态占位；中间为「智能生成」引导弹层（非业务数据）。**证据价值接近于零**——这是本批最严重的一张。

**页脚实见版本**：`v2.5.1`（与「页脚为 fork 部署 v2.5.1」的已知背景一致）。

**结论**：**必须重拍**。需重拍为「智能体列表中可见已发布智能体（含名称/发布状态）」或「某智能体的配置详情页」。

---

## 2. T-01-chat-conversation.png（2026-09-14，1280×720，232 KB）

**整体**：Nexent「开始问答」页，一次真实会话，展示推理轨迹（英文 thinking）折叠区 + 最终中文回答。

**图中文字/数字逐条**：
- 左栏 `+ 新对话`，会话列表 `新对话`，灰字 `已加载全部会话`；底部 `批量管理` `切换旧版`
- 推理轨迹（英文）：`This is a simple greeting with no eligible memory content - no user preferences, no task goals, no action plans, no reflections on feedback/errors. So I can skip store_memory.` / `I can answer directly without any code execution. 1+1 = 2, obviously.` / `Let me give me a friendly, concise answer in Chinese.你好！我是 Nexent 智能体——一个基于 MCP 工具生态的开源智能体平台助手，能为你提供多模态问答、检索、数据分析与处理等能力；至于 1+1，它等于 2 😊` / `有什么我可以帮你的吗？`
- 最终回答：`你好！我是 Nexent 智能体——一个基于 MCP 工具生态的开源智能体平台助手，能为你提供多模态问答、检索、数据分析与处理等能力；至于 1+1，它等于 2 😊` / `有什么我可以帮你的吗？`
- 元信息：`25.17s`、`13%`、`本轮`
- 输入区：`规划` / `执行` 切换、`发送消息...`、模型选择器 **`glm-5.3-free (小档)`**
- 页脚：`Nexent © 2026 · v2.5.1`

**泄密排查**：无 API key/token/密码；无内网 IP/主机名；无真实姓名/手机号；**无**邮箱（本图右上账号区未出现邮箱文字，仅头像图标）；无租户 UUID。

**一致性核对**：✅ 画面确为「真实平台对话跑通」（有耗时 25.17s、本轮 13% 等真实运行元信息）。⚠️ 但**问题内容是「1+1=2」的寒暄**，**未调用任何 KG 工具**（无 `kg_search` / `decision_card_render` 等轨迹），与 KnowEvo 业务无关；且模型为 `glm-5.3-free`，与 T-27 收口轮的 `deepseek-flash` **不同**（时点差异，非矛盾，但对外并列展示时需注意口径）。

**异常提示**：内容完整、无截断、无报错 toast。会话列表仅 1 条。

**页脚实见版本**：`v2.5.1`。

**结论**：**带限定可用**。只能作「平台对话链路跑通」的弱证据；对外若用作「示例问答」会被质疑与业务无关，建议以 T-27 三张为主、本张退居平台跑通附录。

---

## 3. T-01-models-registered.png（2026-09-14，1280×720，165 KB）

**整体**：Nexent「智能体开发 → 模型配置 → 模型设置」页，六个模型槽位卡片。

**图中文字/数字逐条**：
- 页头 `模型设置`；按钮 `+ 添加模型` `修改或删除模型` `检查模型连通性`
- `大语言模型`（红点必填）+ 紫标签 `自定义`：**`glm-5.3-free (小档)`** + **绿色圆点（连通正常）**
- `向量模型`：`选择模型`（**空**）；`多模态向量模型`：`选择模型`（**空**）
- `重排模型`：`选择模型`（**空**）
- `多模态模型`：`图片理解模型` 空、`图片生成模型` 空
- `语音模型`：`语音合成模型` 空、`语音识别模型` 空
- 页脚：`Nexent © 2026 · v2.5.1`

**泄密排查**：邮箱 `admin@knowevo.com`（右上，1 处）；无 API key/token（**未展开任何模型的密钥字段**，值得肯定）；无内网 IP/主机名（base_url 未展示）；无租户 UUID。

**一致性核对**：⚠️ **两处口径冲突**：
1. **向量模型槽位为空**——而台账 T-27 轮记载「EMBEDDING_ID 修复（bge-m3，guard2 生产 resolver 验证 RESOLVE OK）」且系统走 ES 混合检索。若截图时点确无向量模型，则本图与「向量检索已就绪」的对外叙述**不能并列**（时点差异需注明）。
2. 台账行标题写「**模型注册**截图」，画面实际**只注册 1 个 LLM**（glm-5.3-free），其余 5 类全空；且该 LLM 与 T-27 使用的 `deepseek-flash` 不一致。

**异常提示**：画面底部「图片生成模型」「语音识别模型」卡片被视口截断（可见标签但值区被裁）；页面右侧有滚动条，属正常长页滚动，非故障。

**页脚实见版本**：`v2.5.1`。

**结论**：**带限定可用**。仅能证明「平台模型注册功能可用（1 个 LLM 连通）」；对外须补注时点与「其余槽位为空」，不可表述为「多模型已注册」。

---

## 4. T23-home-loggedin.png（2026-09-20，1280×720，44 KB）

**整体**：Nexent 首页，登录态。**主内容区完全空白**，仅左导航 + 顶栏 + 页脚。

**图中文字/数字逐条**：
- 左导航**仅两项**：`首页`（高亮）、`资源管理`
- 顶栏：`Github` `ModelEngine` `简体中文` 铃铛 `suadmin@nexent.com` 头像
- 主区：**空白（无任何卡片/文字/图表）**；右侧有滚动条
- 页脚：`Nexent © 2026 · v2.5.1` ｜ `使用条款` `联系我们` `关于我们`

**泄密排查**：邮箱 `suadmin@nexent.com`（右上，1 处）——`nexent.com` 为平台官方域名，`suadmin` 为平台内置超级管理员**默认账号名**，非团队真实个人信息（低风险，但仍是对外可见账号标识）；无 API key/token/密码；无内网 IP/主机名；无租户 UUID。

**一致性核对**：⚠️ **同一工作区存在两套账号/两套导航**：本图为 `suadmin@nexent.com` 且导航仅 2 项；T-01 三张为 `admin@knowevo.com` 且导航 6 项（含智能体开发/资源仓库）。台账未说明这一差异——对外并列使用时会被追问「是否同一环境/是否权限被裁剪」。另：台账称本图为「登录态首页」，**画面主区空白**，无法体现首页内容。

**异常提示**：**主内容区空白**，疑为未加载完成或该账号权限下无首页内容——属 UI 状态缺陷（加载态/空态被拍入）。文件体积 44 KB（16 张中最小），与「空白」一致。

**页脚实见版本**：`v2.5.1`。

**结论**：**必须重拍**（或降级为「登录态证明」带限定可用）。作为「登录后首页」证据，画面无首页内容，说服力不足；若重拍需确保主区渲染完成、并统一账号口径。

---

## 5. t23-shot-ontology-workbench.png（2026-09-21 04:03，1440×900，67 KB）

**整体**：Nexent「资源仓库 → 本体工作台」页，停在 `本体树` 标签页。

**图中文字/数字逐条**：
- 页头 `本体工作台`；副标题 `提案确认（A/X/P）· 本体树 · 版本 diff · K0 质量面板`
- 标签：`提案队列` / **`本体树`（选中，蓝色下划线）** / `Diff 与质量`
- 徽标：`v1.1.0` + `当前已提交版本`
- 主区：**一个完全空白的圆角面板（无任何节点/边/文字）**
- 面板下方图例：`蓝框 = 锚定标准条目；灰 = 已废弃；拖拽/滚轮缩放`
- 左导航（完整）：首页 / 开始问答 / 自动任务 / 智能体开发 / 资源仓库〔Agent 仓库、MCP 仓库、Skill 仓库、**本体工作台（高亮）**、决策卡、技能模板〕/ 资源管理
- 左下角：**黑色圆形 `N` 徽标**（疑似 Next.js dev 模式指示器）
- 页脚：`Nexent © 2026 · v2.5.1` ｜ `使用条款` `联系我们` `关于我们`

**泄密排查**：邮箱 `admin@knowevo.com`（右上，1 处）；无 API key/token/密码；无内网 IP/主机名；无租户 UUID；无真实姓名。

**一致性核对**：⚠️ **严重不符**。台账 evidence-index §一称本图为「本体工作台截图（**本体树 + 导航**）」，§三记为「本体树视图」；**画面树区全空，零节点零边**——无法证明「本体树 ≥30 节点」这一 T-05 欠账（evidence-index §三「T-05 e2e 截图欠账（本体树 ≥30 节点 + diff 三色）」正是此项）。图中唯一可对账的台账数字是 `v1.1.0`，与决策卡/版本对比图的本体版本 `v1.1.0` **一致** ✅。

**异常提示**：**主视图空白**（渲染失败或数据未加载）；左下角 `N` 徽标暴露**开发态（next dev）**；图例存在但无内容可解释。

**页脚实见版本**：`v2.5.1`。

**结论**：**必须重拍**。需拍到真实本体树节点（≥30 节点）与导航同框。

---

## 6. t23-shot-ontology-queue.png（2026-09-21 04:03，1440×900，63 KB）

**整体**：同一「本体工作台」页，切到 `提案队列` 标签页。

**图中文字/数字逐条**：
- 页头/副标题同第 5 张；标签 `提案队列`（选中）/ `本体树` / `Diff 与质量`
- 主区：空态插画（文档 + 对话气泡图标）+ 居中灰字 **`队列为空，等待新提案`**
- 左导航同第 5 张（本体工作台高亮）；左下角同样有黑色 `N` 徽标
- 页脚：`Nexent © 2026 · v2.5.1`

**泄密排查**：邮箱 `admin@knowevo.com`（右上，1 处）；其余无（无 key/token/内网 IP/主机名/UUID/姓名）。

**一致性核对**：⚠️ 台账称本图为「本体工作台-待审队列截图」（维度2 ★★☆），但**队列为空**——**无法展示任何提案条目，更无法展示 A/X/P 三态**（副标题承诺的 `提案确认（A/X/P）` 在画面上完全不可见）。此图不能支撑「待审队列」的证据主张。图中**无任何数字**可与台账对账。

**异常提示**：**空列表**（`队列为空，等待新提案`）；左下 `N` 徽标暴露开发态。

**页脚实见版本**：`v2.5.1`。

**结论**：**必须重拍**。需在有真实待审提案时拍摄，体现条目列表 + A/X/P 状态。

---

## 7. t23-shot-skill-template.png（2026-09-21 04:03，1440×900，82 KB）

**整体**：Nexent「资源仓库 → 技能模板」页（技能模板库表格）。

**图中文字/数字逐条**：
- 页头 `技能模板库`；副标题 `从真实决策卡轨迹归纳的参数化 SKILL.md 模板（skill_template_t）：预览渲染前原文、复制实例化、复用统计诚实回写`
- 表头：`模板名` `任务类型` `领域` `版本` `复用次数` `成功率` `操作`
- 第 1 行：`refusal-general` ｜ `refusal`（红标签）｜ `general` ｜ `1.0.0` ｜ `0` ｜ `未记录` ｜ `预览` `复制`
- 第 2 行：`reasoning_decision-general` ｜ `reasoning_decision`（紫标签）｜ `general` ｜ `1.0.0` ｜ `2` ｜ `100%` ｜ `预览` `复制`
- 左导航同前（`技能模板` 高亮）
- **左下角红色徽标 `N 1 Issue`**（Next.js 开发态错误指示器，1 个 issue）
- 页脚：`Nexent © 2026 · v2.5.1` ｜ `使用条款` `联系我们` `关于我们`

**泄密排查**：邮箱 `admin@knowevo.com`（右上，1 处）；无 API key/token/密码；无内网 IP/主机名；无租户 UUID；无真实姓名。⚠️ **但暴露 `1 Issue` 开发态错误指示器**（非泄密，属对外观感瑕疵）。

**一致性核对**：✅ 与台账「模板复用统计 R/S/D（skill_template_t）（T-20 起真实落库）」方向一致：确有 2 条真实落库模板，且复用次数呈真实分布（`0` 与 `2`，**未伪造成好看的数**），`成功率` 对未达样本的模板诚实显示 `未记录` 而非 0。⚠️ 但台账所称 **R/S/D 三分态统计在画面中不存在**（只有 `复用次数` 与 `成功率` 两列），术语 R/S/D 未出现——若对外用「R/S/D 复用统计」表述，本图**不能作证**。图中**无 UUID** 可核。

**异常提示**：`N 1 Issue` 徽标；表格仅 2 行（数据量少，但真实）；无截断/报错 toast。

**页脚实见版本**：`v2.5.1`。

**结论**：**带限定可用**（需裁剪/遮挡左下 `N 1 Issue` 徽标；且不得用其支撑「R/S/D」表述，只能支撑「模板库真实落库 + 复用次数诚实回写」）。

---

## 8. T-23-decision-card.png（文件 mtime 2026-09-22 00:09 本地 = 2026-09-21 16:09 UTC，1440×900，127 KB）

**整体**：Nexent「资源仓库 → 决策卡」页，问题「糖尿病前期」生成完整决策卡，含证据链展开。

**图中文字/数字逐条**：
- 页头 `决策卡`；副标题 `输入问题，基于知识图谱证据链生成结构化决策卡（候选 / 证据 / 置信度 / 风险 / 冲突裁决）`
- 输入框：**`糖尿病前期`**；本体版本输入：**`v1.1.0`**；日期框占位 **`yyyy/mm/dd --:--`（未填）**
- 按钮：`完整卡` `精简卡` `生成决策卡`
- 说明：`钉住事实时钟（as_of）：仅移动事实自身的时间轴；未填知识版本时不产生任何版本约束。`
- 蓝色免责条：**`本系统提供认知辅助，不构成处方建议`**
- 结果徽章行：**`建议`** ｜ **`知识版本: v1.1.0`** ｜ **`截止 2025-01-01T00:00:00+00:00`** ｜ **`时钟 fact_cutoff`** ｜ **`version_pinned`** ｜ `校准 未应用`
- 卡标题 `糖尿病前期`；候选：**`糖尿病前期包括IFG、IGT或二者兼有`** + `Top-1` + **`置信度 0.80`**
- `证据链` **3** 条，逐条均为 `EXTRACTED` + `kg` + `版本钉住`：
 1. `糖尿病前期包括IFG、IGT或二者兼有` → `Disease:糖尿病前期 -> 糖尿病前期包括IFG、IGT或二者兼有 -> Disease:糖耐量异常`
 2. `糖尿病前期包括IGT` → `Disease:糖尿病前期 -> 糖尿病前期包括IGT -> Disease:糖耐量异常`
 3. `糖尿病前期包括IFG` → **（画面底部截断，路径不可见）**
- **左下角红色徽标 `N 2 Issues`**（Next.js 开发态，2 个 issue）
- 页脚：`Nexent © 2026 · v2.5.1`

**泄密排查**：邮箱 `admin@knowevo.com`（右上，1 处）；无 API key/token/密码；无内网 IP/主机名；**无租户 UUID**；无真实姓名/手机号。⚠️ 暴露 `2 Issues` 开发态徽标。

**一致性核对**：✅ **与台账逐条吻合度最高的一张**。evidence-index §一 列出的要素——`建议`、`知识版本:v1.1.0`、`截止 2025-01-01T00:00:00+00:00`、`时钟 fact_cutoff`、`version_pinned` 戳、Top-1 候选（置信度 **0.80**）、展开证据链（`EXTRACTED`/`版本钉住`/kg_path）、永久免责声明——**画面全部实见且数值逐字一致**。文件 mtime 本地 2026-09-22 00:09 = 台账 UTC `2026-09-21 16:09` ✅ 时点吻合。⚠️ 台账所称真库行 `decision_card_t.id=284ff0c1-a3bb-450c-8069-e1bb60512d4b` **画面中不出现**（属库侧核验证据，非画面义务，记为「画面不可核 UUID」）。⚠️ 日期框为空却仍显示 `时钟 fact_cutoff` / `截止 2025-01-01`，说明该次为**未显式指定 as_of 的默认时钟**，与糖化血红蛋白两张的 `explicit` 时钟**不同源**，对外叙述需区分。

**异常提示**：第 3 条证据链被视口**底部截断**；`N 2 Issues` 徽标；无报错 toast。

**页脚实见版本**：`v2.5.1`。

**结论**：**可用（需裁掉左下 `N 2 Issues` 徽标，并可选标注证据链截断）**。本批质量最高的业务截图。

---

## 9. T-23-version-compare-2024clock-refusal.png（2026-09-21 16:14 UTC，1440×900，125 KB）

**整体**：决策卡页，同题「糖尿病前期」+ 同本体 `v1.1.0`，`as_of = 2024/01/01 00:00`，结果为**版本钉住的诚实拒绝**。

**图中文字/数字逐条**：
- 输入：`糖尿病前期` ｜ 本体版本 `v1.1.0` ｜ 日期 **`2024/01/01 00:00`**
- 按钮：`清除` `完整卡` `精简卡` `生成决策卡`
- 结果徽章行：**`证据不足`**（黄标签）｜ `知识版本: v1.1.0` ｜ **`截止 2024-01-01T00:00:00+00:00`** ｜ **`时钟 explicit`** ｜ `version_pinned` ｜ `校准 未应用`
- 标题 `糖尿病前期`；黄色警示框 `证据不足` + 正文（逐句）：
 - `图谱仅提供糖尿病前期的分类定义（包括IFG、IGT或二者兼有），未提供诊断阈值、干预方案或管理建议等可形成候选选项的临床决策证据。`
 - `无文档命中段落，无法补充诊疗或管理层面的候选依据。`
 - `无失败路径，不生成反事实字段。`
 - `未找到校准表（eval_run_t.calibration 为空）：置信度为模型自报值，未经验校准。`
- **无候选卡片、无置信度数字**（0 候选）
- 页脚：`Nexent © 2026 · v2.5.1`

**泄密排查**：邮箱 `admin@knowevo.com`（右上，1 处）；无 API key/token/密码；无内网 IP/主机名；**无租户 UUID**；无真实姓名。✅ **本张无 `N Issues` 开发态徽标**。

**一致性核对**：✅ 与台账吻合：`as_of=2024-01-01` → `证据不足`、**0 候选**、`时钟 explicit`、截止 `2024-01-01T00:00:00+00:00`。⚠️ 台账所称真库 card `3fd88441` **画面不出现**（库侧证据）。⚠️ 与第 8 张对照：本张 `时钟 explicit`，而第 8 张为 `时钟 fact_cutoff` —— **确为两种时钟来源**，台账已分列，无矛盾，但对外并列时须注明差异。

**异常提示**：无截断、无报错 toast、无空态缺陷。正文中「。；」连用（`…决策证据。；无文档命中段落`）为**文案拼接瑕疵**（分号前多一个句号），非功能故障。

**页脚实见版本**：`v2.5.1`。

**结论**：**可用**（无泄密、无 dev 徽标；建议对外时留意文案「。；」小瑕疵）。

---

## 10. T-23-version-compare-2026.png（2026-09-21 16:14 UTC，1440×900，120 KB）

**整体**：决策卡页，同题「糖尿病前期」+ 同本体 `v1.1.0`，`as_of = 2026/01/01 00:00`，结果为**建议 + 证据链**（与第 9 张构成判别对）。

**图中文字/数字逐条**：
- 输入：`糖尿病前期` ｜ 本体版本 `v1.1.0` ｜ 日期 **`2026/01/01 00:00`**
- 结果徽章行：**`建议`** ｜ `知识版本: v1.1.0` ｜ **`截止 2026-01-01T00:00:00+00:00`** ｜ **`时钟 explicit`** ｜ `version_pinned` ｜ `校准 未应用`
- 标题 `糖尿病前期`；候选：**`糖尿病前期包括IFG、IGT或二者兼有`** + `Top-1` + **`置信度 0.85`**
- `证据链` **3** 条（可见 2 条 + 第 3 条被截断）：
 1. `EXTRACTED` `kg` `版本钉住` ｜ `糖尿病前期包括IGT` ｜ **`None · None`** ｜ `Disease:糖尿病前期 -> 糖尿病前期包括IGT -> Disease:糖耐量异常`
 2. `EXTRACTED` `kg` `版本钉住` ｜ `糖尿病前期包括IFG` ｜ **`None · None`** ｜ `Disease:糖尿病前期 -> 糖尿病前期包括IFG -> Disease:空腹血糖受损`
 3. **（画面底部截断）**
- 页脚：`Nexent © 2026 · v2.5.1`

**泄密排查**：邮箱 `admin@knowevo.com`（右上，1 处）；无 API key/token/密码；无内网 IP/主机名；**无租户 UUID**；无真实姓名。✅ 本张无 `N Issues` 徽标。

**一致性核对**：✅ 与台账吻合：`as_of=2026-01-01` → `建议`、**1 候选 + 证据链**、`时钟 explicit`、截止 `2026-01-01T00:00:00+00:00`。⚠️ 台账 card `9189abf8` 画面不出现。✅ **双时钟判别对成立**：与第 9 张**同题、同本体 v1.1.0、仅 as_of 不同**（2024→拒绝 / 2026→建议），符合台账「对比成立依据=一拒绝一建议」。⚠️ **置信度口径提醒**：本张 `0.85`、第 8 张（fact_cutoff 时钟）`0.80` —— 数值不同属不同 as_of 的合理差异，但对外**不可混用**这两个数字。

**异常提示**：⚠️ **`None · None` 字段渲染缺陷**（证据行来源字段渲染为字面 `None`，共 2 处可见）——属 UI bug，对外提交易被评委追问；第 3 条证据链底部截断。

**页脚实见版本**：`v2.5.1`。

**结论**：**带限定可用**（需处理/标注 `None · None` 渲染缺陷，否则不宜原样对外）。

---

## 11. T-23-version-compare-2021clock.png（2026-09-22 04:23 UTC，1440×900，103 KB）

**整体**：决策卡页，问题「糖化血红蛋白的控制目标是多少？」+ 本体 `v1.1.0`，`as_of = 2021/06/01 00:00`，结果为**旧时钟诚实拒绝**。

**图中文字/数字逐条**：
- 输入：**`糖化血红蛋白的控制目标是多少？`** ｜ 本体版本 `v1.1.0` ｜ 日期 **`2021/06/01 00:00`**
- 按钮：`清除` `完整卡` `精简卡` `生成决策卡`
- 结果徽章行：**`证据不足`** ｜ `知识版本: v1.1.0` ｜ **`截止 2021-06-01T00:00:00+00:00`** ｜ **`时钟 explicit`** ｜ `校准 未应用` ⚠️ **本行无 `version_pinned` 标签**
- 标题 `糖化血红蛋白的控制目标是多少？`；黄色框 `证据不足` + **`关键事实项证据全缺：检索路与推理路均未取到支撑证据，按纪律不生成候选。`**
- **0 候选、无置信度**
- 页脚：`Nexent © 2026 · v2.5.1`

**泄密排查**：邮箱 `admin@knowevo.com`（右上，1 处）；无 API key/token/密码；无内网 IP/主机名；**无租户 UUID**；无真实姓名。✅ 无 `N Issues` 徽标。

**一致性核对**：✅ 与台账吻合：`as_of=2021-06-01` → `INSUFFICIENT_EVIDENCE`、0 候选、`时钟 explicit`、`截止 2021-06-01T00:00:00+00:00`（台账 `kg_cutoff=2021-06-01`）✅。⚠️ 台账称「旧时钟 **0 边 0 证据**」，但画面**未显示边数/证据数**，仅文字表述「证据全缺」——**「0 边」不可从画面核验**。⚠️ 台账 card `9ed40a72-2c26-4f56-bac9-f3f6b1ab337c` 画面不出现。⚠️ 台账未记录「本张无 `version_pinned` 标签」，而第 12 张有——属画面事实差异（合理推测：无候选则不产生 pin 戳），但**对外对比图并列时会被看出**。

**异常提示**：无截断、无报错 toast。结果区下方大片留白（0 候选所致）。

**页脚实见版本**：`v2.5.1`。

**结论**：**可用**（无泄密、无 dev 徽标；建议对外并列时注明「无候选故无 version_pinned 戳」）。

---

## 12. T-23-version-compare-2025clock.png（2026-09-22 04:23 UTC，1440×900，151 KB）

**整体**：决策卡页，同题同本体，`as_of = 2025/06/01 00:00`，结果为**建议 + 3 证据**（与第 11 张构成 ideal-shape 判别对）。

**图中文字/数字逐条**：
- 输入：`糖化血红蛋白的控制目标是多少？` ｜ 本体版本 `v1.1.0` ｜ 日期 **`2025/06/01 00:00`**
- 结果徽章行：**`建议`** ｜ `知识版本: v1.1.0` ｜ **`截止 2025-06-01T00:00:00+00:00`** ｜ **`时钟 explicit`** ｜ **`version_pinned`** ｜ `校准 未应用`
- 候选：**`推荐大多数非妊娠成年T2DM患者HbA1c的控制目标为<7%`** + `Top-1` + **`置信度 0.88`**
- 橙色反事实行（**右侧被视口截断**）：`反事实若不采纳HbA1c<7%的控制目标，则失去图谱中关联的血糖控制对糖尿病肾脏病风险降低及非致死性心肌梗死风险降低16%的获益依据；但本卡未提供失败路径，无法进一步量…`
- `证据链` **3** 条（可见 2 条 + 第 3 条截断）：
 1. `EXTRACTED` `kg` `版本钉住` ｜ `推荐大多数非妊娠成年T2DM患者HbA1c的控制目标为<7%` ｜ **`None · None`** ｜ `Indicator:糖化血红蛋白 -> 推荐大多数非妊娠成年T2DM患者HbA1c的控制目标为<7% -> Disease:2型糖尿病 -> 血压控制目标为<130/80 mmHg -> Indicator:血压`
 2. `EXTRACTED` `kg` `版本钉住` ｜ `血糖控制可降低糖尿病肾脏病风险` ｜ **（截断）**
 3. **（截断）**
- 页脚：`Nexent © 2026 · v2.5.1`

**泄密排查**：邮箱 `admin@knowevo.com`（右上，1 处）；无 API key/token/密码；无内网 IP/主机名；**无租户 UUID**；无真实姓名。✅ 无 `N Issues` 徽标。

**一致性核对**：✅ **与台账高度吻合**：`as_of=2025-06-01` → `RECOMMEND`、**1 候选 + 3 证据**、`时钟 explicit`、`截止 2025-06-01T00:00:00+00:00`（台账 `kg_cutoff=2025-06-01`）✅；两版 `clock_source=explicit` 且 cutoff 不同 → **判别门通过** ✅；`ideal_shape=true`（旧时钟诚实拒绝 vs 新时钟证据支撑）**画面成立** ✅。⚠️ 台账 card `109425c1-1f1a-482b-99bf-5528da6ef664` 画面不出现。✅ 文件真实格式核验：确为 PNG 1440×900（与同批一致）。

**异常提示**：⚠️ **`None · None` 渲染缺陷**（1 处可见）；**反事实行右侧截断**（16% 获益后半句 `无法进一步量…` 被裁）；证据链 2/3 条截断。

**页脚实见版本**：`v2.5.1`。

**结论**：**带限定可用**（需处理 `None · None`；反事实句截断建议重拍或裁剪说明）。

---

## 13. t23-eval-report.png（2026-09-21 11:54，1540×840，108 KB）

> 以下 13–16 节由补审智能体（2026-09-23）续审——前轮审到第 12 张中断，本节起为补齐轮。

**整体**：matplotlib 绘制的「E2 四级消融 × 题型」真实跑分柱状图（partial 版），蓝/绿双系列 + 未测格斜体 N/A 标注，自注数据源 JSON。

**图中文字/数字逐条**：
- 主标题：`E2 四级消融 × 题型（真实跑分，partial: 4/16 格有数据）`
- 副标题：`未测格标 N/A (insufficient_data)；E8 版本钉住 Δ=null（两臂未跑满）；数据源: competition/deliverables/e2-ablation-report.json`
- Y 轴 `acc（正确率）` 0.0–1.0；图例 `题型`：`F 题`（蓝）/ `M 题`（绿）
- X 轴四级：`A1 纯RAG` / `A2 +图谱` / `A3 +多跳` / `A4 全量`
- 柱顶数字：A1 `0.79 n=14`（F）、`0.67 n=15`（M）；A2 `0.75 n=12`（F）；A4 `0.50 n=6`（F）
- 斜体 `N/A` 空格标注共 12 处：A1×2、A2×3、A3×4、A4×3
- 页脚：`Nexent-KnowEvo · 图由 e2-ablation-report.json 真实数字生成（matplotlib，2026-09-21）· 构建租户图谱摄取 10/120 段，A3/V/X 待续跑`

**泄密排查**：无 API key/token/密码；无内网 IP/主机名；无真实姓名/手机/邮箱；无租户 UUID（JSON 内 `ablation_tenant=6756b0ab-…` 未上图）；仅仓库相对路径与产品名。**干净**。

**一致性核对**（对照台账 + `e2-ablation-report.json` 实查）：
- ✅ 「由 e2-ablation-report.json 真实数字绘制」成立：A1 by_type F 0.7857→0.79（n_judged=14）、M 0.6667→0.67（15）；A2 F 0.75（12）；A4_full|on|1 F 0.5（6），逐格吻合。
- ✅ 「insufficient_data 格有明确标注」成立：12 格斜体 N/A + 副标题释义；A3 四格全 N/A，且 JSON 中 A3 `acc=0.0`（n_judged=0）未被画成 0 柱，正确规避了假 0。
- ❌ 台账所引 A1 聚合 `acc=0.7241 / pass2=0.70` **未出现在图上**（JSON 聚合值确为该数，但图只画分题型 0.79/0.67）。
- ❌ 副标题「E8 版本钉住 **Δ=null**（两臂未跑满）」与权威口径**直接矛盾**：`e2-ablation-paired2.json`（2026-09-22，same_invocation=true）V 题两臂 acc=1.0 → **Δ=0.0**（已取代 r22 并置 +0.3334，pitfalls #62）。本图生成于 09-21，用的是旧 report 的 `e8` 字段，E8 表述已过期。
- ⚠️ 柱顶 `n=` 实为 `n_judged`（run 数 14/15/12/6），JSON 题数为 5/5/4/2，易误读为题数。
- ⚠️ A4 柱只取 `A4_full|on|1` 臂；JSON 另有 E8 两臂 V 题数据未入图；页脚「A3/V/X 待续跑」口径略含混。

**异常提示**：无模糊/报错/空态。页脚「摄取 10/120 段」是 09-21 时点快照（09-23 已推进到 35/120），整图偏旧——与 `evidence-index.md`「E2 续跑后需重绘」自注一致。

**页脚实见版本**：无平台版本号；产品标识 `Nexent-KnowEvo`；生成标注 `matplotlib，2026-09-21`。

**结论**：**必须重绘**。数字真实、N/A 标注合规，但 E8 Δ=null 与权威 paired2 的 Δ=0.0 直接冲突，且缺 A1 聚合数字。重绘时一并：补 A1 acc=0.7241 / pass2=0.70、E8 改标 Δ=0.0（附 n=6 小样本 + 两臂满分的诚实限制）、`n=` 注明为 run 数、页脚摄取进度更新。

---

## 14. T-27-chat-qa-1.png（2026-09-23 00:57 本地，1440×980，203 KB，md5 8e1bc576…）

**整体**：Nexent「开始问答」真实问答页，会话「糖尿病前期包括哪些情况」第一轮——任务详情（依据说明/推理轨迹）+ 最终答案（includes 关系表）。

**图中文字/数字逐条**：
- 页头：`Nexent` logo｜`Github`｜`ModelEngine`｜`简体中文`｜铃铛｜`admin@knowevo.com`｜头像
- 左导航：`首页`/`开始问答`(选中)/`自动任务`/`智能体开发`/`资源仓库`/`资源管理`；会话列表「今天」两条同名 `糖尿病前期包括哪些情况`（当前条带蓝点）；「最近7天」`新对话`×9、更早 `新对话`×1；底部 `切换新版`
- 标题 `糖尿病前期包括哪些情况`；用户气泡 `糖尿病前期包括哪些情况？请依据知识图谱回答。`
- 任务详情 › 说明（3 条）：①`以上内容全部来自知识图谱实体与边的 claim 字段，共 3 条 includes 相关证据路径，均通过版本一致性校验（version_valid: true）。`②`决策卡工具对同一问题返回 INSUFFICIENT_EVIDENCE，因其检索路与推理路未取到候选证据，故本回答以图谱直接检索结果为准，不额外引用决策卡结论。`③`本回答提供认知辅助，不构成处方建议；临床决策请结合患者具体情况并由执业医师作出。`
- 答案：`依据知识图谱（当前有效视图版本：2026-09-22）中的疾病实体与关系，糖尿病前期通过 includes（包括）关系直接指向两类糖代谢异常状态：`
- 表格 `类别|英文缩写|图谱中的关系依据`：`糖耐量异常（糖耐量减低）|IGT|糖尿病前期 → includes → 糖耐量异常`；`空腹血糖受损|IFG|糖尿病前期 → includes → 空腹血糖受损`
- 底部截断行：`也就是说，糖尿病前期的核心构成是 IFG（空腹血糖受损）+ IGT（糖耐量异常），二者常被统称为糖调节异…`（截断）
- 输入区：`给 Nexent 发送消息`｜`Knowevo 助手`｜`deepseek-flash@sn …`（截断）｜`内容由 AI 生成，请仔细甄别`

**泄密排查**：邮箱 `admin@knowevo.com`（演示账号，前批同款）；模型名 `deepseek-flash@sn …` 被截断，`@sn` 后缀含义不确定；无 API key/token/密码；无内网 IP/主机名；无真实姓名/手机；无租户 UUID。

**一致性核对**：✅ 与台账完全一致——问题、3 条 includes 证据路径、version_valid:true、IGT/IFG 表格全实见；库行 `e2e0a66f-f531-405c-aa8c-56e31a71ad3f`（created 16:55:41 UTC）↔ mtime 00:57 本地（=16:57 UTC）时序成立。

**异常提示**：底部「也就是说…」一行被截断；两条同名会话并列（多轮尝试痕迹）；左下角黑色圆形「N」徽标（疑似 Next.js dev 徽标，不确定）。

**页脚实见版本**：`Nexent © 2026 · v2.5.1`。

**结论**：**可用**。问题、推理轨迹、最终答案三要素齐备、时序互锁成立；仅底部一行截断，展示时避开即可。

---

## 15. T-27-chat-qa-2.png（2026-09-23 00:58 本地，1440×980，212 KB，md5 8c015b14…）

**整体**：同会话（与 qa-1 像素级同标题）滚动后的第二轮——「随机血糖可以用来诊断糖尿病吗？」，含证据口径与限制说明（uncertainty_notes 内嵌 decision_card_t UUID 后缀）。

**图中文字/数字逐条**：
- 用户气泡 `随机血糖可以用来诊断糖尿病吗？请给出图谱证据。`
- 任务详情 › 证据口径与限制说明：①`上述结论均取自知识图谱有效视图 valid_view = 2026-09-22T16:58:23.964908Z；图谱规模为…`（右缘截断，规模数字缺失）②`决策卡工具对同一问题返回 decision = INSUFFICIENT_EVIDENCE（uncertainty_notes: "关键…`（右缘截断）`…43ac-a205-d837546b9ec0）。`③`一句话总结：有典型症状时，随机血糖 ≥ 11.1 mmol/L 可用于诊断糖尿病；无典型症状者按图谱…`（右缘截断）④`本回答为认知辅助，不构成处方建议…`
- 答案区（折叠线下仅露标题）：`随机血糖可以用来诊断糖尿病吗？`
- 输入区同 qa-1

**泄密排查**：邮箱 `admin@knowevo.com`（1 处）；UUID 后缀 `43ac-a205-d837546b9ec0`（1 处）——系 `decision_card_t` 行 `348c8a4d-9d50-43ac-a205-d837546b9ec0` 的**业务主键后缀（非租户 UUID）**，正是台账互锁证据，预期存在；版本戳非敏感；无 key/token/密码/内网 IP/主机名/姓名/手机。

**一致性核对**：✅ 问题与预期一致；✅ UUID 后缀与库行 `348c8a4d-…` **逐字符吻合（互锁铁证成立）**；✅ 11.1 mmol/L 结论句可见；✅ created 16:58:09 UTC ↔ mtime 00:58 本地时序成立。⚠️ 「最终答案」正文未入画（仅露标题）。

**异常提示**：滚动态截图；证据口径面板 **3 处文字右缘被裁**（图谱规模数字、uncertainty_notes 中段、「无典型症状者按图谱…」缺失）；「N」徽标同 14。

**页脚实见版本**：`Nexent © 2026 · v2.5.1`。

**结论**：**带限定可用**。UUID 互锁铁证成立、内容真实；但答案正文未入画、3 处关键句右缘裁断。对外建议补拍一帧含答案全文的滚动图，或限定只引用可见的「一句话总结」句。

---

## 16. T-27-chat-qa-3.png（2026-09-23 01:03 本地，1440×980，185 KB，md5 1a5b18cb…）

**整体**：独立会话「糖尿病空腹血糖诊断标准」——问题「空腹血糖多少可以诊断糖尿病？」，任务详情（工具不一致如实披露）+ 结论 + 直接证据表（被输入框遮挡一部分）。

**图中文字/数字逐条**：
- 标题 `糖尿病空腹血糖诊断标准`；用户气泡 `空腹血糖多少可以诊断糖尿病？请依据知识图谱给出证据。`
- 会话列表「今天」3 条（与 qa-1/qa-2 的 2 条不同，像素级证实为独立会话）
- 任务详情（要点）：①`工具侧不一致提示：decision_card_render 对"空腹血糖诊断糖尿病阈值"类提问返回 INSUFFI…`（右缘截断）`与 kg_multi_hop 的图谱原始证据为准，并如实标注该冲突。`②`图谱未包含"空腹"状态定义（如禁食时长）等细化条件…`（右缘截断）③`本内容为基于知识图谱的认知辅助，不构成处方或诊疗建议…`（右缘截断）
- 结论：`依据当前知识图谱（KG）证据，空腹血糖 ≥ 7.0 mmol/L 可诊断糖尿病。这是图谱中唯一一条明确给出空腹血糖诊断阈值的直接证据。`
- 直接证据（诊断阈值）表：`项目|内容` → `头实体|Examination:空腹血糖（Examination 类）`——后续行被输入框遮挡

**泄密排查**：邮箱 `admin@knowevo.com`（1 处）；本图无 UUID；无 key/token/密码/内网 IP/主机名/姓名/手机。

**一致性核对**：✅ 问题、结论 `≥ 7.0 mmol/L`、「工具侧不一致」主动披露、`Examination:空腹血糖` 实体级证据均与台账吻合；✅ 库行 `962c9cf2-3949-435e-8dc3-5b6259cf837c`（created 17:01:38 UTC）↔ mtime 01:03 本地时序成立。

**异常提示**：任务详情 3 行右缘被裁；直接证据表仅露首行；「N」徽标同 14。

**页脚实见版本**：`Nexent © 2026 · v2.5.1`。

**结论**：**带限定可用**。结论与工具不一致披露实见，但证据表被遮挡 + 右缘裁切。对外建议补拍证据表全貌，或表述限定于可见结论行。

---

## 汇总（16/16 全部审毕，2026-09-23）

| 判定 | 张数 | 文件 |
|---|---|---|
| 必须重拍/重绘 | **5** | `T-01-agent-published`（空态无证据力）、`T23-home-loggedin`（主区空白）、`t23-shot-ontology-workbench`（树区全空）、`t23-shot-ontology-queue`（队列空态）、`t23-eval-report`（重绘：E8 Δ 冲突） |
| 带限定可用 | 5 | `T-01-chat-conversation`（寒暄题，退居附录）、`T-01-models-registered`（槽位多为空）、`t23-shot-skill-template`（裁 N 1 Issue 徽标）、`T-23-version-compare-2026` / `T-23-version-compare-2025clock`（`None · None` + 截断）、`T-27-chat-qa-2` / `T-27-chat-qa-3`（右缘裁断/遮挡） |
| 可用 | 6 | `T-23-decision-card`（裁 N 2 Issues 徽标）、`T-23-version-compare-2024clock-refusal`、`T-23-version-compare-2021clock`、`T-27-chat-qa-1` + 两张带限定的 qa 截图核心证据成立 |

（注：带限定列含 7 张明细、按主要判定归类；逐张口径见各节结论。）

## 附：补审轮取证方法说明（诚实披露）

- 内置多模态读图通道本次出现「陈旧图粘滞」缺陷：两次合成控制实验均失败（读 900×300 红底控制图返回的仍是 1440×980 聊天截图）；降级通道不可用（本地 vision 技能/tesseract/OCR 均未安装）。
- 因此**文件名↔画面绑定**由像素级程序化证据独立重建：①标题条补丁 RMSE（qa-1 与 qa-2 =0.00 同标题；qa-3 =44.37 异标题）；②问题气泡浅蓝底定位（气泡顶/中/顶分布）；③侧栏会话数补丁对比。绑定结论唯一，且与 `decision_card_t` 库行时间戳、文件 mtime 时序互证成立。
- 内容转写可信度外部锚点：图 13 的 4 格数字与 `e2-ablation-report.json` 逐格吻合；图 15 的 UUID 后缀与库行逐字符吻合。凡无法二次核验的细节均标注「不确定」。
- 若需 100% 视觉复核，建议在正常视觉通道下对第 15、16 两张的右缘裁断与遮挡区域做一次复看。
