# 截图重拍计划（Screenshot Reshoot Plan）· Nexent-KnowEvo

> **用途**：任务Q2A 产出。通读 `deliverables/screenshot-review.md`（2026-09-23 逐张 vision 审图）后，对其中判定「必须重拍/重绘」的 5 张图逐张写成拍摄清单（前置数据条件 / 页面路径 / 预期画面元素）。
> ⚠️ **执行边界**：本计划**只规划、不起服务**。实际重拍需要任务Q2B 起服务栈（backend 5010 + PG + ES + minio）后衔接执行——本会话**不起任何服务**，写完计划即停。
> 5 张清单来源：`screenshot-review.md` 汇总表「必须重拍/重绘 = 5 张」。

---

## 1. `T-01-agent-published.png`（当前：空态无证据力 → 必须重拍）

- **前置数据条件**：存在至少一个**已发布**智能体（`knowevo_assistant`，`ag_tenant_agent_t.agent_id=1`）；需以管理员 `admin@knowevo.com` 登录「智能体开发 → 智能体配置」页。
- **页面路径**：平台「智能体开发 → 智能体配置」→ 智能体列表 / 某智能体配置详情页。
- **预期画面元素**：① 可见已发布智能体名称 `knowevo_assistant`（或 Knowevo 助手）与**发布状态标识**；② 配置详情含 duty prompt、绑定的 5 个自研 KG MCP 工具（Local MCP 绑定）；③ 完整左侧业务导航（首页/开始问答/自动任务/智能体开发〔模型/知识库/智能体/记忆/评估〕/资源仓库/资源管理）；④ 顶栏邮箱 `admin@knowevo.com`、页脚版本号。
- **验收**：画面须能支撑「智能体已发布」结论（原图是空态弹层，证据价值近零，故必须重拍）。

## 2. `T23-home-loggedin.png`（当前：主区空白 → 必须重拍）

- **前置数据条件**：管理员登录态；统一账号口径（原图 `suadmin@nexent.com` 与 T-01 的 `admin@knowevo.com` 两套账号并存，重拍须统一）。
- **页面路径**：平台「首页」（登录后）。
- **预期画面元素**：① 首页主内容区**渲染完成**（卡片/快捷入口可见，非空白）；② 完整左侧导航；③ 顶栏账号一致（建议统一用 `admin@knowevo.com` 侧，与 T-01/T-27 一致）；④ 页脚版本号。
- **验收**：主区有实际内容，可体现「登录后首页」；避免空态/加载态被拍入。

## 3. `t23-shot-ontology-workbench.png`（当前：本体树区全空 → 必须重拍）

- **前置数据条件**：对**演示租户**真跑一轮 `build_ontology_round` 出 **≥30 待审提案**（不许造数）；本体工作台本体树已加载 v1.1.0。
- **页面路径**：「资源仓库 → 本体工作台 → 本体树」标签。
- **预期画面元素**：① 本体树渲染出 **≥30 节点**（与 v1.1.0 的 10 类/10 关系对应）；② 左侧导航（本体工作台高亮）+ 版本徽标 `v1.1.0`；③ 图例（蓝框=锚定标准条目 / 灰=已废弃）；④ 无 `Next.js dev` 黑色 `N` 徽标（须 production 构建或裁剪）。
- **验收**：满足 T-05 e2e 截图欠账「本体树 ≥30 节点 + 导航同框」。

## 4. `t23-shot-ontology-queue.png`（当前：队列空态 → 必须重拍）

- **前置数据条件**：同上，存在**真实待审提案**时（与第 3 张同源数据：提案队列 A/X/P 三态条目已生成）。
- **页面路径**：「资源仓库 → 本体工作台 → 提案队列」标签。
- **预期画面元素**：① 提案列表含**真实条目**（非「队列为空」空态）；② 体现 **A（接受）/ X（拒绝）/ P（待定）三态**标签或状态列；③ 与第 3 张同源版本 `v1.1.0`；④ 无 dev 徽标。
- **验收**：可支撑「待审队列 + A/X/P 状态」证据。

## 5. `t23-eval-report.png`（当前：E8 Δ 与权威口径冲突 → 必须重绘）

- **前置数据条件**：E2 四级消融矩阵回填（A3 multihop 待跑；E8 配对终版 `e2-ablation-paired2.json` 已为 Δ=0.0）。**重绘为纯前端/matplotlib 零 LLM 重绘**（先例：t23 零 LLM 重绘 v3）；重绘前建议等任务Q2B 把 E2 全量矩阵回填到 `e2-ablation-report.json`。
- **页面路径**：由 `~/kw-plot/plot_e2_report.py`（仓外生成器）重绘，产物替换本图。
- **预期画面元素（修正项）**：① E8 标注改 `Δ=0.0`（附 n=6 小样本 + 两臂满分诚实限制，旧 Δ=+0.3334 不作数）；② 补 A1 聚合 `acc=0.7241 / pass2=0.70`；③ `n=` 注明为判定 run 数（非题数）；④ 12 格 `insufficient_data` 维持斜体 N/A，**A3 四格不画假 0 柱**；⑤ 页脚摄取进度更新到实际落库段数。
- **验收**：与 `e2-ablation-report.json` / `e2-ablation-paired2.json` 逐格吻合，E8 Δ=null 冲突消除。

---

## 衔接说明（交给任务Q2B）

- 第 1–2、5 张：平台前端页，起 backend + 平台栈即可拍/重绘。
- 第 3–4 张：**必须**先对演示租户真跑 `build_ontology_round` 出 ≥30 待审提案，**不许造数**（数据前置见上）。
- 全 5 张拍/绘完成后，按 `screenshot-review.md` 逐张复核（泄密排查 + 页脚版本号 + dev 徽标裁剪），并登记 `evidence-index.md` 状态列 + `cost-ledger.md`。
- 视觉验收（A4 visual-judge）本轮禁用（与 Q2B 摄取抢 sensenova2 配额 429）；待服务栈收口、provider 恢复后由后续会话执行。
